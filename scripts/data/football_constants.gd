extends Node


# ═══════════════════════════════════════════
#  FIELD — реальные размеры FIFA
# ═══════════════════════════════════════════

const FIELD_LENGTH := 105.0        # полная длина поля (Z)
const FIELD_WIDTH := 68.0          # полная ширина поля (X)
const HALF_FIELD_LENGTH := FIELD_LENGTH / 2.0    # 52.5
const HALF_FIELD_WIDTH := FIELD_WIDTH / 2.0      # 34.0

const GOAL_WIDTH := 7.32           # между стойками
const GOAL_HEIGHT := 2.44          # от газона до перекладины
const GOAL_DEPTH := 2.0           # глубина сетки (визуальная)
const GOAL_POST_RADIUS := 0.08    # толщина штанги

const PENALTY_AREA_DEPTH := 16.5  # от лицевой линии
const PENALTY_AREA_WIDTH := 40.32 # полная ширина

const GOAL_AREA_DEPTH := 5.5
const GOAL_AREA_WIDTH := 18.32

const CENTER_CIRCLE_RADIUS := 9.15
const CENTER_SPOT_RADIUS := 0.15
const PENALTY_SPOT_DIST := 11.0   # от лицевой линии
const PENALTY_ARC_RADIUS := 9.15  # радиус дуги штрафной (от точки пенальти)
const CORNER_ARC_RADIUS := 1.0

const LINE_THICKNESS := 0.12       # ширина линий разметки


# ═══════════════════════════════════════════
#  BALL
# ═══════════════════════════════════════════

const BALL_RADIUS := 0.11
const BALL_MASS := 0.43
const BALL_DRAG := 0.985
const BALL_AIR_RESISTANCE := 0.999
const SHOT_POWER := 18.0
const PASS_POWER := 12.0
const SHOT_Y_UP := 0.3           # подъём мяча при ударе
const PASS_Y_UP := 0.1           # подъём мяча при пасе
const BALL_TRAP_MAX_SPEED := 9.0 # быстрее этого (м/с) мяч не подбирается прочими игроками (адресат паса — минуя)
const FLIGHT_END_SPEED := 2.0    # ниже этой скорости (м/с) полёт завершается → OPEN, коллизия с игроками off.
								 # НАМНОГО ниже trap-порога: пока мяч летит быстро — он блокируется игроками
								 # (стенка), а не «пролетает сквозь», выпав из FLIGHT слишком рано
const MAGNUS_FORCE := 4.0         # масштаб бокового/подъёмного импульса кручения в полёте (эфф. боковое ускор. ≈ _curl.z*это)
const MAGNUS_DECAY := 0.998      # затухание вектора _curl за физкадр (близко к 1 → вращение держится
								 # весь полёт → поздний S-хук у ворот, а не ранний загиб с ноги)


# ═══════════════════════════════════════════
#  SHOOTING (Фаза 1 — три удара + прицел + вынос)
# ═══════════════════════════════════════════

# --- Прямой удар (bullet) / прицел / вынос ---
const SHOT_POWER_MIN := 28.0        # горизонтальная скорость при мин. заряде, м/с (твёрдый пол — слабый удар не вялый)
const SHOT_POWER_MAX := 42.0        # при полном заряде (сильнее и дальше)
const SHOT_AIM_Y_MIN := 0.8         # высота точки прицела при мин. заряде, м (не по земле)
const SHOT_OVER_LIFT := 1.0         # на макс. заряде цель поднимается на height+это (риск через перекладину), м
const SHOT_SCATTER_BASE := 0.9      # базовый разброс прицела, м
const SHOT_SCATTER_DIST_REF := 22.0 # дистанция, на которой разброс базовый, м
const SHOT_ASSIST := 0.5            # «магнит» удара к створу: 0 — без помощи (промах возможен), 1 — всегда в раму
const SHOT_ASSIST_MARGIN := 0.4     # насколько внутрь рамы (м от штанг/перекладины) держать притянутую цель
const SHOT_AIM_SENSITIVITY := 2.5   # чувствительность горизонтального прицела: наклон стика → штанга (0 = центр); больше = угол достигается меньшим наклоном
const SHOT_GROUND_CHARGE_MAX := 0.25 # заряд ниже этой доли → прямой/кручёный удар идёт НИЗОМ (по газону); ≈0.125с при KICK_CHARGE_MAX_TIME=0.5
const SHOT_GROUND_AIM_Y := 0.25     # высота прицела удара низом, м (у самой травы)
const SHOT_GROUND_POWER := 40.0     # сила настильного удара низом, м/с (короткий тап — но бьёт крепко)
const CLEARANCE_ZONE_DIST := 32.0   # дальше этого до чужих ворот → вынос
const CLEARANCE_FACING_DOT := 0.2   # facing·(на ворота) ниже → вынос
const CLEARANCE_POWER := 26.0       # сила выноса, м/с
const CLEARANCE_LIFT := 6.0         # подъём выноса, м/с

# --- Кручёный (Magnus) ---
# Модель «в дальнюю девятку»: прицел выносится ЗА штангу (CURL_AIM_OUT), а инсвинг Magnus
# заносит мяч обратно В угол. Так дуга выразительная, но мяч всё равно садится в угол
# (усилить дугу = поднять STRENGTH и синхронно AIM_OUT, иначе перетянет в центр / уйдёт мимо).
const CURL_STRENGTH_MIN := 1.0      # боковая сила кручения при мин. заряде (в единицах _curl.z)
const CURL_STRENGTH_MAX := 2.3      # при полном заряде (сильнее закручивает)
const CURL_LIFT := 0.0              # вертикальный Magnus (0: дуга уже есть из баллистики, иначе мяч спиралит в небо)
const CURL_AIM_OUT := 4.0           # на сколько метров ЗА штангу целиться при полном заряде (инсвинг вернёт в угол)

# --- Черпачок (chip) ---
const CHIP_PEAK_MIN := 2.4          # высота параболы при мин. заряде, м (пониже — настильнее)
const CHIP_PEAK_MAX := 5.2          # при полном заряде (выше перекидывает)
const CHIP_DIST_MIN := 18.0         # дальность приземления при мин. заряде, м (твёрдый пол — слабый черпачок не совсем близко)
const CHIP_DIST_MAX := 62.0         # при полном заряде (сильный — далеко перекидывает)


# ═══════════════════════════════════════════
#  PLAYER
# ═══════════════════════════════════════════

const PLAYER_SPEED := 8.0
const PLAYER_HEIGHT := 1.8
const PLAYER_RADIUS := 0.3
const PLAYER_CAPSULE_HEIGHT := 1.5
const PLAYER_ROTATION_SPEED := 10.0
const PLAYER_START_Z := 0.0         # стартовая позиция Z

# --- Living locomotion (velocity+inertia, рецепт OpenSoccer; тюнинг-старт) ---
const LOCO_TOP_SPEED := 8.0          # обычная максимальная скорость, м/с
const LOCO_SPRINT_SPEED := 12.0      # максимальная при спринте (≈1.5×)
const LOCO_ACCEL := 25.0             # разгон, м/с² (≈0.32 с до полной)
const LOCO_DECEL := 20.0             # торможение, м/с² (мягче разгона → глайд)
const LOCO_TURN_ROT := 18.0          # темп доворота тела, 1/с
const LOCO_TURN_MIN_SPEED := 1.0     # ниже этой скорости не доворачиваемся
const LOCO_MAX_BANK_DEG := 20.0      # макс. крен корпуса в повороте, градусы
const LOCO_BANK_SMOOTH := 8.0        # сглаживание крена, 1/сек (гасит рывки от дискретного WASD-ввода)
const LOCO_RUN_SCALE_FUDGE := 1.33   # анти-слайд: scale = (speed/top)*fudge
const LOCO_RUN_ANIM_SPEED := 1.5     # порог входа в стейт run, м/с
const LOCO_SPRINT_ANIM_SPEED := 9.0  # порог входа в стейт sprint, м/с
const PLAYER_COLLISION_MASK := 2     # слой полевых игроков (bit2): бьются только друг о друга
const BOUNDARY_COLLISION_LAYER := 4  # слой границ поля (bit3): отдельно от мяча/питча (слой1),
									 # чтобы игрок упирался в стены, но НЕ толкал мяч физически


# ═══════════════════════════════════════════
#  DRIBBLING
# ═══════════════════════════════════════════

const DRIBBLE_FORWARD_DIST := 0.5
const DRIBBLE_HEIGHT := 0.08
# Дриблинг — НЕПРЕРЫВНОЕ удержание мяча впереди (lead), а не дискретные толчки. Дистанция
# удержания РАСТЁТ со скоростью: медленно/стоя мяч у ног (плотный контроль, нельзя «пробежать
# мимо» — мяч следуется), на бегу/спринте — далеко впереди (сложнее контроль). Когда мяч ушёл
# дальше CHASE (вырвался на скорости/отскочил) — свободно катится, игрок бежит догонять
# (гистерезис с CATCH); догнал (<CATCH) — снова под контролем. СТИК задаёт направление удержания.
const DRIBBLE_CATCH_DIST := 0.5       # ближе этого (м) — снова под контролем (конец догона)
const DRIBBLE_CHASE_DIST := 1.1       # дальше этого (м) — мяч вырвался, игрок бежит догонять
const DRIBBLE_LEAD_BASE := 0.5        # удержание впереди на месте/медленно, м
const DRIBBLE_LEAD_PER_SPEED := 0.08  # прирост удержания на 1 м/с скорости игрока (выше = мяч дальше на бегу)
const DRIBBLE_CONTROL_GAIN := 12.0    # жёсткость подтягивания мяча к точке удержания
const DRIBBLE_CONTROL_LERP := 0.4     # сглаживание [0..1]: ниже = «живее»/с лагом, выше = плотнее/жёстче
const DRIBBLE_ROLL_DRAG := 0.97       # трение качения, пока мяч вырвался (фаза догона)
const DRIBBLE_SPRINT_PUSH_EXTRA := 2.0  # спринт добавляет к «скорости удержания» → мяч дальше вперёд (сложнее контроль)
# Спринт-неточность: выигрывая в скорости, игрок иногда теряет в точности — плавный случайный
# увод направления и дистанции удержания мяча, масштаб по силе спринта (на обычном беге = 0).
const DRIBBLE_SPRINT_WOBBLE_ANGLE := 22.0  # макс. случайный увод направления удержания при полном спринте, градусы
const DRIBBLE_SPRINT_WOBBLE_LEAD := 0.9    # макс. случайная дельта дистанции удержания при полном спринте, м
# Владение НЕ теряется от собственного рывка/резкого поворота — только по внешнему фактору
# (подкат/перехват зовут release_dribble отдельно). Флаг chasing + авто-догон возвращают мяч;
# дистанционный сброс — лишь страховка для реально улетевшего мяча (выше любой дистанции удержания).
const DRIBBLE_KEEP_DIST := 15.0   # дальше этого (м) владелец отпускает мяч (только совсем улетевший)
const DRIBBLE_CHASE_STEER := 0.25  # доля подруливания стиком в фазе догона [0..1]: 0 = строго к мячу (сильнее магнит), выше = свободнее обойти соперника
# Притягивание управляемого игрока к НИЧЕЙНОМУ мячу (вне владения): подруливаем направление
# движения к мячу, если игрок идёт примерно к нему — чтобы не целиться точно и проще подбирать.
const PICKUP_ASSIST_RADIUS := 3.0    # в пределах этого (м) до бесхозного мяча включается подруливание
const PICKUP_ASSIST_STRENGTH := 0.8  # макс. доля подруливания к мячу (у самого мяча; линейно спадает к радиусу)
const SPRING_STIFFNESS := 300.0
const SPRING_DAMPING := 15.0
const SPRING_MAX_FORCE := 50.0
const DRIBBLE_DRAG := 0.995
const DRIBBLE_ACQUIRE_DIST := 1.0
const DRIBBLE_LOSE_DIST := 3.0
const DRIBBLE_RELEASE_COOLDOWN_MSEC := 500
const KICKER_REACQUIRE_COOLDOWN_MSEC := 1500


# ═══════════════════════════════════════════
#  MATCH
# ═══════════════════════════════════════════

const MATCH_DURATION_MIN := 90
const HALF_DURATION_MIN := 45
const HALFTIME_DURATION_MIN := 15
const RESET_BALL_Y := 0.5


# ═══════════════════════════════════════════
#  AI
# ═══════════════════════════════════════════

const DEBUG_DISABLE_OPPONENT := true # ВРЕМЕННО: true — ИИ-соперник просто стоит + стенки болванок (тест дриблинга/ударов)
const DEBUG_MARK_TEAMMATE := false    # ВРЕМЕННО: true → соперник маркирует ИИ-тиммейта (встаёт в линию паса) — тест пасов/перехватов/приёма под давлением
const DEBUG_DISABLE_TEAMMATE := true  # ВРЕМЕННО: true — ИИ-тиммейт не спавнится (удобнее тестить вратаря один-в-один)
const DEBUG_MARK_DISTANCE := 2.5      # дистанция маркировки: соперник встаёт между мячом и тиммейтом на столько метров
const DEBUG_BALL_TRAIL := true        # ВРЕМЕННО: цветной след за мячом (виден путь удара) — для тестов траекторий
const DEBUG_BALL_TRAIL_POINTS := 90   # длина следа в кадрах истории позиций мяча
const AI_SPEED := 5.0
const AI_SHOOT_RANGE := 25.0
const AI_DRIBBLE_SPEED_FACTOR := 0.8
const AI_KICK_COOLDOWN := 0.8
const AI_KICK_POWER := 10.0
const AI_ACQUIRE_RANGE := 1.8


# ═══════════════════════════════════════════
#  SLIDE TACKLE
# ═══════════════════════════════════════════

# Скорость/дистанция скольжения подобраны так, чтобы физический слайд (move_and_collide)
# занимал заметную часть длины клипа tackle (~1.8с), а не долетал до конца дистанции за
# долю секунды, пока клип ещё продолжает играть — раньше (SPEED=18.0) слайд укладывался
# в ~0.22с, и игрок визуально телепортировался в точку, а затем ещё ~1.6с доигрывал
# подкат стоя на месте. Первая попытка (SPEED=5.0) была МЕДЛЕННЕЕ реальной скорости бега
# соперника (simple_ai.gd @export var speed = 6.5 — константа AI_SPEED=5.0 выше по файлу
# им не используется, легаси) — подкат сзади (сближение вдогонку) физически никогда не
# догонял цель, хотя сбоку (сближение поперёк) работал. SPEED поднят с запасом выше 6.5,
# чтобы вдогонку соперника хватало. RECOVERY_TIME подобран так, чтобы суммарный commit
# (слайд + recovery) остался близко к длине клипа, как было тюнено раньше (~1.7-1.8с).
const SLIDE_TACKLE_SPEED := 9.0
# Было 4.0, затем 5.5 — увеличено для более дальнобойного подката. RECOVERY_TIME
# уменьшен в ответ, чтобы суммарный commit (слайд + recovery = RANGE/SPEED +
# RECOVERY_TIME) остался близко к длине клипа tackle (~1.8с), как и раньше — иначе
# слайд физически доезжает уже после того, как анимация доиграла до конца, и тело
# едет по инерции со "замёрзшими" ногами.
const SLIDE_TACKLE_RANGE := 6.5
# Диагностический флаг (подкат на месте, без move_and_collide) для изоляции анимации
# tackle от скольжения — см. tools/merge_mixamo.py IN_PLACE_CLIPS. Причина зависания
# найдена и исправлена там (заморозка вертикали Hips для tackle убрана), возвращаем
# обычное поведение со скольжением.
const SLIDE_TACKLE_INPLACE := false
const SLIDE_TACKLE_RECOVERY_TIME := 1.02
const SLIDE_TACKLE_AREA_RADIUS := 1.5
# Порог сбивания игрока-соперника с ног (match_manager._check_tackle_player_hit) —
# НАМНОГО теснее SLIDE_TACKLE_AREA_RADIUS выше. Тот радиус щедрый специально (дотянуться
# до мяча), но раньше от него же ронялся и игрок — с разницей центров тел до ~1.5м, когда
# капсулы (radius=0.3 у каждой, см. _setup_away_player/_setup_teammate) визуально даже не
# соприкасались.
# 0.9 ≈ сумма радиусов капсул (0.6) + небольшой запас, чтобы не мазать по кадру.
const SLIDE_TACKLE_HIT_RADIUS := 0.9
const SLIDE_TACKLE_BALL_DIR_Y := 0.15
const SLIDE_TACKLE_BALL_POWER := 7.0
# Было 2.5 — решение "подкатывать" принималось иногда слишком издалека, и с добавленным
# упреждением цели (_start_tackle) чем дальше цель в момент решения, тем грубее оценка
# lead_time (нет итерации схождения). Урезано, чтобы ИИ решался ближе — упреждение точнее.
const AI_TACKLE_RANGE := 2.0
const AI_TACKLE_COOLDOWN := 2.0


# ═══════════════════════════════════════════
#  TACKLE FALL / GRAVITY (тюнинг-старт)
# ═══════════════════════════════════════════

const GRAVITY := 20.0                 # аркадная гравитация, м/с²
# Анимационное падение жертвы подката (Path B: физ-ragdoll несовместим с масштабированным
# Mixamo-скелетом в Godot). Фаза knockdown держит позу «лежит» (fallen_idle) и отбрасывает
# тело кодом, затем 2 переката и вставание.
const KNOCKDOWN_TIME := 0.5           # длительность фазы «сбит/лежит» до первого переката, с
const KNOCKBACK_DISTANCE := 1.0       # отброс тела в сторону от подкатчика за knockdown, м
const ROLL_DISTANCE := 1.2            # смещение тела за один перекат, м
# Слой физкостей для дремлющей библиотеки RagdollSkeleton (сейчас в рантайме не строится —
# оставлено на случай, если ограничение Godot с масштабом скелета будет обойдено).
const RAGDOLL_COLLISION_LAYER := 8    # bit4: маскирует слой пола (BOUNDARY_COLLISION_LAYER=4)


# ═══════════════════════════════════════════
#  PASSING (тюнинг-старт)
# ═══════════════════════════════════════════

# Базовая скорость мяча (м/с) по типам паса. launch_ground/launch_lob получают её параметром.
const PASS_LOB_PEAK_HEIGHT := 3.0     # высота дуги навеса, м
const PASS_THROUGH_AIR_PEAK_HEIGHT := 2.5  # высота дуги верхового паса на ход
# Пасы «на ход» — РУЧНЫЕ: направление строго по стику, дальность/сила по ЗАРЯДУ, без автонаводки/
# разброса. Хендофф+receive-assist сохранены (принимающий выходит на мяч = бег на мяч; удар на бегу
# → очередь, а не подкат). Дальность = стик * lerp(MIN,MAX,заряд). Верховой (LB+Y) добивает дальше.
const PASS_THROUGH_AIR_SPACE_MIN := 10.0   # верховой: дальность по стику при тапе, м
const PASS_THROUGH_AIR_SPACE_MAX := 52.0   # верховой: полный заряд — ~пол поля (HALF_FIELD_LENGTH=52.5)
const PASS_THROUGH_SPACE_MIN := 10.0       # наземный: дальность по стику при тапе, м
const PASS_THROUGH_SPACE_MAX := 35.0       # наземный: полный заряд

# Низовой пас теперь ВСЕГДА долетает на заданную дистанцию — скорость выводится из
# дистанции и времени полёта (не из фиксированной силы), заряд лишь меняет время полёта
# (мягче/жёстче), а не «долетит/не долетит». См. PassSystem.ground_pass_speed.
const PASS_GROUND_MIN_TRAVEL_TIME := 0.3   # время полёта при полном заряде (жёсткий, быстрый пас), с
const PASS_GROUND_MAX_TRAVEL_TIME := 0.7   # время полёта при минимальном заряде (мягкий, медленный пас), с
const PASS_GROUND_MIN_SPEED := 15.0        # нижний предел скорости мяча низом, м/с (твёрдый пол — короткий пас тоже крепкий)
const PASS_GROUND_MAX_SPEED := 40.0        # верхний предел скорости мяча низом, м/с
const PASS_GROUND_LIFT := 1.5              # лёгкий подъём мяча с газона при низовом пасе, м/с (не прилипает к траве)
const PASS_LEAD_SPEED_ESTIMATE := 12.0     # типичная скорость мяча для оценки времени упреждения (lead_point) — НЕ финальная скорость запуска

# Заряд множит базовую силу/высоту в диапазоне [MIN..MAX] по доле заряда.
const PASS_CHARGE_MAX_TIME := 0.6
const PASS_POWER_CHARGE_MIN := 0.95   # доля силы (высоты дуги навеса) при мгновенном отпускании — твёрдый пол, слабый навес не вялый
const PASS_POWER_CHARGE_MAX := 1.6    # доля силы при полном заряде

# Авто-наводка.
const PASS_LEAD_GAIN := 0.25          # упреждение цели по её скорости (сек)
const PASS_THROUGH_EXTRA_LEAD := 4.0  # доп. вынос точки «на ход» вперёд по скорости цели, м
const PASS_DOT_BIAS := 0.15           # штраф за дистанцию в score выбора цели (0 → чистый dot)
const PASS_MAX_RANGE := 45.0          # дальше цель не рассматривается, м

# Коридор перехвата.
const PASS_CORRIDOR_HALF_WIDTH := 1.2 # полуширина коридора у точки паса, м
const PASS_CORRIDOR_SPREAD := 0.06    # прирост полуширины на метр вдоль паса

# Разброс точности.
const PASS_SPREAD_BASE := 12.0        # макс. разброс угла, градусы (при assist=0, дальней дистанции)
const PASS_SPREAD_DIST_REF := 25.0    # дистанция, на которой разброс достигает базового
const PASS_ASSIST := 0.75             # 0..1 «лёгкость»: 1 → почти без разброса

# Receive-assist (доводка принимающего к мячу).
const PASS_RECEIVE_PREDICT_WINDOW := 0.04  # на сколько сек вперёд предсказываем позицию мяча (queue-автобег Фазы 2)
const PASS_RECEIVE_LEAD_TIME := 0.2        # на сколько сек вперёд по скорости мяча ведём точку приёма (перехват)
const PASS_RECEIVE_ONLINE_DOT := 0.9       # |dot| выше → мяч летит почти в/от нас → встречаем на линии, без упреждения вбок
const PASS_RECEIVE_MAX_TIME := 2.0         # страховочный таймаут фазы приёма, сек
# Мяч теперь летит намного быстрее общего радиуса подбора (DRIBBLE_ACQUIRE_DIST=1.0) —
# receive-assist может не успеть довести игрока ровно на линию мяча за короткое время
# полёта (игрок топ-спидом 8-12 м/с физически не угонится за мячом на 20-40 м/с). Даём
# ИМЕННО адресату паса более широкий радиус подбора, чтобы быстрый, но разумно нацеленный
# пас не проносило мимо. Не трогает общий подбор (DRIBBLE_ACQUIRE_DIST) — соперник и
# случайные подборы вне паса по-прежнему используют узкий радиус.
const PASS_RECEIVE_CATCH_RADIUS := 1.5     # радиус подбора мяча получателем во время receive-assist, м

# Give-and-go («стенка»).
const PASS_WALL_WINDOW := 3.0         # окно возврата, сек
const PASS_WALL_RUN_FORWARD := 10.0   # вынос рывка отдавшего вперёд по атаке, м
const PASS_WALL_RUN_LATERAL := 6.0    # вынос рывка в сторону, м

# Активный перехват ИИ-соперника (честный, не читерский).
const AI_INTERCEPT_REACT := 0.3       # задержка реакции соперника на пас, сек
const AI_INTERCEPT_CHANCE := 0.8      # шанс среагировать (иначе «зевает»)


# ═══════════════════════════════════════════
#  ACTION QUEUE (Фаза 2 — удар/пас «в одно касание»)
# ═══════════════════════════════════════════
const QUEUE_BALL_SPEED_GAIN := 0.02   # прибавка к charge_ratio за 1 м/с скорости влетающего мяча
const QUEUE_MAX_TIME := 6.0           # сколько (с) отложенное действие ждёт касания до автоотмены
const QUEUE_REACH_RADIUS := 2.0       # игрок в этом радиусе (м) от мяча → «дотянулся» → бьём в одно касание
const QUEUE_CONSIDER_RADIUS := 8.0    # ничейный/летящий мяч в этом радиусе (м) → можно ставить в очередь (борьба)


# ═══════════════════════════════════════════
#  CAMERA
# ═══════════════════════════════════════════

const CAMERA_FOLLOW_SPEED := 3.0
const CAMERA_HEIGHT := 25.0
const CAMERA_Z_OFFSET := 3.0
const CAMERA_TILT := -55.0         # градусов

# Камера от 3-го лица за управляемым игроком (пивот встаёт позади него и смотрит вперёд).
const CAMERA_TP_DISTANCE := 7.0    # отступ камеры назад от игрока, м
const CAMERA_TP_HEIGHT := 3.5      # высота камеры над игроком, м
const CAMERA_TP_LOOK_AHEAD := 3.0  # насколько вперёд игрока смотрит камера, м
const CAMERA_TP_FOLLOW := 6.0      # темп сглаживания следования, 1/с


# ═══════════════════════════════════════════
#  GOAL NET — объёмная сетка + Verlet-колыхание
# ═══════════════════════════════════════════

const NET_DEPTH := 1.5             # глубина коробки ворот за линией (м)
const NET_WIDTH_DIV := 24          # ячеек решётки по ширине ворот (было 8)
const NET_HEIGHT_DIV := 12         # ячеек по высоте (было 6)
const NET_DEPTH_DIV := 5           # ячеек по глубине (было 3)

const NET_GRAVITY := 9.8           # ускорение провисания сетки (м/с²)
const NET_DAMPING := 0.98          # затухание скорости узла за шаг Verlet
const NET_SPRING_STIFFNESS := 400.0 # жёсткость структурных пружин
const NET_SHAPE_RETURN := 3.0      # тяга к исходной форме (снижено с 8, чтобы колыхание было свободнее)
const NET_SIM_STEP := 0.016        # dt шага симуляции (с)
const NET_CONSTRAINT_ITERATIONS := 2   # проходов relaxation за шаг (снижено с 4 — мягче/живее)
const NET_STIFFNESS := 0.2         # жёсткость связей 0..1: НИЗ = сетка свободно колышется/пружинит, 1.0 = жёсткая (стена)
const NET_SLACK := 0.9              # запас материала сетки (1.0 = барабан, меньше = провисает свободнее)
const NET_BALL_RADIUS := 0.6       # радиус влияния мяча на узлы (м)
const NET_BALL_VEL_SCALE := 0.4    # множитель толчка от скорости мяча (было 0.3)
const NET_BALL_MIN_SPEED := 1.5    # мяч медленнее этого (м/с) НЕ толкает сетку (лежащий мяч не колышет)
const NET_BALL_FORCE := 120.0      # базовая сила толчка мяча по нормали

const NET_BOUNCE := 0.15           # упругость коллайдеров-стопперов сетки
const NET_FRICTION := 1.0          # трение стопперов (гасит мяч в сетке)
const NET_CELEBRATION_TIME := 5.0  # пауза празднования до сброса мяча (с)


# ═══════════════════════════════════════════
#  KEEPER (вратарь — тюнинг-старт)
# ═══════════════════════════════════════════

# Позиционирование на линии.
const KEEPER_ALERT_DIST := 30.0        # мяч ближе этого (м) к воротам → режим «тревоги»: стойка готовности + приставные шаги; дальше — обычный idle
const KEEPER_LINE_NARROW_GAIN := 0.08  # на сколько метров выхода с линии теряется за 1 м удаления мяча
const KEEPER_MAX_OFF_LINE := 2.5       # макс. выход из ворот в поле, м
# Досягаемость / зоны сейва.
const KEEPER_REACH := 0.85             # радиус ловли/захвата мяча, м — вытянутая рука (капсула 0.3 +
									   # мяч 0.11 + рука ~0.4). Было 1.4 — вратарь «пылесосил» всё,
									   # что пролетало в полутора метрах, даже мимо него.
const KEEPER_SAVE_AREA_Y := 1.0        # высота центра сейв-зоны, м (грудь) — сфера накрывает низ (колено) и верх (перекладина)
const KEEPER_SCOOP_HEIGHT := 0.6       # мяч ниже этого (м) ловится подбором scoop (низом в ноги)
const KEEPER_JUMP_REACH := 2.5         # выше этого (м) центральный мяч не берётся — прыжок мимо (miss_top).
									   # Чуть ВЫШЕ перекладины (GOAL_HEIGHT 2.44): любой мяч в створе достаётся
									   # прыжком с руками (catch_top); miss_top — только для мячей реально над воротами.
const KEEPER_HIGH_CATCH_CERTAIN := 2.0 # ниже этой (м) центральный мяч ловится ГАРАНТИРОВАННО.
const KEEPER_HIGH_CATCH_CHANCE := 0.5  # в полосе CERTAIN..JUMP_REACH (2.0..2.5) шанс поймать; иначе прыжок мимо.
const KEEPER_DIVE_RANGE := 5.5         # макс. боковой прыжок, м (шире створа — дотягивается активнее)
const KEEPER_ENGAGE_MARGIN := 0.5      # вратарь всё равно ныряет за мячом в пределах этого (м) за створом:
									   # только «впритирку со штангой». Было 2.0 — вратарь сейвил (и ловил)
									   # явные удары мимо ворот.
const KEEPER_HIGH_THRESHOLD := 1.3     # выше этой высоты (м) сейв считается «верхним»
const KEEPER_HEAD_THRESHOLD := 1.9     # ловля 1.3..1.9 м — на уровне головы (catch_head); выше — catch_top (прыжок с руками)
# Нырок.
# Модель нырка как в OpenSoccer: импульс тела ПРОПОРЦИОНАЛЕН смещению до точки (divevel =
# смещение × gain), затем затухает — близкий/дальний нырок доезжают за ~одинаковое время, что
# само синхронизирует их с клипом ФИКСИРОВАННОЙ скорости (без динамического масштабирования).
const KEEPER_DIVE_GAIN := 1.6          # импульс = смещение × это (больше = резче/дальше долетает)
const KEEPER_DIVE_VERT_MULT := 2.0     # вертикаль импульса ×это (верховой нырок взлетает выше)
const KEEPER_DIVE_SPEED := 22.0        # верхний предел скорости броска (limit_length), м/с
const KEEPER_DIVE_DECAY := 0.98        # затухание импульса за физкадр (как OpenSoccer)
const KEEPER_DIVE_ANIM_SPEED := 1.5    # фиксированная скорость клипа нырка (1.0 = натуральная; тюнинг-ручка)
const KEEPER_DIVE_LEAD := 0.08         # запас тайминга старта нырка, с (не используется — ныряем сразу после реакции)
const KEEPER_CATCH_LEAD := 0.4         # за столько (с) до подлёта мяча запускаем клип ловли на месте,
									   # чтобы руки успели подняться (особенно catch_top). Захват мяча —
									   # всё равно по узкому KEEPER_REACH, упреждение только для анимации.
const KEEPER_MISS_LEAD := 0.75        # то же для miss_top — прыжок вверх мимо: замах дольше, чем у ловли,
									   # поэтому стартуем раньше, иначе вратарь прыгает уже вслед мячу.
const KEEPER_REACT := 0.12             # задержка реакции на удар, с
const KEEPER_SAVE_ERROR := 0.5         # разброс точки нырка для побиваемости, м
# Сейв.
const KEEPER_CATCH_MAX_SPEED := 18.0   # ниже этой скорости мяч в нижнем углу ловится, выше — отбивается, м/с
const KEEPER_PARRY_DAMP := 0.3         # доля скорости мяча после отбоя
# Ввод мяча.
const KEEPER_HOLD_TIME := 1.0          # пауза с мячом в руках до выноса, с
const KEEPER_CLEAR_SPEED := 66.0       # горизонтальная скорость выноса (drop kick), м/с (×3 — дальше)
const KEEPER_CLEAR_LIFT := 18.0        # вертикальная скорость выноса, м/с (×3 — выше)
const KEEPER_PASS_DISTANCE := 20.0     # дальность раската мяча рукой по низу (keeper_pass), м — временно в центр поля
const KEEPER_PLACE_DRIBBLE_DIST := 10.0 # ВРЕМЕННО: сколько метров вратарь ведёт мяч после placing ball до полевого паса
const KEEPER_THROW_DISTANCE := 25.0    # дальность броска верхом рукой (keeper_overhand_throw), м — к центру поля
const KEEPER_THROW_PEAK := 6.0         # высота дуги броска верхом, м


# ═══════════════════════════════════════════
#  PENALTY (Фаза A)
# ═══════════════════════════════════════════

# Разбег/анимация (замерено tools/measure_penalty_runup.gd на клипе penalty_kick_*).
# PEN_RUNUP_DIST = накопленный разбег таза к моменту контакта (пред-расстановка на эту дистанцию
# назад → тело синхронно с анимацией к кадру удара). Тайминг контакта/возврата — в ACTION_TIMING
# ("penalty_l"/"penalty_r") в player_visual.gd, не здесь.
const PEN_RUNUP_DIST := 2.8          # разбег таза к контакту (мировые метры), кладём бьющего на неё назад
const PEN_ROOT_SCALE := 0.0367        # raw get_root_motion_position → игровые метры (Hips-трек в сырых единицах)
const PEN_DEFAULT_FOOT := "penalty_r"  # нога по умолчанию (ключ ACTION_CLIPS)
const PEN_FOOT_LATERAL := 0.4          # смещение бьющего вбок под опорную ногу (правая → левее), м

# Прицел / разброс.
const PEN_RETICLE_SPEED := 6.0         # скорость движения маркера по плоскости ворот, м/с
const PEN_RETICLE_RETURN := 12.0       # темп возврата маркера в центр без ввода стика, 1/с (больше = быстрее)
const PEN_RETICLE_START_Y := 1.2       # стартовая высота маркера (центр створа), м
const PEN_AIM_OVERHANG := 0.6          # овершут прицела за штангу/перекладину, м
const PEN_SPREAD_MIN_R := 0.15         # радиус круга разброса при мин. силе, м
const PEN_SPREAD_MAX_R := 1.6          # радиус круга при полной силе, м

# Сила удара.
const PEN_POWER_MIN_SPEED := 24.0      # скорость мяча при мин. заряде, м/с
const PEN_POWER_MAX_SPEED := 44.0      # при полном заряде, м/с
const PEN_CHARGE_MAX_TIME := 0.9       # макс. время заряда силы, с (чуть дольше обычного удара — прицельно)

# Черпачок.
const PEN_CHIP_PEAK_MIN := 2.5         # высота дуги черпачка при мин. заряде, м
const PEN_CHIP_PEAK_MAX := 5.0         # при полном заряде, м
const PEN_CHIP_OVERSHOOT := 1.5        # приземление ЗА линией ворот (в сетке), м — чтобы на линии мяч был ещё в воздухе с ходом (уверенно залетал, а не падал у линии)

# Зоны нырка вратаря (репрезентативные точки на линии).
const PEN_KEEPER_DIVE_LOW_Y := 0.4     # высота «низового» угла, м
const PEN_KEEPER_DIVE_HIGH_Y := 1.9    # высота «верхнего» угла, м
const PEN_KEEPER_DIVE_LATERAL := 2.6   # боковое смещение угла от центра, м

# Фикс-камера пенальти (за бьющим на ворота).
const PEN_CAM_BACK := 9.0              # отступ камеры назад от точки удара (от ворот), м
const PEN_CAM_HEIGHT := 4.0            # высота камеры, м
const PEN_CAM_LOOK_Y := 1.2           # высота точки, куда смотрит камера в воротах, м

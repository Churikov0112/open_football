# Расхождения с оригиналом

Единственный дом для мест, где порт **сознательно** отличается от GameplayFootball. По образцу
[[константы]]: другие страницы ссылаются сюда, а не заводят свой список.

Заведена 2026-08-04 вместе с решением портировать игру целиком (спека
`docs/superpowers/specs/2026-08-04-full-port-scope-roadmap-design.md`). До этого расхождения были
рассыпаны по [[открытые-вопросы]] и комментариям в коде.

## Зачем страница существует

Две причины, и вторая важнее.

1. **Правило «ноль тюнинга» действует до конца порта.** Числа берутся из оригинала как есть, даже когда
   кажутся неверными. Отступление допустимо только вынужденное — техническая невозможность, — а не по
   вкусу. Раз отступлений мало и каждое обосновано, они должны быть перечислимы.
2. **Это белый список для диффа трасс.** Приёмка фаз 5–11 — сравнение трассы нашего порта с трассой
   патченого оригинала (см. [[порт-gameplayfootball]]). Известные расхождения обязаны исключаться из
   отчёта, иначе дифф зашумляется и его перестают читать.

**Правило ведения:** новое расхождение допускается только вместе со строкой в этой таблице. Расхождение,
которое сняли, — удаляется, а не помечается сделанным.

Машиночитаемая выжимка отсюда — `tools/trace_whitelist.txt` (`<колонка>|<условие>|<ссылка>`, условие —
`always`, `tick<N`, `tick>N` или `kind=B`). Ведётся руками и **строго вторична**: строка попадает в
файл, только если ей соответствует строка таблицы на этой странице. Сейчас правил в нём нет, и это
разобранный результат, а не незаполненный файл — разбор по каждому пункту таблиц лежит комментарием в
самом файле. Общая причина: белый список исключает **поле**, а незакрытый шов (`MentalImage`,
суррогатный `HasPossession`, RNG) отравляет все поля разом — такие расхождения снимаются требованием к
сценарию и предупреждением диффа о выходе за безопасную область, а не колонкой в списке.

## Постоянные

Останутся после конца порта.

| Расхождение | Почему | Где |
|---|---|---|
| Коллизии мяч↔тело по **AABB сегментов костей**, а не по геометрии узлов тела | у нас нет их Geometry-узлов: `GetDerivedPosition` заменён серединой сегмента, `AABB::GetRadius` — полудиагональю, паддинг 0.12 | `Gpf.BallBodyCollider` ← `match.cpp:1926-2045` |
| `touch_bodypart` по **ближайшей кости**, а не по Geometry-сегментам | та же причина; сегмент имеет протяжённость, кость — точка. Разница не замерена, мерить надо вместе с предыдущим пунктом | `AnimCollection.AddExtraTouches` ← `animcollection.cpp:883-911` |
| **Шейдеры** воспроизводятся на Godot-шейдерах, а не переносятся | 7 пар `.vert`/`.frag` написаны под их пайплайн, униформы и граф сцены; обвязку мы не портируем. Единственное вынужденное отступление в графике | `data/media/shaders/` |
| **UI переносится по функциям**, без повторения внешнего вида | их `menu` — 4822 строки на несуществующей у нас GUI-системе | `menu/`, фаза 10 |
| «RED ALERT» (пустая коллекция отбора) **перезапускает клип с кадра 0** вместо `exit(1)` | падение процесса — неприемлемая реакция в редакторе | `Gpf.HumanoidBase` ← `humanoid.cpp:338-340` |
| `Uniform` достигает `max` с вероятностью ~2⁻²⁵ | округление `ulong→float`; у оригинала честный `[0,1)` | `Gpf.GpfRng` |

## Временные

Снимаются известной вехой. Пункт, доживший до конца порта, переезжает наверх — но только явным решением.

| Расхождение | Почему сейчас | Снимается |
|---|---|---|
| **Потоки случайных чисел у сторон разные**, хотя генератор уже один: `GpfRng` (xorshift64\*) и там, и тут | одинаковый генератор с одинаковым сидом даёт одинаковую *последовательность*, но у эталона из неё тянут 22 игрока, судья и `TeamAI`, а у порта — один гуманоид. На одного потребителя приходят **разные числа**. Фаза 5 дала не совпадение потоков, а воспроизводимость эталона от прогона к прогону (раньше сид шёл от `std::time`) — этого хватает, чтобы отличать расхождение порта от шума эталона, и только на это фаза 5 опиралась | **фаза 9** — полное совпадение требует пер-игроковых потоков на **обеих** сторонах, а это уже отступление от оригинала; решается вместе с 11-на-11, когда потребители сойдутся |
| **`MentalImage` не портирован** — задержка восприятия нулевая, всюду объективный `Ball.Predict` | главное системное приближение порта: его видят ранние выходы `SelectAnim`, crude query, `NeedTouch`, `CalculateMovementSmuggle`, перебор касаний, `touchPos`, маска частоты ReQueue. Ветка «в памяти мяч не там, где он есть» мертва — обе стороны сравнения один и тот же `Predict`. 14 мест помечены в коде «ШОВ MentalImage» | **фаза 8** |
| **Статы и рост из заглушек**: все статы 0.6, `zMultiplier` отсутствует (все игроки 1.92 м) | нет `PlayerData` — БД оригинала не читается | **фаза 8** (чтение `database.sqlite`) |
| **`AI_GetPass`/`AI_GetShotDirection` не портированы** — направление и сила идут из `TouchInfo` как есть | вне скоупа фаз 1–4. У удара безвредно (рефайненный `ballDirection` дальше не читается), у паса — реальное упрощение | **фаза 9** |
| **Швы матч-слоя суррогатны**: `HasPossession` = «мяч ближе 1.6 м и ниже 1 м», `AllowLastDitch` жёстко `false`, владение 1.0, `teamId = 0`, `GetSide = -1`, `focusDistance = 0`, `closestOpponentDistance = 1000`, `OppLastTouchBias = 0`; у камеры фазы 7 — владеющий = единственный игрок лабы, `FadingTeamPossessionAmount = 1.0` у обеих команд (сдвиг «в сторону атаки» обнуляется арифметикой самой формулы, `match.cpp:739`), забивший для scorer-cam = единственный игрок | в лабе нет матча, команд и соперников. Каждый суррогат меняет поведение, а не только числа | **фаза 8** |
| **Сетка ворот в GDScript-матче** — наш `NetSim` вместо их пружинной модели | их сетка портируется фазой 7 (физика `ball.cpp:331-408` в `Gpf.Ball` за флагом `NettingEnabled`, детект гола — собственность лаб-оркестратора) и живёт в `stadium_lab`; замороженный GDScript-матч остаётся на `NetSim`, пока его не заместит матч-слой | **фаза 8**; `NetSim` уезжает в `legacy/` вместе с остальным замещённым |
| **Поле в размерах FIFA** (105×68) в GDScript-матче вместо их 110×72 | ядро и лабы порта живут в их размерах (`GpfPitch`), поле оригинала приходит в `stadium_lab` фазой 7; презентация замороженного матча остаётся FIFA до замещения | **фаза 8** |

## Не расхождения, хотя выглядят как они

Частая ошибка следующего захода — «починить» одно из этого. Всё перечисленное **верно оригиналу** и
сверено с исходником; подробности и цифры — [[порт-gameplayfootball]] и [[открытые-вопросы]].

- **Карта геометрических офсетов всегда пуста, а `CalculateGeomOffsets` ниоткуда не вызывается** —
  так и в оригинале. Единственный вызов стоит в `HumanoidBase::Process` (`humanoidbase.cpp:719`), но
  игроки исполняют полный override `Humanoid::Process` (`humanoid.cpp:92`), который базу не зовёт;
  голый `HumanoidBase` достаётся только судьям (`playerofficial.cpp:30`), и там тело функции целиком
  закомментировано. **Не «чинить» вызовом из тика**: офсеты крутят кости, а `BallBodyCollider` считает
  столкновение мяча с телом по отрезкам костей — это правка физики касаний, которую фазы 8–9 обязаны
  сверять с оригиналом. Тело функции ждёт фазы 9 (`.scratch/oracle/issues/15`), разбор —
  `.scratch/oracle/issues/14`. Заодно не расхождение и то, что карта живёт **полем**
  `AnimationApplier.Offsets`, а не параметром `Apply`: тип через мост GDScript не проходит, поведение
  идентично, шов тот же, что у `_movementHistory`.
- **Дискард смаггла разворачивает доводку ОТ мяча** (`humanoid.cpp:2267`) — баг самого оригинала,
  побайтово совпадает и в upstream. Правило bug-for-bug оставляет как есть.
- **Скобочный баг `NeedTouch`** (`:1828`): сравнение внутри вызова `FloatToEnumVelocity`, первое условие
  всегда false.
- **Сложение enum-стороны с радианами** в CULL WRONG ROTATIONAL SIDE (`animcollection.cpp:619`).
- **Два разных `GetMaxVelocity`** в одном `CalculatePhysicsVector` — второй вызывается заново, без
  touch-множителя.
- **Перепутанные оси** в блоке качения мяча; **не взаимно-обратная** пара `GetAngles`/`SetAngles`.
- **`LongPass` отсутствует** в списке `smoothFactor = 0.8`.
- Мёртвые ветки, портированные дословно: `Mod*`-флаги, action-sustain-смаггл, мягкое квантование
  последних кадров, lenient-проход отбора, bump-прерывание.

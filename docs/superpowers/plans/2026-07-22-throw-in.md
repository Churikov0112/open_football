# Вбрасывание из аута (Фаза A) — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Реализовать розыгрыш аута: по T ближайший team_1 встаёт за линией с мячом в руках, человек камерой/стиком целится в секторе ±90° «в поле», удержанием A задаёт силу, мяч летит дугой, после выпуска управление уходит к получателю.

**Architecture:** Зеркало удара от ворот — контроллер-автомат `ThrowInController` (`IDLE→SETUP→AIM→STRIKE`) + чистая математика `ThrowInLogic`, гейтинг в `match_manager`. Переиспользуем `FreeKickLogic.rotate_heading`/`push_out_of_radius`, `PassSystem.launch_lob`, `KeeperLogic.drag_horizontal_speed`, `assign_controlled_player`/`begin_pass_receive`, `ball.catch`/`PlayerVisual.get_hold_attachment`. Одна новая способность визуала — `PlayerVisual.hold_pose(clip)` (стойка первого кадра).

**Tech Stack:** Godot 4.7, GDScript. Ассет-сборка — Blender 5.1 (`tools/merge_mixamo.py`).

## Global Constraints

- Godot 4.7 / GDScript. Проза и комментарии — на русском (как в остальном проекте).
- `*Logic`-классы **никогда не читают `FootballConstants`** — тюнинг передаётся параметрами.
- Godot exe: `C:\Users\User\AppData\Local\Godot\Godot_v4.7-stable_win64_console.exe`.
- Запуск headless-теста: `& "<godot-exe>" --path "C:\Users\User\Desktop\projects\OpenFootball" --headless -s "res://tests/<name>.gd"`. Тест печатает `CHECK PASS`/`CHECK FAIL` и выходит 0/1.
- Обращения к `_manager` (типизирован `Node`) через `:=` не компилируются (нельзя вывести тип) — использовать явный тип или не-`:=` (см. CLAUDE.md, «Conventions that will trip you up»).
- Коммит-сообщения заканчивать строкой `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

---

### Task 1: `ThrowInLogic` (чистая математика) + константы

**Files:**
- Create: `scripts/match/throw_in_logic.gd`
- Modify: `scripts/data/football_constants.gd` (добавить секцию `THROW-IN` в конец файла)
- Test: `tests/check_throw_in_logic.gd`

**Interfaces:**
- Produces:
  - `ThrowInLogic.aut_point(ball_pos: Vector3, half_width: float, ball_radius: float) -> Vector3`
  - `ThrowInLogic.base_heading(aut_x: float) -> Vector3`
  - `ThrowInLogic.thrower_placement(aut: Vector3, into_field: Vector3, behind_offset: float, body_y: float) -> Vector3`
  - Константы `FootballConstants.THROW_*` (см. Step 3).

- [ ] **Step 1: Написать падающий тест**

Создать `tests/check_throw_in_logic.gd`:

```gdscript
extends SceneTree
## Headless-проверка чистых функций ThrowInLogic + правила 2 м (push_out_of_radius).

func _init() -> void:
	var ok := true
	ok = _check_aut_positive() and ok
	ok = _check_aut_negative() and ok
	ok = _check_base_heading() and ok
	ok = _check_placement() and ok
	ok = _check_encroach() and ok
	if ok:
		print("CHECK PASS: throw_in_logic")
		quit(0)
	else:
		print("CHECK FAIL: throw_in_logic")
		quit(1)

func _check_aut_positive() -> bool:
	# Мяч у правой боковой (x>0) → точка на линии x=+34, z сохраняется, y=радиус.
	var p := ThrowInLogic.aut_point(Vector3(30, 1.0, 10), 34.0, 0.11)
	if not (is_equal_approx(p.x, 34.0) and is_equal_approx(p.z, 10.0) and is_equal_approx(p.y, 0.11)):
		print("  FAIL aut_positive: ", p)
		return false
	return true

func _check_aut_negative() -> bool:
	var p := ThrowInLogic.aut_point(Vector3(-20, 1.0, -5), 34.0, 0.11)
	if not (is_equal_approx(p.x, -34.0) and is_equal_approx(p.z, -5.0)):
		print("  FAIL aut_negative: ", p)
		return false
	return true

func _check_base_heading() -> bool:
	# Правая линия (+34) → в поле это -X; левая (-34) → +X.
	var r := ThrowInLogic.base_heading(34.0)
	var l := ThrowInLogic.base_heading(-34.0)
	if not (r.is_equal_approx(Vector3(-1, 0, 0)) and l.is_equal_approx(Vector3(1, 0, 0))):
		print("  FAIL base_heading: r=", r, " l=", l)
		return false
	return true

func _check_placement() -> bool:
	# За линией снаружи поля: aut - в_поле*offset. y = body_y.
	var p := ThrowInLogic.thrower_placement(Vector3(34, 0.11, 10), Vector3(-1, 0, 0), 0.5, 0.5)
	if not (is_equal_approx(p.x, 34.5) and is_equal_approx(p.z, 10.0) and is_equal_approx(p.y, 0.5)):
		print("  FAIL placement: ", p)
		return false
	return true

func _check_encroach() -> bool:
	# Соперник в 0.5 м от точки вброса (<2) → вытолкнут ровно на 2 м вдоль (pos-spot); y сохраняется.
	var out := FreeKickLogic.push_out_of_radius(Vector3(34.5, 0.5, 10), Vector3(34, 0.11, 10), 2.0)
	if not (is_equal_approx(out.x, 36.0) and is_equal_approx(out.z, 10.0) and is_equal_approx(out.y, 0.5)):
		print("  FAIL encroach: ", out)
		return false
	return true
```

- [ ] **Step 2: Запустить тест — убедиться, что падает**

Run: `& "C:\Users\User\AppData\Local\Godot\Godot_v4.7-stable_win64_console.exe" --path "C:\Users\User\Desktop\projects\OpenFootball" --headless -s "res://tests/check_throw_in_logic.gd"`
Expected: FAIL/ошибка парсинга — `ThrowInLogic` ещё не существует.

- [ ] **Step 3: Добавить константы**

В конец `scripts/data/football_constants.gd` дописать:

```gdscript

# --- THROW-IN (вбрасывание из аута, Фаза A) ---
const THROW_BEHIND_OFFSET := 0.5      # насколько вбрасывающий стоит за боковой линией (м, наружу поля)
const THROW_AIM_SPEED := 2.0          # скорость поворота направления/тела/камеры стиком (рад/с)
const THROW_AIM_ARC := PI / 2.0       # макс ±отклонение направления от перпендикуляра в поле (90°)
const THROW_CHARGE_MAX_TIME := 0.7    # время набора полной силы удержанием A (с)
const THROW_MIN_DIST := 6.0           # дальность приземления при пустом заряде (м)
const THROW_MAX_DIST := 22.0          # дальность приземления при полном заряде (м)
const THROW_PEAK_MIN := 2.0           # высота дуги при пустом заряде (м)
const THROW_PEAK_MAX := 4.5           # высота дуги при полном заряде (м)
const THROW_ENCROACH_DIST := 2.0      # соперники не ближе этого к точке вброса (правило 15.2, м)
const THROW_CAM_BACK := 4.5           # камера позади вбрасывающего (м)
const THROW_CAM_HEIGHT := 2.2         # высота камеры (м)
const THROW_CAM_LOOK_Y := 1.2         # высота точки, куда смотрит камера (м)
const THROW_CAM_AHEAD := 6.0          # как далеко перед точкой вброса смотрит камера (м)
```

- [ ] **Step 4: Написать `ThrowInLogic`**

Создать `scripts/match/throw_in_logic.gd`:

```gdscript
class_name ThrowInLogic
extends Object
## Чистая математика вброса из аута. НИКОГДА не читает FootballConstants — тюнинг параметрами.

## Точка аута: проекция мяча на ближайшую боковую линию (X = ±half_width по знаку ball.x,
## Z мяча, Y = радиус мяча).
static func aut_point(ball_pos: Vector3, half_width: float, ball_radius: float) -> Vector3:
	var sx := 1.0 if ball_pos.x >= 0.0 else -1.0
	return Vector3(sx * half_width, ball_radius, ball_pos.z)

## Базовое направление вброса — перпендикуляр к боковой линии, внутрь поля.
## Правая линия (aut_x>0) → в поле это -X; левая → +X.
static func base_heading(aut_x: float) -> Vector3:
	return Vector3(-1.0, 0.0, 0.0) if aut_x >= 0.0 else Vector3(1.0, 0.0, 0.0)

## Расстановка вбрасывающего за боковой линией снаружи поля: aut минус направление-в-поле на
## behind_offset (т.е. на behind_offset НАРУЖУ от линии). y = body_y.
static func thrower_placement(aut: Vector3, into_field: Vector3, behind_offset: float, body_y: float) -> Vector3:
	var p := aut - into_field * behind_offset
	p.y = body_y
	return p
```

- [ ] **Step 5: Запустить тест — убедиться, что проходит**

Run: `& "C:\Users\User\AppData\Local\Godot\Godot_v4.7-stable_win64_console.exe" --path "C:\Users\User\Desktop\projects\OpenFootball" --headless -s "res://tests/check_throw_in_logic.gd"`
Expected: `CHECK PASS: throw_in_logic`, код выхода 0.

- [ ] **Step 6: Коммит**

```bash
git add scripts/match/throw_in_logic.gd scripts/data/football_constants.gd tests/check_throw_in_logic.gd
git commit -m "feat(throw-in): ThrowInLogic pure math + THROW-IN constants

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: `PlayerVisual.hold_pose()` — стойка первого кадра

**Files:**
- Modify: `scripts/player/player_visual.gd` (добавить `hold_pose`, одну строку в `trigger`)
- Test: `tests/check_player_visual_pose.gd`

**Interfaces:**
- Consumes: `_playback`, `_states`, `_resolve_action`, `_set_action_speed`, `_fall_lock`, `_active_action` (существующие приватные члены `PlayerVisual`).
- Produces: `PlayerVisual.hold_pose(clip: String) -> bool` — встаёт в кадр 0 клипа и замораживает (`TimeScale=0`); снимается любым `trigger(...)`.

- [ ] **Step 1: Написать падающий тест**

Создать `tests/check_player_visual_pose.gd` (паттерн — как `check_player_visual_actions.gd`: ждём `is_node_ready()` в `_process`):

```gdscript
extends SceneTree
## hold_pose('throw_in') замораживает визуал на кадре 0 (TimeScale=0); несуществующий клип → false;
## последующий trigger() восстанавливает скорость проигрывания.

var _inst: Node

func _initialize() -> void:
	var scene := load("res://scenes/player_visual.tscn") as PackedScene
	if scene == null:
		print("CHECK FAIL: не загрузилась scenes/player_visual.tscn")
		quit(1)
		return
	_inst = scene.instantiate()
	root.add_child(_inst)

func _process(_delta: float) -> bool:
	if _inst == null or not _inst.is_node_ready():
		return false
	var ok := true
	# hold_pose существующего клипа → true и TimeScale=0.
	if not _inst.hold_pose("throw_in"):
		print("CHECK FAIL: hold_pose('throw_in') вернул false")
		ok = false
	else:
		var scale: float = _inst._anim_tree.get(&"parameters/TimeScale/scale")
		if not is_equal_approx(scale, 0.0):
			print("CHECK FAIL: после hold_pose TimeScale=%f, ожидалось 0" % scale)
			ok = false
	# hold_pose несуществующего клипа → false.
	if _inst.hold_pose("no_such_clip"):
		print("CHECK FAIL: hold_pose несуществующего клипа вернул true")
		ok = false
	# trigger() снимает заморозку — TimeScale снова > 0.
	if not _inst.trigger("throw_in"):
		print("CHECK FAIL: trigger('throw_in') вернул false")
		ok = false
	else:
		var scale2: float = _inst._anim_tree.get(&"parameters/TimeScale/scale")
		if scale2 <= 0.0:
			print("CHECK FAIL: после trigger TimeScale=%f, ожидалось >0" % scale2)
			ok = false
	print("CHECK PASS" if ok else "CHECK FAIL")
	quit(0 if ok else 1)
	return true
```

- [ ] **Step 2: Запустить тест — убедиться, что падает**

Run: `& "C:\Users\User\AppData\Local\Godot\Godot_v4.7-stable_win64_console.exe" --path "C:\Users\User\Desktop\projects\OpenFootball" --headless -s "res://tests/check_player_visual_pose.gd"`
Expected: FAIL — метода `hold_pose` ещё нет (ошибка «invalid call»).

- [ ] **Step 3: Добавить `hold_pose` и снятие заморозки в `trigger`**

В `scripts/player/player_visual.gd`, в теле `trigger()`, перед строкой `_playback.travel(StringName(state))` (сейчас ~строка 368) добавить одну строку — любое действие снимает pose-заморозку и вернёт локомоцию после клипа:

```gdscript
	_fall_lock = false   # trigger снимает hold_pose-заморозку (см. hold_pose)
	_playback.travel(StringName(state))
```

Сразу после функции `trigger()` (перед `cancel_action()`) добавить:

```gdscript
## Встать в позу первого кадра клипа-действия и замереть (стойка вброса с мячом в руках).
## Мгновенный переход на кадр 0 (_playback.start, без кроссфейда) + заморозка времени
## (TimeScale=0) + _fall_lock (чтобы селектор локомоции не сдёрнул обратно в idle). Снимается
## вызовом trigger() (сбрасывает _fall_lock и восстанавливает скорость → клип идёт с кадра 0).
func hold_pose(clip: String) -> bool:
	if _playback == null:
		return false
	var state := _resolve_action(clip)
	if state == "" or not _states.has(state):
		return false
	_fall_lock = true
	_active_action = ""
	_set_action_speed(0.0)
	_playback.start(StringName(state))
	return true
```

- [ ] **Step 4: Запустить тест — убедиться, что проходит**

Run: `& "C:\Users\User\AppData\Local\Godot\Godot_v4.7-stable_win64_console.exe" --path "C:\Users\User\Desktop\projects\OpenFootball" --headless -s "res://tests/check_player_visual_pose.gd"`
Expected: `CHECK PASS`, код выхода 0.

- [ ] **Step 5: Регресс — старые визуал-тесты не сломаны**

Run: `& "C:\Users\User\AppData\Local\Godot\Godot_v4.7-stable_win64_console.exe" --path "C:\Users\User\Desktop\projects\OpenFootball" --headless -s "res://tests/check_player_visual_actions.gd"`
Expected: `CHECK PASS` (изменение в `trigger` не сломало обычные действия).

- [ ] **Step 6: Коммит**

```bash
git add scripts/player/player_visual.gd tests/check_player_visual_pose.gd
git commit -m "feat(player-visual): hold_pose(clip) freezes on frame 0 for throw-in stance

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: Ассет — заморозить root-motion `throw_in` + тайминг контакта

**Files:**
- Modify: `tools/merge_mixamo.py` (`IN_PLACE_CLIPS`)
- Modify: `scripts/player/player_visual.gd` (`ACTION_TIMING`)
- Create (временный): `tools/measure_throw_in.gd`
- Rebuild: `assets/models/footballer.glb` (+ его `.import`)

**Interfaces:**
- Consumes: клип `throw_in` уже присутствует в glb (проверяется `check_footballer_glb.gd`).
- Produces: `ACTION_TIMING["throw_in"]` с `contact = длина·13/33`, `lock = длина`, `speed = 1.0`; замороженный горизонтальный root-motion клипа.

- [ ] **Step 1: Добавить `throw_in` в `IN_PLACE_CLIPS`**

В `tools/merge_mixamo.py`, в словарь `IN_PLACE_CLIPS` (после блока вратарских клипов) добавить:

```python
    # Вброс из аута: игрок стоит на месте — морозим горизонталь (0,2), вертикаль (1) живая.
    # Оси Blender-local (см. коммент про tackle выше) — если модель «плывёт»/«висит», перебрать.
    "throw_in": (0, 2),
```

- [ ] **Step 2: Пересобрать glb в Blender и переимпортировать в Godot**

Run (Blender-сборка):
```
& "C:\Program Files\Blender Foundation\Blender 5.1\blender.exe" --background --python tools/merge_mixamo.py -- "C:/Users/User/Desktop/projects/OpenFootball/assets/models/mixamo_src" "C:/Users/User/Desktop/projects/OpenFootball/assets/models/footballer.glb"
```
Затем (переимпорт ассета Godot):
```
& "C:\Users\User\AppData\Local\Godot\Godot_v4.7-stable_win64_console.exe" --path "C:\Users\User\Desktop\projects\OpenFootball" --headless --import
```
Expected: сборка без ошибок; `footballer.glb` и `.import` обновились.

- [ ] **Step 3: Проверить, что клип на месте после пересборки**

Run: `& "C:\Users\User\AppData\Local\Godot\Godot_v4.7-stable_win64_console.exe" --path "C:\Users\User\Desktop\projects\OpenFootball" --headless -s "res://tests/check_footballer_glb.gd"`
Expected: `CHECK PASS` (клип `throw_in` присутствует в списке).

- [ ] **Step 4: Замерить длину клипа `throw_in`**

Создать временный `tools/measure_throw_in.gd`:

```gdscript
extends SceneTree
## Печатает длину клипа throw_in и момент контакта 13/33 для ACTION_TIMING.
func _init() -> void:
	var glb := load("res://assets/models/footballer.glb") as PackedScene
	var inst := glb.instantiate()
	var ap: AnimationPlayer = inst.find_child("AnimationPlayer", true, false)
	if ap == null or not ap.has_animation(&"throw_in"):
		print("MEASURE FAIL: нет AnimationPlayer/клипа throw_in")
		quit(1)
		return
	var len: float = ap.get_animation(&"throw_in").length
	print("throw_in length=", len, " contact(13/33)=", len * 13.0 / 33.0)
	quit(0)
```

Run: `& "C:\Users\User\AppData\Local\Godot\Godot_v4.7-stable_win64_console.exe" --path "C:\Users\User\Desktop\projects\OpenFootball" --headless -s "res://tools/measure_throw_in.gd"`
Expected: строка вида `throw_in length=<L> contact(13/33)=<C>`. **Записать значения `<L>` и `<C>`.**

- [ ] **Step 5: Прописать тайминг в `ACTION_TIMING`**

В `scripts/player/player_visual.gd`, в словарь `ACTION_TIMING` добавить запись (подставить измеренные `<C>` и `<L>` из Step 4 — например, при `length=1.40` это `contact = 0.55`, `lock = 1.40`):

```gdscript
	# Вброс из аута: мяч выпускается на 13/33 клипа (contact), lock = полная длина. Значения —
	# из tools/measure_throw_in.gd; пересобрали клип → перемерить (как PEN_ROOT_SCALE).
	"throw_in": {"contact": <C>, "lock": <L>, "speed": 1.0},
```

- [ ] **Step 6: Удалить временный измеритель и закоммитить**

```bash
git rm tools/measure_throw_in.gd
git add tools/merge_mixamo.py scripts/player/player_visual.gd assets/models/footballer.glb assets/models/footballer.glb.import
git commit -m "chore(throw-in): freeze throw_in root motion + set contact timing (13/33)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

Примечание: `tools/measure_throw_in.gd` создаётся и удаляется в рамках этой задачи — при повторной калибровке (переэкспорт клипа) восстановить из истории git или пересоздать.

---

### Task 4: `ThrowInController` (автомат розыгрыша)

**Files:**
- Create: `scripts/match/throw_in_controller.gd`
- Test: `tests/check_throw_in_flow.gd`

**Interfaces:**
- Consumes: `ThrowInLogic.*` (Task 1), `PlayerVisual.hold_pose` (Task 2), `FreeKickLogic.rotate_heading`/`push_out_of_radius`, `PassSystem.launch_lob`, `KeeperLogic.drag_horizontal_speed`, `PlayerMotor.find_on`/`.set_face_direction`/`.set_control_locked`/`.set_move_intent`, `ball.catch`/`.launch`/`.note_kicker`/`.clear_last_kicker`/`.drag_factor`, и методы менеджера из Task 5: `set_throw_in_active`, `set_throw_in_cam_pose`, `assign_controlled_player`, `begin_pass_receive`, `set_field_ai_active`, `is_celebrating`.
- Produces: узел `ThrowInController` с `setup(manager, ball, camera_pivot, power_bar)`, `start()`, `update(delta)`, `is_active()`, `_fire_charge(ratio)` (для смоук-теста), сигнал `struck`.

- [ ] **Step 1: Написать падающий смоук-тест**

Создать `tests/check_throw_in_flow.gd` (калька `check_goal_kick_flow.gd`):

```gdscript
extends SceneTree
## Headless smoke-тест флоу вброса: грузим match.tscn, стартуем розыгрыш и бросаем (в обход
## Input), крутим кадры и проверяем — мяч получил импульс, режим снялся, без крашей.

var _mm: Node
var _ti: Node
var _elapsed: float = 0.0
var _started: bool = false
var _fired: bool = false
var _max_ball_speed: float = 0.0

func _initialize() -> void:
	var scene: PackedScene = load("res://scenes/match.tscn")
	_mm = scene.instantiate()
	root.add_child(_mm)

func _process(delta: float) -> bool:
	_elapsed += delta
	if _elapsed > 0.15 and not _started:
		_ti = _mm.get_node_or_null("ThrowInController")
		if _ti == null:
			print("CHECK FAIL: нет узла ThrowInController")
			return true
		_ti.start()
		_started = true
		if not _mm.is_throw_in_active():
			print("CHECK FAIL: throw-in-режим не включился после start")
			return true
		return false
	# Имитируем бросок на 70% заряда (минуем Input-заряд).
	if _elapsed > 0.3 and _started and not _fired:
		_ti._fire_charge(0.7)
		_fired = true
		return false
	if _fired:
		_max_ball_speed = maxf(_max_ball_speed, _mm.ball.linear_velocity.length())
	# Ждём контакта (13/33 клипа) + запас.
	if _elapsed > 4.0:
		var released: bool = not _mm.is_throw_in_active()
		var launched: bool = _max_ball_speed > 1.0
		print("SMOKE: max_ball_speed=", _max_ball_speed, " throw_in_active=", _mm.is_throw_in_active())
		if released and launched:
			print("CHECK PASS: throw_in flow (launched + released)")
			quit(0)
		else:
			print("CHECK FAIL: released=", released, " launched=", launched)
			quit(1)
		return true
	return false
```

- [ ] **Step 2: Запустить тест — убедиться, что падает**

Run: `& "C:\Users\User\AppData\Local\Godot\Godot_v4.7-stable_win64_console.exe" --path "C:\Users\User\Desktop\projects\OpenFootball" --headless -s "res://tests/check_throw_in_flow.gd"`
Expected: FAIL — `нет узла ThrowInController` (контроллер ещё не подключён в менеджере; сам файл создаём в Step 3, проводку — в Task 5).

- [ ] **Step 3: Написать `ThrowInController`**

Создать `scripts/match/throw_in_controller.gd`:

```gdscript
extends Node
## Контроллер вброса из аута (Фаза A). Автомат IDLE→SETUP→AIM→STRIKE. Бьющий — ближайший к точке
## аута полевой игрок team_1. Мяч в руках; стик крутит направление+тело+камеру (сектор ±90° в
## поле); A задаёт силу; на 13/33 клипа мяч летит дугой. После выпуска управление — тому, кому
## летит мяч. Математика — ThrowInLogic + переиспользуемые FreeKickLogic/PassSystem/KeeperLogic.

signal struck

enum Phase { IDLE, SETUP, AIM, STRIKE }

var _manager: Node
var _ball: RigidBody3D
var _camera_pivot: Node3D
var _power_bar: ProgressBar

var _phase: int = Phase.IDLE
var _thrower: CharacterBody3D
var _opp_group: StringName = &"team_2"
var _spot: Vector3 = Vector3.ZERO           # точка вброса (на боковой линии)
var _into: Vector3 = Vector3.RIGHT          # перпендикуляр в поле (базовое направление)
var _heading: Vector3 = Vector3.RIGHT       # направление вылета (= тело = камера), кламп ±90°

var _charging: bool = false
var _charge: float = 0.0
var _pending_ratio: float = 1.0
var _locked: bool = false                   # A нажата (направление зафиксировано)
var _contact_connected := false

func setup(manager: Node, ball: RigidBody3D, camera_pivot: Node3D, power_bar: ProgressBar) -> void:
	_manager = manager
	_ball = ball
	_camera_pivot = camera_pivot
	_power_bar = power_bar

func is_active() -> bool:
	return _phase != Phase.IDLE

## Старт вброса: точка = проекция мяча на ближайшую боковую линию; бьющий = ближайший team_1.
func start() -> void:
	if _phase != Phase.IDLE:
		return
	_spot = ThrowInLogic.aut_point(_ball.global_position,
		FootballConstants.HALF_FIELD_WIDTH, FootballConstants.BALL_RADIUS)
	_into = ThrowInLogic.base_heading(_spot.x)
	_heading = _into
	_thrower = _nearest_team1(_spot, null)
	if _thrower == null:
		return   # некому вбрасывать — отменяем старт
	_opp_group = &"team_2" if _thrower.is_in_group("team_1") else &"team_1"
	_setup()

func _setup() -> void:
	_phase = Phase.SETUP
	_manager.set_throw_in_active(true)
	# Расстановка вбрасывающего за линией, лицом в поле, мотор залочен.
	_thrower.global_position = ThrowInLogic.thrower_placement(_spot, _into,
		FootballConstants.THROW_BEHIND_OFFSET, 0.5)
	_thrower.look_at(_thrower.global_position + _into, Vector3.UP)
	var tm := PlayerMotor.find_on(_thrower)
	if tm != null:
		tm.set_control_locked(true)
		tm.set_move_intent(Vector3.ZERO)
		tm.set_face_direction(_into)
	# Мяч в руку вбрасывающего (CAUGHT — сам приклеен к кости кисти каждый кадр; гол забить нельзя).
	var vis := _thrower_visual()
	if vis != null and _ball.has_method(&"catch"):
		var hold: Node3D = vis.get_hold_attachment()
		if hold != null:
			if _ball.has_method(&"clear_last_kicker"):
				_ball.clear_last_kicker()
			_ball.catch(_thrower, hold)
	# Стойка замаха (первый кадр throw_in, заморожен).
	if vis != null:
		vis.hold_pose("throw_in")
	# Правило 2 м: соперники вбрасывающего не ближе THROW_ENCROACH_DIST к точке вброса.
	_clear_opponents_from_spot()
	# Управление — на вбрасывающего (человек драйвит прицел); полевой AI заморожен.
	_manager.assign_controlled_player(_thrower)
	_manager.set_field_ai_active(false)
	_charging = false
	_charge = 0.0
	_locked = false
	_update_camera_pose()
	_phase = Phase.AIM

## Ближайший к точке `to` полевой игрок team_1 (вратарь исключён по группе role_gk), кроме exclude.
func _nearest_team1(to: Vector3, exclude: Node) -> CharacterBody3D:
	var best: CharacterBody3D = null
	var best_d := INF
	for n in _manager.get_tree().get_nodes_in_group(&"team_1"):
		if not is_instance_valid(n) or n == exclude or n.is_in_group(&"role_gk") or not (n is CharacterBody3D):
			continue
		var d := Vector3(n.global_position.x - to.x, 0.0, n.global_position.z - to.z).length_squared()
		if d < best_d:
			best_d = d
			best = n
	return best

## Соперники вбрасывающего ближе THROW_ENCROACH_DIST к точке вброса — вытолкнуть + залочить мотор.
func _clear_opponents_from_spot() -> void:
	for n in _manager.get_tree().get_nodes_in_group(_opp_group):
		if not is_instance_valid(n) or not (n is Node3D):
			continue
		var adjusted := FreeKickLogic.push_out_of_radius(n.global_position, _spot,
			FootballConstants.THROW_ENCROACH_DIST)
		if not adjusted.is_equal_approx(n.global_position):
			n.global_position = adjusted
		var m := PlayerMotor.find_on(n)
		if m != null:
			m.set_control_locked(true)
			m.set_move_intent(Vector3.ZERO)

func update(delta: float) -> void:
	match _phase:
		Phase.AIM:
			_aim_update(delta)
		Phase.STRIKE:
			pass
	_update_camera_pose()

func _aim_update(delta: float) -> void:
	if not _locked:
		var stick_x := Input.get_axis(&"move_left", &"move_right")
		if absf(stick_x) > 0.15:
			_heading = FreeKickLogic.rotate_heading(_heading, _into, stick_x,
				FootballConstants.THROW_AIM_SPEED, delta, FootballConstants.THROW_AIM_ARC)
		# Тело доворачивается вместе с направлением.
		var tm := PlayerMotor.find_on(_thrower)
		if tm != null:
			tm.set_face_direction(_heading)
		if Input.is_action_just_pressed(&"pass_short"):
			_start_charge()
	if _charging:
		_charge += delta
		var ratio := clampf(_charge / FootballConstants.THROW_CHARGE_MAX_TIME, 0.0, 1.0)
		_power_bar.visible = true
		_power_bar.value = ratio
		var fill := _power_bar.get_theme_stylebox("fill")
		if fill:
			fill.bg_color = Color.GREEN_YELLOW.lerp(Color.RED, ratio * ratio)
		if ratio >= 1.0 or not Input.is_action_pressed(&"pass_short"):
			_fire_charge(ratio)

## Коммит: направление фиксируется, начинается набор силы.
func _start_charge() -> void:
	_charging = true
	_locked = true
	_charge = 0.0

func _fire_charge(ratio: float) -> void:
	_charging = false
	_power_bar.visible = false
	_pending_ratio = ratio
	_begin_strike()

## Запуск клипа броска: снимаем стойку (trigger), ждём action_contact.
func _begin_strike() -> void:
	_phase = Phase.STRIKE
	var vis := _thrower_visual()
	if vis != null and not _contact_connected:
		vis.action_contact.connect(_on_thrower_contact, CONNECT_ONE_SHOT)
		_contact_connected = true
	if vis == null or not vis.trigger("throw_in"):
		_on_thrower_contact("throw_in")   # фолбэк без анимации — бросаем сразу

func _on_thrower_contact(_action: String) -> void:
	_contact_connected = false
	var g: float = ProjectSettings.get_setting("physics/3d/default_gravity", 9.8)
	var from: Vector3 = _ball.global_position
	var flat := Vector3(_heading.x, 0.0, _heading.z).normalized()
	# Дуга: дальность и высота по заряду; горизонталь с поправкой на драг (иначе недолёт).
	var land_dist := lerpf(FootballConstants.THROW_MIN_DIST, FootballConstants.THROW_MAX_DIST, _pending_ratio)
	var peak := lerpf(FootballConstants.THROW_PEAK_MIN, FootballConstants.THROW_PEAK_MAX, _pending_ratio)
	var land := from + flat * land_dist
	land.y = FootballConstants.BALL_RADIUS
	var lob := PassSystem.launch_lob(from, land, peak, g)
	var vy: float = lob.y
	var flight_t: float = (2.0 * vy / g) if g > 0.01 else 0.0
	var dt := 1.0 / float(Engine.physics_ticks_per_second)
	var hspeed := KeeperLogic.drag_horizontal_speed(land_dist, flight_t, _ball.drag_factor, dt)
	var vel := flat * hspeed + Vector3.UP * vy
	if _ball.has_method(&"launch"):
		_ball.launch(vel, false)
	# Мяч был CAUGHT (dribbler=вбрасывающий) → помечаем бьющего (анти-самоблок/кулдаун).
	if _ball.has_method(&"note_kicker"):
		_ball.note_kicker(_thrower)
	struck.emit()
	# Управление — тому, кому летит мяч (ближайший team_1 к приземлению, кроме вбрасывающего).
	var receiver := _nearest_team1(land, _thrower)
	if receiver != null:
		_manager.assign_controlled_player(receiver)
		if _manager.has_method(&"begin_pass_receive"):
			_manager.begin_pass_receive(receiver)
	_release()

func _release() -> void:
	var tm := PlayerMotor.find_on(_thrower)
	if tm != null:
		tm.set_face_direction(Vector3.ZERO)
		tm.set_control_locked(false)
	# Гол во время вброса (напр. рикошет) — заморозку празднования держит _celebrate_then_reset.
	if _manager.is_celebrating():
		_manager.set_field_ai_active(false)
	else:
		_manager.set_field_ai_active(true)
	_manager.set_throw_in_active(false)
	_phase = Phase.IDLE

## Фикс-камера 3-го лица за вбрасывающим (за точкой вдоль -heading), смотрит в поле.
func _update_camera_pose() -> void:
	if _phase == Phase.IDLE:
		return
	var eye := _spot - _heading * FootballConstants.THROW_CAM_BACK + Vector3(0.0, FootballConstants.THROW_CAM_HEIGHT, 0.0)
	var look := _spot + _heading * FootballConstants.THROW_CAM_AHEAD + Vector3(0.0, FootballConstants.THROW_CAM_LOOK_Y, 0.0)
	var t := Transform3D.IDENTITY
	t.origin = eye
	t = t.looking_at(look, Vector3.UP)
	_manager.set_throw_in_cam_pose(t)

func _thrower_visual() -> PlayerVisual:
	if _thrower == null:
		return null
	for c in _thrower.get_children():
		if c is PlayerVisual:
			return c
	return null
```

- [ ] **Step 4: Запустить смоук-тест — всё ещё падает на проводке**

Run: `& "C:\Users\User\AppData\Local\Godot\Godot_v4.7-stable_win64_console.exe" --path "C:\Users\User\Desktop\projects\OpenFootball" --headless -s "res://tests/check_throw_in_flow.gd"`
Expected: FAIL — `нет узла ThrowInController` (менеджер ещё не создаёт узел; проводка — Task 5). Файл контроллера при этом парсится без ошибок (нет сообщений парсера про `throw_in_controller.gd`).

- [ ] **Step 5: Коммит**

```bash
git add scripts/match/throw_in_controller.gd tests/check_throw_in_flow.gd
git commit -m "feat(throw-in): ThrowInController state machine (SETUP/AIM/STRIKE)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 5: Проводка в `match_manager` (триггер, гейтинг, камера)

**Files:**
- Modify: `scripts/match/match_manager.gd` (поля, `_ready`, `_setup_inputs`, геттеры/сеттеры, `_process`, `_physics_process`)

**Interfaces:**
- Consumes: `ThrowInController` (Task 4).
- Produces: `match_manager.is_throw_in_active()`, `set_throw_in_active(on)`, `set_throw_in_cam_pose(pose)`; узел `ThrowInController` в дереве; триггер по `throw_in_debug` (T).

- [ ] **Step 1: Поля контроллера**

В `scripts/match/match_manager.gd`, сразу после полей goal kick (`var _goal_kick_cam_pose: Transform3D = Transform3D.IDENTITY`, ~строка 36) добавить:

```gdscript
var _throw_in                                  # ThrowInController
var _throw_in_active: bool = false
var _throw_in_cam_pose: Transform3D = Transform3D.IDENTITY
```

- [ ] **Step 2: Инстанс в `_ready`**

После блока создания goal kick (`_goal_kick.setup(self, ball, camera_pivot, power_bar, _keeper)`, ~строка 153) добавить:

```gdscript
	_throw_in = preload("res://scripts/match/throw_in_controller.gd").new()
	_throw_in.name = "ThrowInController"
	add_child(_throw_in)
	_throw_in.setup(self, ball, camera_pivot, power_bar)
```

- [ ] **Step 3: Инпут-действие T**

В `_setup_inputs()`, в словарь `actions` (рядом с прочими `*_debug`, ~строка 243) добавить:

```gdscript
		&"throw_in_debug":  {"keys": [KEY_T],     "buttons": [], "axes": []},
```

(T также привязана к `corner_call`, но контексты взаимоисключающие — оба action сосуществуют.)

- [ ] **Step 4: Геттеры/сеттеры**

После `set_goal_kick_cam_pose` (~строка 653) добавить:

```gdscript
func is_throw_in_active() -> bool:
	return _throw_in_active


func set_throw_in_active(on: bool) -> void:
	_throw_in_active = on


func set_throw_in_cam_pose(pose: Transform3D) -> void:
	_throw_in_cam_pose = pose
```

- [ ] **Step 5: Парковка камеры + скрытие маркера + исключение из charge-гейта в `_process`**

(a) В цепочке парковки камеры, после `elif _goal_kick_active:` … (строки 863–864) добавить ветку:

```gdscript
	elif _goal_kick_active:
		camera_pivot.global_transform = _goal_kick_cam_pose
	elif _throw_in_active:
		camera_pivot.global_transform = _throw_in_cam_pose
```

(b) Скрытие cyan-маркера — расширить условие (сейчас `if _goal_kick_active:`, ~строка 888):

```gdscript
	if _goal_kick_active or _throw_in_active:
		if _controlled_marker != null:
			_controlled_marker.visible = false
```

(c) Исключить вброс из общего charge-гейта менеджера (~строка 901) — дописать конъюнкт:

```gdscript
	if not _penalty_active and not _free_kick_active and not _corner_active and not _goal_kick_active and not _throw_in_active:
```

- [ ] **Step 6: Делегирование + триггер в `_physics_process`**

После блока goal kick (`_goal_kick.start(_keeper, _keeper_brain.goal_line_z); return`, ~строка 959) добавить:

```gdscript
	# Вброс из аута — свой контроллер, обычные системы заглушены.
	if _throw_in_active:
		_throw_in.update(delta)
		return
	# Вброс по T — только из чистого состояния (не во время празднования гола).
	if Input.is_action_just_pressed(&"throw_in_debug") and not _celebrating:
		_throw_in.start()
		return
```

- [ ] **Step 7: Смоук-тест флоу проходит**

Run: `& "C:\Users\User\AppData\Local\Godot\Godot_v4.7-stable_win64_console.exe" --path "C:\Users\User\Desktop\projects\OpenFootball" --headless -s "res://tests/check_throw_in_flow.gd"`
Expected: `CHECK PASS: throw_in flow (launched + released)`, код выхода 0.

- [ ] **Step 8: Валидация обеих headless-загрузок (обе нужны — ловят разное)**

Run (меню-загрузка):
```
& "C:\Users\User\AppData\Local\Godot\Godot_v4.7-stable_win64_console.exe" --path "C:\Users\User\Desktop\projects\OpenFootball" --headless --quit
```
Run (сцена матча):
```
& "C:\Users\User\AppData\Local\Godot\Godot_v4.7-stable_win64_console.exe" --path "C:\Users\User\Desktop\projects\OpenFootball" --headless --quit-after 2 res://scenes/match.tscn
```
Expected: нет НОВЫХ категорий ошибок парсинга/рантайма относительно baseline из CLAUDE.md (сравнивать по тексту/категории, не по числу — известный baseline: `!is_inside_tree()`, дубли регистрации `ACTION_CLIPS` kick/pass, `WorldEnvironment3D`).

- [ ] **Step 9: Коммит**

```bash
git add scripts/match/match_manager.gd
git commit -m "feat(throw-in): wire ThrowInController into match_manager (T trigger, gating, camera)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

- [ ] **Step 10: Ручная приёмка (человек, живой запуск)**

Запустить игру (Godot без `--headless`), встать мячом у боковой, нажать **T**. Проверить по спеку:
- вбрасывающий = ближайший team_1, стоит за линией с мячом в руках в позе замаха (не «плывёт»/«висит» — иначе перебрать оси в `IN_PLACE_CLIPS`, Task 3 Step 1);
- камера 3-го лица за спиной; стик крутит тело+камеру+направление, упирается на ±90°, назад не идёт;
- удержание **A** копит силу (power-bar), отпуск → бросок дугой в выбранную сторону; выпуск на замахе (≈13/33 клипа);
- соперники вытолкнуты минимум на 2 м от точки вброса;
- после броска управление на ближайшего к приземлению team_1, он выбегает на мяч, игра продолжается.

---

## Self-Review (выполнено при написании плана)

- **Покрытие спека:** триггер T (Task 5 Step 3/6), точка аута + вбрасывающий (Task 1, Task 4 `start`), стойка с мячом (Task 2 + Task 4 `_setup`), прицел ±90° (Task 4 `_aim_update` + `THROW_AIM_ARC`), сила=дальность/дуга (Task 4 `_on_thrower_contact`), фикс направления на A (Task 4 `_start_charge`/`_locked`), 2 м расчистка (Task 1 тест + Task 4 `_clear_opponents_from_spot`), передача управления получателю (Task 4 `_on_thrower_contact`), заморозка AI (Task 4 `_setup`/`_release`), камера (Task 4 `_update_camera_pose` + Task 5), ассет root-motion + тайминг 13/33 (Task 3), гейтинг (Task 5). Все разделы спека имеют задачу.
- **Плейсхолдеры:** единственные незаполненные значения — `<C>`/`<L>` в Task 3 Step 5 — это калибровка по измерению (Task 3 Step 4), с формулой и worked-example; не TODO.
- **Согласованность типов/имён:** `set_throw_in_active`/`is_throw_in_active`/`set_throw_in_cam_pose`, `assign_controlled_player`, `begin_pass_receive`, `set_field_ai_active`, `is_celebrating` — имена совпадают между контроллером (Task 4) и менеджером (существующие + Task 5). `hold_pose`/`trigger` — Task 2. `ThrowInLogic.aut_point`/`base_heading`/`thrower_placement` — Task 1 ↔ Task 4.

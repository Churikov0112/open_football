# 10 — Приёмка фазы 7

**What to build:** фаза объявлена закрытой: машинные швы зелёные, глазная приёмка бок о бок с
эталон-exe пройдена, документация отражает новое состояние.

Машинная часть. Дифф трасс `walk_line` — нулевой, прогон обязателен: правки лезли в тик `Gpf.Ball`,
и хотя по построению дифф не меняется (все ветки сетки под флагом «мяч в воротах», сценарий мяча
их не касается), опечатка в переносе нулевого диффа не оставит. Порядок — эталон → порт → дифф,
команды в `.scratch/oracle/run-commands.md`. Плюс стандартный набор: `dotnet build` без
предупреждений, обе headless-команды без новых категорий ошибок, весь существующий набор
`check_gpf_*` зелёный.

Глазной чек-лист (человек, рядом с эталон-exe): стадион и пропорции; газон — полосы (две ширины по
перезапускам), шум, виньетка; разметка из overlay; щиты подменяются и меняются от загрузки к
загрузке; гол — сетка ловит и колышется; снаружи — мяч сквозь сетку; штанга — отскок со звуком;
касания — звук с вариацией питча; камера wide — ведение, сглаживание, дрожание; после гола —
scorer-cam.

Скриншот-диффы против эталон-exe сознательно не делаются: покадровое совпадение картинок разных
рендереров недостижимо и не нужно.

Документация — по правилам `CLAUDE.md`: таблица фаз и раздел фазы 7 в
`docs/wiki/порт-gameplayfootball.md`, новые данные `GpfPitch` в `docs/wiki/константы.md`, хвосты в
`docs/wiki/открытые-вопросы.md` (физика сетки принята глазами до гол-сценария фазы 8; сквозная
сетка снаружи — будущая видимая регрессия ощущения при замещении `NetSim`, не чинить), строка в
`log.md`, маппинг новых скриптов в `.claude/hooks/wiki-hint.py`. В тикет
`.scratch/oracle/issues/17-rng-potrebiteli-vne-geympleya.md` дописывается перепись
RNG-потребителей вне геймплея, появившихся в этой фазе (камера, звук, солнце, покос, щиты,
`fastrandom` газона) — фаза 9 сводит потоки по этому списку.

**Blocked by:** 04, 06, 07, 08, 09

**Status:** ready-for-human

- [x] Дифф трасс `walk_line` нулевой (эталон → порт → дифф по командам `run-commands.md`)
- [x] `dotnet build` без предупреждений; обе headless-команды без новых категорий ошибок; весь
      набор `check_gpf_*` зелёный
- [x] **Глазной чек-лист пройден по всем десяти пунктам, результат зафиксирован** — за человеком,
      агент этот пункт закрыть не может
- [x] Вики обновлена: раздел фазы 7 и таблица фаз, константы, открытые вопросы
- [x] Строка в `log.md`; маппинг новых скриптов в хук подсказок вики
- [x] Перепись RNG-потребителей вне геймплея дописана в тикет 17

## Как сделано

**Машинная приёмка — полный цикл, всё зелёное:**

| Проверка | Результат |
|---|---|
| Эталон-exe (`oracle.config`) | `EXIT=0`, `out/ref.csv` свежий |
| Порт (`run_scenario.gd`) | `EXIT=0` |
| Дифф трасс (`trace_diff.gd`) | `EXIT=0` — дискретные поля совпали на 450 спаренных тиках, все непрерывные в шуме (максимум `move_x` 0.00137 при допуске 0.001 — в пределах правила роста) |
| `dotnet build` | 0 предупреждений, 0 ошибок |
| `check_gpf_*` | 37 из 37 зелёные |
| Меню-загрузка headless | `EXIT=0`, ошибок нет |
| Сцена матча headless | `EXIT=0`, ровно baseline: `WorldEnvironment3D`, `!is_inside_tree()`, `states.has(p_name)`, дубль-регистрация переходов |

**Документация.** Вики: статус страницы порта (фаза 7 код-complete, следующая — 8), строка 7 в
таблице фаз, новый подраздел «Фаза 7 — что она дала», раздел «Сетка ворот» вместо утверждения «не
портирована», переписаны пункты про сетку и звук в «Что НЕ портировано», список файлов `src/gpf/`
и `src/lab/` пополнен восемью новыми, добавлен раздел `stadium_lab` в лаб-сцены, счёт тестов 28 → 37.
`константы.md`: новый раздел «Порт GameplayFootball (фаза 7)» — 20 строк чисел, каждое сверено
грепом по коду; шапка отмечает дату сверки этого раздела отдельно. `открытые-вопросы.md`: пункт про
сетку переписан в прошедшее время, добавлены хвосты фазы (трамплин сетки, спекуляр газона, время
генерации, недоказанный порядок бросков дрожания, порядок тика лабы). Врезки-указатели на
`сетка-ворот.md` и `поле-и-камера.md` — обе страницы описывают замороженный матч, и без врезки
читатель не узнал бы, что у порта своя сетка и своя камера. `расхождения-с-оригиналом.md`: два
упоминания фазы 7 переведены в прошедшее время. `log.md`: веха. `wiki-hint.py`: девять новых
маппингов (проверено прогоном хука). Тикет 17: таблица RNG-потребителей на стороне порта, три
уточнения для фазы 9.

Вики-линт после правок: 25 страниц, битых `[[ссылок]]` нет, сирот нет, все страницы в `index.md`.

**Наблюдение для глазной приёмки** (не дефект, чинить нельзя — тюнинг запрещён): яркость картинки
заметно гуляет между запусками из-за случайного солнца (`SetRandomSunParams`) — на одном прогоне
газон сочно-зелёный, на другом заметно серее из-за бликов на нормалях полос. Формула оригинала;
при сверке с эталон-exe стоит перезапустить обе стороны пару раз, а не судить по одному кадру.

**Глазной чек-лист пройден 2026-08-06.** Десять пунктов приняты; проход занял две сессии, потому что
две «жалобы» оказались отдельными задачами и разбирались в середине.

| Пункт | Вердикт |
|---|---|
| Стадион, пропорции | ок |
| Газон: полосы, шум, виньетка | ок (разная яркость между запусками — случайное солнце, см. ниже) |
| Разметка из overlay | ок |
| Щиты меняются от загрузки к загрузке | ок |
| Гол: сетка ловит и колышется | ок |
| Снаружи мяч проходит сквозь сетку | ок, «отловить тяжело» — попасть в узкое окно снаружи неудобно |
| Штанга: отскок со звуком, громкость от силы | ок |
| Касания: звук с вариацией питча | ок — пасы и удары звучат, громкость разная |
| Камера wide: ведение, сглаживание, дрожание | ок |
| После гола scorer-cam | ок, **с оговоркой ниже** |

Две находки прохода закрыты по ходу и в этот тикет не входят: **инверсия стрелок** — реальный баг фазы
7, маппинг остался от временной камеры (`c02d844`); **дриблинг на повороте** — не фаза 7, а
непортированный магнит `AI_GetBallControlMovement` (`cbacbd7`). Приём, которым обе разобраны: не спорить
о картинке, а прогнать один сценарий численно в `ball_lab` и `stadium_lab` — совпали, значит причина не
в презентации.

**Оговорка к scorer-cam (не дефект фазы 7, работа фазы 8).** Камера облетает забившего правильно, но
игрок остаётся полностью управляемым: празднований у порта пока нет. В оригинале это матч-слой. Хвост
записан в [[открытые-вопросы]].

**Запуск лабы** (для повторной проверки):

```powershell
& "C:\Users\User\Desktop\Godot_v4.7.1-stable_mono_win64\Godot_v4.7.1-stable_mono_win64_console.exe" `
  --path "C:\Users\User\Desktop\projects\OpenFootball" res://scenes/lab/stadium_lab.tscn
```

Эталон рядом: `cd "C:/Users/User/Desktop/projects/GameplayFootball/build/Release" && ./gameplayfootball.exe football.config`.
Чтобы увидеть гол и scorer-cam: довести мяч до ворот стрелками и ударить `S`; `R` — сброс.

# 07 — Приёмка фазы 5 на сценарии `walk_line`

Status: ready-for-human
Type: task

**What to build:** первое реальное сведение трасс. Один сценарий, два прогона, один вердикт: до какого
тика порт совпадает с эталоном по дискретным полям. Это **шаг 3** приёмки фазы 5 (шаги 1 и 2 —
байт-идентичность двух прогонов эталона на `walk_line` и на `rng_probe` — закрыты тикетом 02).

**Сценарий: контролируемый игрок бежит фиксированный маршрут вдали от мяча.** Выбран так намеренно, и
причин две.

Первая: **RNG-патч не уравнивает потоки случайных чисел между сторонами.** Одинаковый xorshift64\* с
одинаковым сидом даёт одинаковую последовательность, но у эталона из неё тянут 22 игрока, судья и
`TeamAI`, а у порта — один гуманоид. На одного потребителя приходят разные числа. Патч даёт другое и
достаточное: эталон становится воспроизводимым от прогона к прогону.

Вторая: **временные расхождения из `расхождения-с-оригиналом.md`, которые фаза 5 не закрывает.** Статы
и рост закрыты точкой патча 6 (форс 0.6 и `defaultPlayerHeight` на стороне эталона), но `MentalImage`,
суррогатные швы матч-слоя (`HasPossession`, `AllowLastDitch`, `focusDistance`) и непортированные
`AI_GetPass`/`AI_GetShotDirection` живы до фазы 8, и белым списком они не лечатся: список исключает
поля, а незакрытый шов отравляет все поля разом. Отсюда жёсткие требования к сценарию: **дистанция до
мяча больше 10 м на всём прогоне, ни одного касания, ни одной кнопки действия** — при мяче дальше 10 м
ранние выходы `SelectAnim` срабатывают до всех потребителей задержки восприятия. Дифф печатает
предупреждение, если прогон вышел за эту область.

**Найденное не чинится здесь.** Первый прогон ретроспективно проверяет уже написанные 7 761 строку фаз
1–4, и ненулевая вероятность, что он найдёт ошибку в уже «принятом». Роадмап считает это плюсом вехи,
а не риском. Каждая находка оформляется отдельным тикетом; фаза 5 измеряет, а не чинит, и объём вехи
не должен размываться починками.

**Blocked by:** 03, 05.

- [x] Сценарий `walk_line` лежит в `tests/scenarios/`, с комментарием о том, что он проверяет.
      **Переделан этой работой:** маршрут развёрнут в +Y, длина срезана до 450 тиков; в комментарии
      сценария записано, почему именно так и чем это замерено
- [x] Маршрут держит игрока дальше 10 м от мяча и не содержит кнопок действия — дифф не печатает
      предупреждения о выходе за безопасную область. Минимум дистанции 15.98 м (тик 0), дальше растёт
      до 37 м; касаний ноль в обеих трассах
- [x] Прогон эталона и прогон порта на этом сценарии выполнены, обе трассы и оба манифеста получены.
      Эталон прогнан **дважды** — трасса и манифест байт-идентичны (`cmp`), то есть шаг 1 приёмки
      переподтверждён на новом сценарии
- [x] Вердикт диффа манифестов зафиксирован: **совпали**, 1452 клипа, тот же порядок, дублей пары
      `anim_name`+`foot` ноль, код возврата 0. Тикет на порядок загрузки не нужен
- [x] Контроль не переезжал: `controlled = 1` на всех 450 тиках у игрока 26; спарено 450 из 450,
      тиков без контролируемого ноль
- [x] Вердикт диффа трасс по дискретным полям зафиксирован: **первое расхождение — тик 0, поле
      `anim_name`** (`movement/idle/000_idlelevel1` против `autogen [v0 b0] => [v0 b0 a0]_mirror`);
      `anim_type`, `function_type` и `touch_frame` совпали на всей длине. Код возврата 1
- [x] Вердикт непрерывного слоя зафиксирован: `angle` с тика 1, `pos_x`/`pos_y`/`move_y` с тика 4,
      `rel_body_angle` с 22, `move_x` с 29; `pos_z`, `move_z` и оба смаггла — в шуме (ровно нули)
- [x] Обе команды прогона и команда диффа записаны в [run-commands.md](../run-commands.md); цикл
      «прогнал — сдиффал» повторяется тремя командами оттуда без вспоминания флагов
- [x] Найденное расхождение — уже заведённый [тикет 10](./10-idle-anim-id-rashoditsya-s-originalom.md);
      новых тикетов по порту не заводится, всё остальное в отчёте — его следствие. Ни одна строка
      портированного геймплея этой работой не изменена. Отдельно заведён
      [тикет 11](./11-uglovye-polya-diffa-bez-perioda.md) — дефект **инструмента**, найденный этой же
      приёмкой
- [x] Расхождение известным отступлением **не** объясняется: это ошибка порта, а не сознательное
      отличие, поэтому строки ни в `docs/wiki/расхождения-с-оригиналом.md`, ни в
      `tools/trace_whitelist.txt` не добавлено. Белый список остаётся пустым

Полный вердикт с цифрами — [acceptance-verdict.md](../acceptance-verdict.md).

## Решение, которого не было в тексте тикета

**Сценарий переделан, а не принят как есть.** Тикет предполагал, что `walk_line` требованиям уже
удовлетворяет; дифф показал обратное по всем четырём пунктам (контроль переезжал на 421, дистанция
падала до 6.3 м, 109 тиков с касанием, 322 тика без контролируемого). Переделка — это и есть работа
тикета 07, хвост, оставленный ему тикетом 09.

**450 тиков — это потолок, а не выбор.** `Team::UpdateSwitch` (`team.cpp:534-541`) отдаёт управление
партнёру, взявшему уникальное владение; запретить это сценарий не может, кнопка `Switch` тут ни при
чём. Команда 1 забирает мяч на тике 460 (замерено прогоном на 600 тиков), значит длиннее 460 приёмка
в живом матче невозможна в принципе. Более длинный прогон потребует либо второй точки съёма (не
человеческая команда), либо матч-слоя порта — то есть фазы 8.

## Чего эта приёмка НЕ доказывает

Сравнение обрывается о собственную первую находку. Пока `GetIdleMovementAnimID` не починен, стороны
стоят на разных клипах с нулевого тика, и ретроспективная проверка 7 761 строки фаз 1–4 — главный
ожидаемый эффект вехи — **ещё не выполнена**. Она станет возможной после тикета 10, повторным
прогоном той же пары команд.

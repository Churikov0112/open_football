# Порт ядра GameplayFootball (C#)

Порт игрового ядра [GameplayFootball](https://github.com/vi3itor/GameplayFootball) (Apache 2.0) —
стратегическое решение от 2026-07-29: вместо дальнейшего тюнинга собственного `PlayerMotor`/lead-follow
переносим проверенное 9/10-ощущение оригинала. Полный контекст решения, отвергнутые альтернативы
(active-ragdoll/Rabona, GDExtension, big-bang перевод на C#) и фазировка — в роадмап-спеке
`docs/superpowers/specs/2026-07-29-gameplayfootball-port-roadmap-design.md`; разбор оригинального кода
по файлам и номерам строк — в приложенном тех-отчёте `docs/superpowers/specs/
2026-07-29-gameplayfootball-core-report.md`. Ядро портируется на **C#** (`.NET`-редакция Godot 4.7.1,
solution `OpenFootball.sln`); существующий GDScript **не** переписывается превентивно — подсистема
замещается тогда, когда до неё дошла своя фаза порта, и уезжает в `legacy/`, а не удаляется. Скоуп с
2026-08-04 расширен до **всей игры** — см. раздел «Курс с 2026-08-04» ниже.

## Статус: фаза 4 (смагглы + физика мяча) завершена и принята

Фазы 1–3 закрыты и приняты визуально человеком (фаза 3 — 2026-07-30: непрерывный разгон/торможение/
повороты в `walk_lab`, без квантованных скачков по бакетам скорости; выпрямление корпуса при возврате в
спринт опознано как поведение оригинала).

Фаза 4 (задачи 1–10) **код-complete**: кватернионные примитивы и константы поля, `Gpf.Ball` — физика
мяча целиком минус сетка, отбор клипа по наследнику (crude query + сорт-цепочки + `NeedTouch`),
`GetBestCheatableAnimID` с деформированной смаггл-зоной, реальные смагглы в тике и apply-буфер
наследника, `ReQueue`, touch-векторы и исполнение касания, лаб-сцена `ball_lab`, перевод матча на
фикс-тик 100 Гц. Все **25** headless-тестов `tests/check_gpf_*.gd` зелёные, обе стандартные
headless-валидации без новых категорий ошибок, `dotnet build` чист (0 предупреждений).

**Приёмка глазами пройдена 2026-08-03**: в `ball_lab` проверены разные виды бега, пас и удар; матч после
перевода на фикс-тик 100 Гц ощущается как прежде. Первый прогон дал три наблюдения, все разобраны
замерами: кадёнция касаний задаётся скоростью (норма), зазор нога-мяч разложился на ненарисованную ступню
(дорисована) и развёрнутый смаггл (баг оригинала, сверен и оставлен), потеря мяча на повороте —
конструкция зоны чита плюс отсутствие автодогона в лабе. Детали и цифры — [[открытые-вопросы]].

Фаза 4 закрыла три открытых вопроса фазы 3: движение отбиралось базовым `AnimSelector` вместо цепочки
наследника, apply-буфер складывал смагглы в порядке базы, а полей `smooth`/`smoothFactor` не было вовсе.

Фаза 6 прежнего роадмапа («ретаргет на Mixamo») **отменена** решением 2026-08-03: скелет один —
утилитарный GPF, Mixamo уходит в легаси.

## Курс с 2026-08-04: портируется вся игра, а не ядро

Решение человека — **полный порт**: «портируем всё, просто не удаляем наш старый код». Спека —
`docs/superpowers/specs/2026-08-04-full-port-scope-roadmap-design.md`; она заменяет роадмап от 29.07 в
частях «Что остаётся нашим», фазы 5–6 и открытые хвосты.

**Граница:** портируется всё, что читает или пишет игровое состояние — матч, команды, игроки,
контроллеры, `Eliza`, `AIfunctions`, off-the-ball стратегии, `TeamAI` с формациями, судья с фолами и
карточками, `MentalImage`, поле, сетка ворот, камеры, БД игроков. Плюс всё, что нарисовано и звучит:
29 `.ase`, 123 текстуры, 6 wav, шрифты. **Не портируется только движок** — рендерер, граф сцены,
CPU-скиннинг, GUI-система, загрузчики форматов, ODE: их роль исполняет Godot. UI переносится по функциям,
все экраны, без повторения внешнего вида.

Следствия, принятые сознательно: игра становится **11 на 11**, поле переезжает в размеры оригинала
**110×72** (не FIFA), их пружинная сетка замещает наш `NetSim`, ветка стандартов и `PlayerFactory`/ростер
уезжают в `legacy/`. Взамен со `referee.cpp` приходят фолы и карточки. Модель игрока — оригинальная;
собственная линия (болванка, MPFB2) остаётся лежать и не развивается до конца порта.

**Правила на время порта:** ноль тюнинга (числа из оригинала как есть, тюнинг открывается после фазы 11);
каждое вынужденное отступление — строкой в [[расхождения-с-оригиналом]]; старый код не удаляется, а
уезжает в `legacy/` комплектом (скрипты + сцены + тесты + строки `project.godot`) под `.gdignore`;
GDScript-матч заморожен полностью (см. [[открытые-вопросы]]).

### Оракул — приёмка диффом трасс

Приёмка глазами не масштабируется: доказательство — фаза 4 была принята глазами, после чего зонд
`_probe_touches.gd` нашёл развёрнутый смаггл, срабатывавший на **100 %** касаний в лабе с одним игроком.
На 22 гуманоидах с ИИ и судьёй это безнадёжно.

Поэтому фаза 5 строит **оракул**: ветка `oracle` в форке `Churikov0112/GameplayFootball` (от `windows`).
Спека и тикеты — `.scratch/oracle/`; команды прогона — `.scratch/oracle/run-commands.md`, живой образец
трассы — `.scratch/oracle/trace-example.md`.

**Патч эталона — семь точек, а не три.** Роадмап называл три; остальные нашли разведка и первые
прогоны, и без каждой из них приёмка недостижима.

1. **ГПСЧ** — все вызовы `random(` идут через `boostrandom()` в `base/math/bluntmath.cpp`; тело
   заменено тем же `xorshift64*`, что в `Gpf.GpfRng`, с сидом из **сценария**.
2. **Ввод** — `HIDScript : IHIDevice` новыми файлами в `src/hid/`: ни клавиатуры, ни человека.
3. **Дамп** — одна добавленная строка в конце `Match::Process`; писатель новыми файлами.
4. **Автостарт** — в оракул-режиме форсируется существующий `MenuTask::QuickStart`, и человек играет
   за команду 1: начальный кикофф всегда у команды 0 (`referee.cpp:16-17`), а судья снимает
   `inSetPiece` только по касанию бьющего, так что за команду 0 матч не начинался вовсе.
5. **Порядок загрузки коллекции** — списки файлов сортируются побайтово по пути с прямыми слэшами,
   как `Ordinal` в порте. Иначе порядок диктует файловая система, и равные ключи `StableSort`
   разрешаются иначе — выбор клипа расходится **без ошибки в логике**.
6. **Статы и рост** — форсируются в 0.6 и `defaultPlayerHeight`, как заглушки порта: стат скорости
   входит в `GetMaxVelocity`, оттуда в варпинг и в выбор клипа.
7. **Put-фаза пиннится к тику** — иначе прогон невоспроизводим: `Match::CheckBallCollisions` читает
   геометрию сцены, которую пишет Put-фаза графической последовательности по стенным часам, и отскок
   мяча от тела становится функцией FPS. Оригинал это признаёт комментарием в точке вызова
   (`match.cpp:870`).

**Одного сида из конфига мало, потому что генератор оригинала глобальный, а его потребители живут не
только в матч-логике:** `opengl_renderer3d.cpp:2228-2229` тянет **два числа на каждый отрендеренный
кадр** (а частота кадров задаётся стенными часами), `proceduralpitch.cpp:454-457` генерирует текстуры
поля в четырёх `boost::thread` с восемью сайтами `random(` внутри, плюс `menu/`, `utils.cpp` и цвет кожи
при загрузке составов. Без разделения число вытянутых до данного тика значений зависело бы от FPS, а
конкурентные потоки дали бы ещё и гонку на состоянии.

Спека закладывалась на `thread_local`, и **это оказалось неверно**: фазы задач уходят в пул
`TaskManager` (`tasksequence.cpp:50-53`) и к потоку не привязаны — каждый тик может исполниться другим
потоком. Состояния два, но разделены **по назначению вызова**, а не по потоку: общее внутри
`DeterministicRandomScope` (игровая логика), потоковое черновое — рендер и генерация газона.

Чего этот патч **не** даёт — совпадения потоков чисел между сторонами: см. строку про RNG в
[[расхождения-с-оригиналом]].

**Дисциплина номеров строк:** весь порт заякорен на строки форка `vi3itor`, поэтому в каждом
существующем файле число добавленных строк равно числу удалённых. Свободны только `main.cpp` (не
портируется) и `sources.cmake`. Эталон для якорей всегда `git show windows:src/...`.

**Готовый дамп в оригинале частично был:** блок `_positionLogging` (`match.cpp:1105-1128`) пишет
`positions.log` и стоит ровно там, где нужно, — в конце `Match::Process`, после него только инкремент
`iterations`; жизненный цикл файла тоже проложен (`:393` открытие, `:488` закрытие). Переиспользовать
его нельзя — он пишет только позиции, без анимационного состояния, — поэтому писатель трассы
**дублируется рядом**, а существующий блок и его флаг не трогаются.

**Сторона порта:** `Gpf.TraceWriter` (писатель), `Gpf.Lab.OracleScenario` (парсер сценария),
оракул-режим в `BallLabMain` (порт `_GetHidInput`, правило нулевого тика, стартовое состояние из
первой строки трассы эталона), запуск `tools/run_scenario.gd`, приёмка `tests/check_gpf_trace.gd`.
Ядро про файлы и формат не знает; единственное, что в нём добавилось, — геттер
`HumanoidBase.GetSpatialDirectionVec()` для дедзоны `_GetHidInput`.

**`HumanController::_GetHidInput` (`humancontroller.cpp:497-523`) до фазы 5 портирован не был** —
перевода «направление + кнопки → желаемая скорость» в порте не существовало, лаба задавала скорость
номером (`SetCommand(dir, velocityId)`). Это второй новый шов на нашей стороне после писателя трассы, и
он живёт в лабе, а не в ядре: сценарий описан **в пространстве кнопок** именно затем, чтобы участок
«кнопки → команда» тоже попадал под дифф. Существующий `SetCommand` не менялся — на нём стоит
`check_gpf_ball_walker.gd`.

#### Формат сценария

Дельта-кодирование: строка на **изменение** ввода, а не на тик (минута при 100 Гц — это 6000 тиков,
посценарный список нечитаем). Состояние держится до следующего изменения. Файлы —
`tests/scenarios/*.txt` в репозитории **порта**; эталон получает путь ключом конфига, читают обе
стороны.

```
# walk_line: разгон по прямой ОТ мяча, поворот на 90°, торможение
seed 20260731
ticks 450
0    dir 0,1,0   buttons Sprint
250  dir -1,0,0
380  buttons -
```

- `seed` и `ticks` — **обязательные** заголовочные директивы; сценария без `seed` не бывает (молчаливого
  дефолта нет), `ticks` считается от тика 0 трассы, и по его достижении обе стороны закрывают файл.
- Строка = `<тик> [dir <x,y,z>] [buttons <Имя>|<Имя>|...|-]` и обязана нести **хотя бы один** токен:
  голый номер тика — ошибка парсера, а не no-op.
- Имена кнопок — из `e_ButtonFunction` **без префикса** (`Sprint`, `Dribble`, `ShortPass`, `Shot`, …),
  `-` = ни одной. Аналоговых значений сценарий не выражает: заряд паса и удара набирается длительностью
  удержания, а не величиной нажатия.
- **`Switch` запрещён** — парсер валит ошибкой: `Team::Process` по этой кнопке переводит управление на
  другого игрока, и строки `controlled = 1` начали бы принадлежать разным людям.
- **Длина вектора `dir` значима** — это отклонение стика: короче `analogStickDeadzone` (0.75) —
  дедзона, направление берётся текущее, скорость `idleVelocity`. `dir 0,0,0` — легальная запись «ввода
  нет». Оси — оригинала (Z вверх, «вперёд» = −Y), как в `IHIDevice::GetDirection()`.
- `#` — комментарий. **Неизвестный токен = ошибка с указанием строки**, не молчаливое игнорирование:
  проигнорированная строка даёт ложное совпадение трасс.

#### Формат трассы и манифеста

CSV, разделитель `,`, десятичная точка, все float — шестью знаками (`snprintf %.6f` ↔
`ToString("F6", InvariantCulture)`). **Схема файла одна, 25 полей в каждой строке**, первая колонка
`kind` разводит две разновидности; заголовок несёт имена колонок `P`-строки. Порядок внутри тика:
сначала `B`, затем `P` по возрастанию `player_id` — мяч отдельной строкой, чтобы на 22 игроках не
дублировать его 22 раза.

| Строка | Занятые поля |
|---|---|
| `P` — состояние игрока, ключ (`tick`, `player_id`) | `kind`, `tick`, `player_id`, `controlled`, `anim_name`, `anim_id`, `anim_type`, `function_type`, `frame_num`, `touch_frame`, `foot`, `enum_velocity`, `quadrant_id`, `pos_x/y/z`, `angle`, `rel_body_angle`, `move_x/y/z`, `action_smuggle`, `movement_smuggle`, `rotation_smuggle_begin`, `rotation_smuggle_end` — все 25 |
| `B` — состояние мяча, ключ `tick` | позиции 1–8: `kind`, `tick`, `ball_x/y/z`, `ball_mom_x/y/z`; остальные 17 пусты. Имена заголовка к ней **не относятся** — под именем `player_id` в `B`-строке лежит `ball_x` |

**`rotation_smuggle_begin/end` — не измеряемое поле, а вход порта.** Это единственное состояние клипа,
которое порт восстановить не может: `ResetSituation` его обнуляет (`humanoidbase.cpp:965-966`, верно
оригиналу), а у эталона на нулевом тике клип выбран нормальным отбором и смаггл зависит от истории
матча. Без него угол расходится **с первого же тика при полностью совпадающем клипе**: эталон
поворачивается на 0.0035 рад за тик шестнадцать тиков подряд (ease-in `humanoid.cpp:724-727`), порт
стоит. Порт сажает эти два числа в текущий клип **в конце пред-прокрутки**, когда условие нулевого тика
уже наступило: сажать раньше бесполезно — до нулевого тика гуманоид успевает несколько раз переизбрать
клип, и каждый выбор смаггл перезаписывает.

**`B`-строки в вердикт фазы 5 не входят:** порт ставит мяч из этой же строки с нулевым моментом, а у
эталона он в тот же тик может катиться после кикоффа — сравнение дало бы гарантированный «рост» на
каждом прогоне. Дифф читает их только ради дистанции до мяча; мяч войдёт в вердикт на фазе 8 вместе с
касаниями. **Вращения мяча и `float_velocity` в трассе нет намеренно** — публичных геттеров нет ни у
одной из сторон, а поле без геттера пришлось бы либо доставать правкой портируемых заголовков (против
дисциплины строк), либо подменять «похожей» величиной. Дискретный сигнал скорости несёт
`enum_velocity`, непрерывный — вектор `move_*`.

**Клип идентифицируется именем.** `anim_name` для файловых клипов — путь относительно `animations/`:
прямые слэши, нижний регистр, без `.anim` (`movement/idle/000`). Запятая в имени запрещена — сломала бы
CSV, и писатель на ней падает. Имена автогенов синтезированы одной формулой на обеих сторонах и **не
уникальны**; коллизия разрешается парой `anim_name` + `foot`, неразрешённые дифф берёт из манифеста и
сравнивает такие строки по `anim_id`. На живой библиотеке дублей ноль.

**Манифест коллекции** — отдельный артефакт, `index,anim_name,anim_type,foot,frame_count`, пишется раз
на старте каждой стороной. Сравнивается **построчно, включая `index`**: смысл манифеста именно в
порядке, потому что сорт-цепочка отбора идёт через `StableSort`, а тот разрешает равные ключи порядком
исходного списка. Совпадение манифеста и есть то, что делает `anim_id` законным точным ключом.

#### Правило нулевого тика

Эталон стартует **матч** (22 игрока, кикофф, судья), порт — **лабу** (один игрок и мяч). Без явного
правила выравнивания расхождение на первом же тике гарантировано.

Тик 0 — первый тик, на котором одновременно `IsInPlay() && !IsInSetPiece()` (до этого `Team::Process`
снимает выбор у человеческого геймера, и ввод сценария адресовать некому) и контролируемый стоит на
`idleMovementAnimId` с `frame_num == 0`. **Порт применяет ровно то же условие** — и это не
формальность: писатель зовётся в конце тика, а `Tick` инкрементирует `frameNum` в начале, поэтому
первый же шаг после `ResetSituation` дал бы `frame_num = 1` против `frame_num = 0` у эталона, и трассы
разъехались бы на тик по всем полям сразу.

**Трасса эталона — вход прогона порта:** из её первой `P`-строки с `controlled = 1` порт берёт
стартовые позицию и угол (`ResetSituation`), из `B`-строки того же тика — позицию **и момент** мяча.
Отсюда жёсткий порядок цикла: сначала эталон, потом порт, потом дифф. Пустая трасса или трасса без
`controlled = 1` — внятная ошибка, а не старт из нулей; условие нулевого тика ждёт не дольше 3000 тиков
матча, после чего обе стороны печатают последнее состояние контролируемого и выходят ненулём, не
создавая трассы.

**Момент мяча обязателен, хотя сценарий фазы 5 мяча не касается.** Спека закладывала нулевой момент —
это оказалось неверно: сорт-цепочка отбора клипа читает `Ball::Predict(200)` и по порогу **16 м**
выбирает `desiredIdleLevel` (`humanoid.cpp:1565-1566`), а от него зависит, какой idle-клип выигрывает.
С замороженным мячом порт видел 15.985 м вместо 16.48 и брал другой клип **на нулевом же тике**.
Позиция мяча перекладывается только на старте, а не каждый тик: иначе выключилась бы физика мяча
порта, которую обязана мерить фаза 8.

Отсюда же **требование к сценарию: мяч дальше 16 м от контролируемого на нулевом тике.** Правило нуля
сверяет текущий клип с `GetIdleMovementAnimID()`, а ближе 16 м эти два отбора дают разные клипы, и
условие не наступает никогда. У `walk_line` запас тонкий — 16.48 против 16.

Про ногу: `ResetSituation` ставит `foot = Right` дефолтом, но к концу тика `foot` уже перезаписан ногой
текущего клипа, поэтому при совпавшем `anim_name` совпадает и нога.

#### Три команды прогона

Порядок жёсткий — трасса эталона нужна порту как вход.

```bash
cd "C:/Users/User/Desktop/projects/GameplayFootball/build/Release" && ./gameplayfootball.exe oracle.config
```

```bash
"C:/Users/User/Desktop/Godot_v4.7.1-stable_mono_win64/Godot_v4.7.1-stable_mono_win64_console.exe" --path "C:/Users/User/Desktop/projects/OpenFootball" --headless -s res://tools/run_scenario.gd -- tests/scenarios/walk_line.txt out/ref.csv out/port.csv out/port_manifest.csv
```

```bash
"C:/Users/User/Desktop/Godot_v4.7.1-stable_mono_win64/Godot_v4.7.1-stable_mono_win64_console.exe" --path "C:/Users/User/Desktop/projects/OpenFootball" --headless -s res://tools/trace_diff.gd -- out/ref.csv out/port.csv out/ref_manifest.csv out/port_manifest.csv tools/trace_whitelist.txt
```

Каталог `out/` не версионируется и должен существовать: писатели создают файлы, но не каталоги. Ключи
`oracle.config` (`oracle_scenario`, `oracle_trace`, `oracle_manifest`) и разбор кодов возврата — в
`.scratch/oracle/run-commands.md`. **Вывод эталона идёт в `build/Release/log.txt`**, не в консоль: exe
собран с подсистемой WIN32, и `printf` некуда девать; строки оракула ищутся по `Oracle::`.

**Дифф двухслойный**, потому что побитового совпадения float между C++/MSVC и C#/.NET не бывает:
дискретные поля (`anim_name`, кадр, `functionType`, нога, `touchFrame`) сравниваются точно,
непрерывные — с допуском; известные расхождения исключаются по [[расхождения-с-оригиналом]].
Клип сравнивается **по имени**, а не по индексу: индексы сопоставимы только после того, как совпал
манифест коллекции.

Логика диффа — `tools/oracle_trace.gd` (дискретный слой) и `tools/oracle_manifest.gd` (манифесты),
CLI-обёртки `tools/trace_diff.gd` и `tools/manifest_diff.gd`, приёмка `tests/check_trace_diff.gd` на
синтетических трассах. Стадии жёстко упорядочены, и каждая при неудаче **останавливает** дифф, потому
что дальше сравнивать нечего:

1. **манифесты** — разошлись по составу или по порядку;
2. **постоянство контролируемого** — `controlled = 1` во всей трассе обязан стоять у одного
   `player_id`, иначе строки принадлежат разным людям (вердикт «контроль переезжал»);
3. **дискретные поля** спаренных строк — печатается только **первый** разошедшийся тик, поле, оба
   значения и контекст ±5 тиков; полная таблица расхождений не печатается никогда.

**Непрерывный слой** считается на всей длине трассы независимо от того, разошлось ли дискретное поле:
он ловит ошибки, **не меняющие выбор клипа**. Интересен не факт превышения допуска, а **начало роста** —
первый тик, где |Δ| уже больше допуска класса, дальше на окне `window` тиков не убывает ни разу и к
концу окна удваивается. Одиночный всплеск началом роста не считается. По каждому полю печатается одна
строка: тик начала роста либо «в шуме» вместе с наибольшим наблюдённым |Δ| (без него строка скрывала бы
поле, которое допуск превышает, но не растёт). Окно отсчитывается **по спаренным тикам**, а не по
номерам, иначе стандарты рвали бы его на каждом тике без контролируемого. **Угловой класс сравнивается
по кратчайшей дуге**: `−3.141574` и `3.141593` — один и тот же угол, и вычитание не только печатало бы
6.28 на пустом месте, но и рвало бы окно «не убывает» на настоящем росте, пересёкшем ±π, — то есть
молчало бы там, где обязано говорить. Растущее расхождение — тоже
расхождение: вердикт `drift`, код возврата ненулевой. Допуски и окно — в [[константы]], переопределяются
необязательным файлом допусков (шестой аргумент CLI).

Строки спариваются по (`tick`, `controlled = 1`); `player_id` в спаривании не участвует. **Граница
между «трасса битая» и «сравнивать нечего» проходит по `B`-строкам:** `B` пишется раз в тик
безусловно, поэтому дырка в её нумерации — пропущенный тик, то есть ошибка чтения, а тик, у которого
`B` есть, а строки `controlled = 1` нет, — законное состояние (`Team::Process` снимает выбор игрока,
пока мяч не в игре) и просто не спаривается; его считают в шапке отчёта. Туда же идут число
проигнорированных строк `controlled = 0`, неразрешённые коллизии имён автогенов (такие строки
сравниваются по `anim_id`) и предупреждение о выходе за безопасную область фазы 5 — касание мяча или
дистанция до него меньше 10 м. Код возврата: `0` — совпало, `1` — любое расхождение, `2` — сломанный
вход.

**Приёмка фазы 5 пройдена** (`.scratch/oracle/acceptance-verdict.md`): на сценарии `walk_line`
манифесты совпали побайтово (1452 клипа), контроль не переезжал, предупреждений о выходе за безопасную
область нет.

Оракул сразу окупился — обе находки ретроспективные, по уже принятым фазам 1–4, и обе не поймали ни
компилятор, ни 25 headless-тестов, ни глаз:

1. **`GetIdleMovementAnimID` стоял не на том объекте** (`.scratch/oracle/issues/10`, починено). В
   оригинале это метод `HumanoidBase`, и последние два сорта его цепочки читают `spatialState` —
   idle-клип есть функция позы игрока, а не свойство коллекции, кэшировать его при загрузке нельзя.
   Порт держал его константой `AnimCollection` и брал первый попавшийся movement-клип с медленными
   скоростями, то есть автоген. После починки нулевой тик совпадает полностью.
2. **`desiredLookAt` в movement-команде смотрел не туда** (`.scratch/oracle/issues/12`, починено).
   Лаба клала «позиция + направление × 10» — ровно ту версию, которая в оригинале лежит
   **закомментированной** (`humancontroller.cpp:315`). Настоящая формула
   (`playercontroller.cpp:432-443`, применение `:600`) доворачивает направление ввода **к мячу**, и тем
   сильнее, чем медленнее игрок. Поле кормит `KeepBestBodyDirectionAnims`, то есть решает выбор клипа
   напрямую: на тике 24 фильтр выбрасывал клип эталона из 8 выживших кандидатов.
3. **Расхождение на тике 84** (`.scratch/oracle/issues/13`) оказалось **не ошибкой порта**: мяч порта
   катился свободно и разошёлся с эталонным на 8 м за 84 тика, а `lookAt` считается от мяча, из него
   строится «забор» CULL WRONG ROTATIONAL SIDE (`animcollection.cpp:590-620`), и тот культил другие
   клипы. Закрыто решением кормить мяч из трассы **каждый тик**: сценарий фазы 5 мяча не касается,
   `B`-строки в вердикт не входят — мяч для порта такой же внешний вход, как 22 игрока ИИ у эталона.
   Заодно нашлась ошибка писателя: колонка `foot` писала ногу spatial-состояния (обновляется лишь
   после кадра 12) вместо исходящей ноги текущего клипа.

**Итог: дискретные поля совпали на всех 450 тиках, непрерывные — в шуме, код возврата 0.** Граница
совпадения шла тик 0 → 24 → 84 → весь прогон. Ретроспективная проверка фаз 1–4 на этом сценарии
**выполнена**: 7 761 строка порта воспроизводит патченый оригинал тик в тик.

Оракул при этом проверяет только пройденный путь: `walk_line` не касается мяча, не жмёт кнопок
действия и не уходит в стандарты, а сам мяч кормится из трассы — значит его физика на этом прогоне не
измеряется. Касания, пас, удар, падения, вратарь и 22 игрока ждут своих сценариев на фазах 6–9.

**Сценарий приёмки ограничен живым матчем.** `walk_line` бежит 450 тиков, и это потолок, а не выбор:
`Team::UpdateSwitch` (`team.cpp:534-541`) отдаёт управление партнёру, взявшему уникальное владение,
без всякой кнопки `Switch`, и команда человека забирает мяч на тике 460. Маршрут вдобавок развёрнут
**от** мяча (в +Y): кикофф всегда у команды 0, мяч уходит в коридор y ≈ −13..−20, и бег «вперёд» вёл
контролируемого прямо под мяч, в область, отравленную незакрытыми швами фазы 8. Прогон длиннее
потребует матч-слоя порта, то есть фазы 8.

### Фазы 5–11

| # | Веха | Приёмка |
|---|---|---|
| 5 | **Оракул** — патч ветки `oracle`, писатель трассы, формат сценария, дифф | ✅ **закрыта**: дифф на `walk_line` — совпадение по всем дискретным полям на 450 тиках, непрерывные в шуме |
| 6 | **Процедурные слои** — только «труба» офсетов (~110 строк): тело `CalculateGeomOffsets` **мертво в оригинале**, см. ниже | ✅ **закрыта**: дифф `walk_line` остался нулевым, `check_gpf_offsets` зелёный |
| 7 | **Поле, стадион, ассеты, камеры, сетка** — `proceduralpitch.cpp`, `ball.cpp:331-408`, 29 `.ase`, текстуры, звуки | глаза, бок о бок с exe |
| 8 | **Матч-слой, судья, БД** — `match.cpp`, `team.cpp`, `player.cpp`, `MentalImage`, контроллеры, `referee.cpp`, `database.sqlite` | трасса матча, два игрока под человеческим вводом |
| 9 | **ИИ и 11 на 11** — `elizacontroller`, `AIfunctions`, `offtheball`, `teamAIcontroller` | полный матч тик за тиком против оригинала |
| 10 | **UI и меню** — все экраны функционально на Godot UI | кликается и делает нужное |
| 11 | **Чистка** — замещённое в `legacy/`, вики в `docs/wiki/legacy/` | обе headless-команды зелёные |

**Фаза 6 схлопнулась: процедурные офсеты в оригинале мертвы.** Роадмап оценивал её в ~250 строк и
приёмку «трасса + глаза (слой ног)», исходя из того, что слой живой. Разведка 2026-08-05 показала
обратное: `CalculateGeomOffsets` вызывается ровно из одного места — `HumanoidBase::Process`
(`humanoidbase.cpp:719`), — а игроки исполняют полный override `Humanoid::Process` (`humanoid.cpp:92`),
который базу не зовёт (единственное его обращение к базе — `HumanoidBase::ResetSituation`). Голый
`HumanoidBase` инстанцируется только для судей (`playerofficial.cpp:30`), и там тело функции целиком
лежит внутри `/* todo */`. `SetOffset` во всём дереве вызывается только из этих двух тел. Значит карта
`offsets` пуста у всех и всегда, а ветка её применения в `Animation::Apply` (`animation.cpp:424-433`)
не срабатывает ни разу за матч.

Отсюда решение (2026-08-05): фаза переносит **только «трубу»** — `BiasedOffset`, карту `offsets`,
`SetOffset` (включая удаление записи при нулевом bias), восемь строк применения в `Animation::Apply` и
проводку карты через apply-буфер с очисткой там же, где в оригинале. **Сделано 2026-08-05**: дифф
`walk_line` остался нулевым (дискретные поля на всех 450 тиках, непрерывные в шуме — цифры совпали с
вердиктом фазы 5 до знака), новый `tests/check_gpf_offsets.gd` накрывает саму математику ветки
(абсолютный/относительный офсет, частичный bias, удаление при нулевом bias) — она понадобится фазе 9.
Тело функции отложено на
фазу 9: двум его веткам нужны живой соперник (`GetBestPossessionPlayerID` чужой команды) и мировые
позиции костей внутри ядра, а ядро про `Skeleton3D` не знает намеренно. Оживлять функцию **не**
станем — это была бы правка геймплея, а не порт: офсеты крутят кости, а `BallBodyCollider` считает
столкновение мяча с телом по отрезкам костей, то есть фазы 8–9 сверяли бы трассу с изменённой физикой
касаний. Приёмка фазы поэтому одна: дифф `walk_line` обязан остаться нулевым (по построению останется,
но гонять обязательно — правка лезет в `Animation::Apply` и в буфер тика). Доказательство мёртвости
десятью проверками и разбор отвергнутых вариантов —
`.scratch/oracle/issues/14-geom-offsety-mertvy-v-originale.md`; отложенное — [[открытые-вопросы]].

Побочный вывод методологический: роадмап оценивал объём фазы по числу строк C++, не проверив, что эти
строки исполняются. Перед фазами 7–10 проверять то же самое.

Судья приехал вместе с матч-слоем не по вкусу, а по коду: `referee.cpp` дёргает
`match->StartSetPiece()`/`StopSetPiece()`/`PrepareSetPiece()`, а логика розыгрыша живёт в `match.cpp`.
Ассеты стоят перед матч-слоем потому, что приёмка фазы 9 — полный матч на 22 игрока, и смотреть его без
поля, ворот и камеры невозможно.

**Риск, зафиксированный заранее:** `CrudeSelection` — линейный проход по всей коллекции клипов на каждый
выбор, а ReQueue пытается выбирать с периодом 20–80 мс. На 22 гуманоидах при 100 Гц это на порядок больше
вызовов, чем в лабе. Замерять на фазе 8 (когда игроков станет двое), а не когда их станет 22.

## Структура

- `assets/gpf/` — датасет GameplayFootball как есть: 293 файла `.anim` (+3 служебных `.anim.util`) под
  `assets/gpf/animations/**` (293 включают 10 в `templates/`), `LICENSE-GameplayFootball` (Apache 2.0).
  **Read-only данные** — это исходники оригинала, руками не редактируются.
- `src/gpf/` — переиспользуемое C#-ядро порта, без зависимости от лабы или матча:
  - `Animation.cs` (`Gpf.Animation`) — парсер `.anim` + интерполяция кадра (порт `animation.cpp`);
    фаза 2 добавила write-API (`Clone`/`SetKeyFrame`/`Shift`/`SetVariable`/`SetName`/
    `SetCurrentFootId`), `Mirror()`, `ConvertToStartFacingForwardIfIdle()` (вызывается из
    `LoadFromFile`), `NormalizeDirectionTags()` и 12 ленивых дескрипторов клипа (`GetIncoming/
    OutgoingVelocity`, `GetIncoming/OutgoingMovement`, `GetIncoming/OutgoingBodyAngle`,
    `GetIncoming/OutgoingBodyDirection`, `GetOutgoingDirection`, `GetOutgoingAngle`, `GetTranslation`)
    с кэшем `DirtyCache()`.
  - `QuatUtil.cs` (`Gpf.QuatUtil`) — кватернионная математика 1:1 с `quaternion.cpp` (slerp с `bias>1`
    для экстраполяции, `SameNeighborhood`, покомпонентный lerp) — **не** заменять на `Quaternion.Slerp`
    из Godot, поведение должно совпадать с оригиналом побитово; фаза 2 добавила `GetAngles`/
    `GetAnglesVec` (эйлеры) и `AngleAxis`, фаза 4 — `GetRotationTo`/`GetRotationAngle`/
    `GetRotationMultipliedBy` (`quaternion.cpp:403-424`) для вращения мяча. Осторожно:
    `GetAngleAxis` зовёт `acos` **без клампа** и на `|w|` чуть больше 1 даёт NaN, как оригинал, — а
    `GetRotationMultipliedBy` через него ходит.
  - `SkeletonBuilder.cs` (`Gpf.SkeletonBuilder`) — 14-костный `Skeleton3D` 1:1 `player.object` + базис
    конверсии осей `GpfSpace`.
  - `AnimationApplier.cs` (`Gpf.AnimationApplier`) — применение сэмпла клипа на `Skeleton3D`, порт
    `Animation::Apply`; фаза 2 добавила `baseRotZ`-доворот позиции корня (`position.Rotate2D`) и
    `basePos`-смещение (`animation.cpp:413-415, 715`) — закрывает шов, оставленный в фазе 1; фаза 6
    добавила `BiasedOffset` (порт `animation.hpp:46-54`), карту `Offsets` и ветку её применения
    (`:424-433`). Карта живёт **полем применителя**, а не параметром `Apply`, как в C++: тип
    `Dictionary<string, BiasedOffset>` через мост GDScript не проходит, а `Apply` зовут из
    `check_gpf_apply.gd` — параметром метод стал бы для GDScript невидимым. Тот же ШОВ, что у
    `_movementHistory`.
  - `BluntMath.cs` (`Gpf.BluntMath`) — порт `bluntmath.{hpp,cpp}`/`vector3.hpp`/`utils.cpp`:
    `Curve`/`NormalizedClamp`/`ModulateIntoRange`/`FixAngle`/`AtoF`/`AtoI`/`GetVectorFromString`/
    `GetRotated2D`/`GetAngle2D` (обе перегрузки)/`GetNormalized`/`Get2D`. Общий математический
    фундамент фазы 2, читают все остальные новые файлы.
  - `Velo.cs` (`Gpf.Velo`) — константы скоростей оригинала (`gamedefines.hpp:18-27`: idle 0 / dribble
    3.5 / walk 5.0 / sprint 8.0, переключатели 1.8/4.2/6.0, `AnimSprint` 7.0) + конверсии
    `RangeVelocity`/`ClampVelocity`/`FloorVelocity`/`EnumToFloatVelocity`/`FloatToEnumVelocity`/
    `GetVelocityID`. `AnimSprint = 7.0` — исходное пространство спринта для `StretchSprintTo`, **не
    путать** с литералом 7.0 в `Animation.BucketVelocity` (там квантование дескрипторов клипа).
  - `GpfPitch.cs` (`Gpf.GpfPitch`, фаза 4) — размеры поля/ворот оригинала (`gamedefines.hpp`):
    полуширина 55, полудлина 36, ворота 3.7×2.5 при глубине 2.55, горизонт предсказания мяча 3000 мс,
    история 4000 мс. **Данные, не тюнинг** — меняются только вслед за C++. Ядро порта живёт в «их»
    размерах; сведение с FIFA-полем проекта ([[константы]]) — не задача фазы 4, лаба гоняется прямо в
    этих числах. `RefCounted` с `Get`-обёртками: через мост GDScript видит только статические методы,
    `const`-поля не читаются.
  - `GpfRng.cs` (`Gpf.GpfRng`, фаза 4) — xorshift64\* вместо unseeded `random()` оригинала
    (мультиплеер-дисциплина №3 роадмапа: ядро должно быть детерминированным). Инжектируется в
    `Humanoid`/`TouchVectors` сеттером, лаба сидирует явно. **Последовательность с эталон-exe заведомо
    не совпадает** — у оригинала сид от времени; расхождение принято и задокументировано.
  - `AnimCollection.cs` (`Gpf.AnimCollection`) — 34 квадранта (idle + 3 скорости × 11 углов), `Load`
    всей библиотеки (файлы ×2 зеркала + автогены из `templates/`), `GenerateAutoAnims`, `PrepareAnim`
    (дескрипторы + `touch_bodypart` + `quadrant_id`), `CrudeSelection` (грубый булев фильтр,
    ~12 независимых секций); фаза 3 добавила кэш позиций корня per-клип (`_positionCaches`, порт
    `Match::Match`, `match.cpp:86-105`) — параллельный `_animations` список, читают `PhysicsVector` и
    `HumanoidBase`.
  - `CrudeSelectionQuery.cs` (`Gpf.CrudeSelectionQuery`) — параметры запроса `CrudeSelection`: тип
    функции, стопа, сторона, вход/выход скорости, подбор мяча, направление тела/мяча, свойства,
    тип спотыкания, форсированная нога.
  - `AnimSelector.cs` (`Gpf.AnimSelector`) — `StableSort` (единственный разрешённый способ сортировки
    отбора), `ForceInto*`-таблицы, предикаты сравнения клипов и movement-путь `SelectAnim`
    (`SelectMovementAnim`/`SelectMovementDataSet`).
  - `SpatialState.cs` (`Gpf.SpatialState`) — обычный C#-класс (не `RefCounted`, через мост не ходит),
    порт `struct SpatialState` (`humanoidbase.hpp:172-194`): 19 полей физического+анимационного
    состояния тела (позиция/угол/скорость, три варианта движения, relative-body-direction, нога);
    дефолты повторяют `HumanoidBase::ResetPosition` (`humanoidbase.cpp:937-947`, `foot=Right`).
  - `PhysicsVector.cs` (`Gpf.PhysicsVector`) — порт `HumanoidBase::CalculatePhysicsVector`
    (`humanoidbase.cpp:2014-2544`, варпинг траектории клипа под физику игрока: статы, разгон/
    торможение, сопротивление воздуха, макс. скорость, ограничение угла поворота за кадр) + статики
    `CalculateMovementAtFrame`/`StretchSprintTo`/`GetMaxVelocity` (`humanoid_utils.cpp:68-100,129-144`,
    `playerbase.cpp:131-139`). `RefCounted` с сеттерами состояния/статов/конфига и мостом
    `CalculateForAnim`/`GetLastPositions`/`GetLastRotationOffset` для тестов.
  - `HumanoidBase.cs` (`Gpf.HumanoidBase`) — контур состояния movement-пути: порядок тика `Tick`
    (`CalculateSpatialState` → `frameNum++` → выбор следующего клипа на границе → apply-буфер,
    `humanoidbase.cpp:585-711`), `CalculateSpatialState`/`CalculateFactualSpatialState` (`:1622-1737`),
    выбор следующего движения-клипа (`SelectNextMovementAnim`, движение-ветка `SelectAnim`,
    `:1374-1601`). Два места портированы по НАСЛЕДНИКУ `Humanoid` (путь игроков, не базовый
    `HumanoidBase` — судьи): лерп `rotationSmuggleOffset` с 16-кадровым капом (`humanoid.cpp:722-742`)
    и «hax»-формула `desiredBodyDirectionRel` (`humanoid.cpp:1664-1665`) — см. «Инварианты фазы 3».
  - `Humanoid.cs` (партиальная часть того же `HumanoidBase`, фаза 4) — путь **игроков**: `SelectAnim`
    наследника (`humanoid.cpp:1159-1820` — crude query, сорт-цепочка, ветки по типам, `NeedTouch`),
    `GetBestCheatableAnimID`/`GetBodyBallDistanceAdvantage`, `CalculateMovementSmuggle`,
    `GetBestPossibleTouch`, `ExecuteTouchTick`. Самый большой файл ядра (~2100 строк). Отдельного класса
    нет намеренно: игроки в оригинале инстанцируются как `Humanoid` (`player.cpp:88`), и разводить
    partial на два типа значило бы разойтись с оригиналом.
  - `PlayerCommand.cs` (`Gpf.PlayerCommand`, фаза 4) — команда контроллера/ИИ (`gamedefines.hpp:175-229`):
    желаемый `FunctionType`, движение, `lookAt`, `TouchInfo` (направление/сила ввода и желаемого),
    trip-тип, модификатор knock-on. Обычный C#-класс, не `RefCounted`. **`Clone()` обязателен**: в C++
    это struct, и снимок `OriginatingCommand` копируется по значению — ReQueue-фильтры сравниваются
    именно со снимком, ссылка позволила бы переписать его задним числом.
  - `TouchVectors.cs` (`Gpf.TouchVectors` + `Gpf.BallBodyCollider`, фаза 4) — порт
    `humanoid_utils.cpp:146-520` (`GetDifficultyFactors`, `GetBallControlVector`, `GetTrapVector`,
    `GetShotVector`) и `Match::CheckBallCollisions` (`match.cpp:1926-2045`). Всё статики, `Match*`/
    `Player*` развёрнуты в плоские аргументы, `random()` заменён инжектируемым `GpfRng`. Параметры
    оригинала, не читаемые в теле, **сохранены в сигнатурах** и погашены `_ = ...`.
  - `Ball.cs` (`Gpf.Ball`, фаза 4) — физика мяча целиком (`ball.cpp:137-615`) минус сетка ворот:
    `Process` (тик), `CalculatePrediction` (один проход считает и кэш траектории на 3 с вперёд, и
    состояние на +10 мс), `Predict(ms)`, `Touch`, `SetPosition`/`SetMomentum`/`SetRotation`,
    `GetAveragePosition`, `ResetSituation`. См. «Инварианты фазы 4» — порядок секций внутри шага там
    объявлен неприкосновенным.
- `src/lab/` — сцены-приёмники для глаз, к матчу отношения не имеют:
  - `LabMain.cs` (`Gpf.Lab.LabMain`) — корень `scenes/lab/anim_lab.tscn`: сканирует
    `assets/gpf/animations`, листает клипы, держит фикс-тик 100 Гц. С 2026-08-03 грузит серую
    **болванку тела** (`assets/models/gpf_blockout.glb`, см. [[презентация-и-ассеты]]) и берёт её
    `Skeleton3D` вместо построенного `BuildUtilitySkeleton` — модель приходит со скелетом, сверенным
    тестом `check_gpf_blockout.gd`. Нет файла — молча откатывается на палочник, в лог идёт какой
    режим. Клавиша `M` прячет меш, чтобы увидеть кости внутри.
  - `StickmanRenderer.cs` (`Gpf.Lab.StickmanRenderer`) — линии костей по `GetBoneGlobalPose`; левая
    сторона красная, правая синяя (визуальный контроль зеркала). Плюс **ступня** — отрезок
    голеностоп → носок вдоль локального `−Y` кости (`FootLength = 0.18` м): в скелете ступни нет, а
    касание в данных клипа объявлено впереди-ниже косточки, и без этого отрезка на приёмке нога
    визуально «не достаёт» до мяча на длину ступни. Чистая презентация — ядро про ступни не знает.
  - `BallLabMain.cs` (`Gpf.Lab.BallLabMain`, фаза 4) — корень `scenes/lab/ball_lab.tscn`: игрок + мяч на
    ядре порта. **Оркестратор одного тика** в порядке `Match::Process`: очередь команд → `Humanoid.Tick`
    → `BallBodyCollider.Check` → `Ball.Process` → отрисовка. Держит то, что в оригинале принадлежит
    матчу, а не гуманоиду (кулдаун межтельных коллизий), и буфер действия человека-контроллера.
  - `WalkLabMain.cs` (`Gpf.Lab.WalkLabMain`) — корень `scenes/lab/walk_lab.tscn`: грузит всю
    `AnimCollection`, палочник шагает/бежит/поворачивает по клавишным командам; фаза 3 убрала
    lite-интеграцию фазы 2 — состояние, выбор клипа и apply-буфер целиком за `Gpf.HumanoidBase`, лаба
    только вводит команду, отрисовывает и печатает HUD. Клавиша `A` циклит пресеты статов
    (0.3/0.6/0.9, дефолт 0.6); HUD `v=` печатает непрерывную скорость в м/с (не бакет).
  - `LabBody.cs` (`Gpf.Lab.LabBody`) — общее видимое тело для всех трёх лаб: грузит
    `gpf_fullbody.glb`, отдаёт его `Skeleton3D` и список **всех 12** `MeshInstance3D` (взять первый —
    значит красить и прятать одну двенадцатую). Клавиши `M`/`T` — ниже, раздел лаб-сцен.
- `scenes/lab/walk_lab.tscn` — минимальная сцена (один `Node3D` со скриптом `WalkLabMain.cs`),
  параллельна `anim_lab.tscn`, друг друга не трогают.

## Формат `.anim` — три части файла

`Animation.LoadFromFile` разбирает файл в три фазы подряд (`LoadData` → `LoadFootballExtension` →
`LoadXmlTail`, порт `Animation::Load`):

1. **CSV-строки** — 14 строк-треков в фиксированном порядке `TrackOrder` (`player`, `body`, `middle`,
   `neck`, оба плеча/локтя, оба бедра/колена/лодыжки). Ключи разрежены: строка `player` несёт только
   позицию корня (`frame,x,y,z`), остальные 13 — только **абсолютную локальную ориентацию** джойнта
   (`frame,qx,qy,qz,qw`, порядок компонент как в `Godot.Quaternion`).
2. **`extension`-строки** (опционально) — `extension,football,frame,x,y,z,frame,x,y,z,...`: кадры
   касания мяча + позиция мяча в пространстве клипа. Оригинал чистит карту касаний на каждый вызов
   `Load`, поэтому при нескольких `extension,football`-строках в одном файле переживает только
   последняя.
3. **XML-хвост** — плоские теги верхнего уровня (`<type>`, `<baseanim>`, `<balldirection>`, ...) →
   словарь тег→сырой текст, читается через `GetVariable(tag)`/`GetAnimType()`.

## Инварианты

- **«Их» пространство остаётся внутри.** Клип и скелет живут в осях оригинала (Z-вверх, «вперёд» = −Y).
  Конверсия в Godot-оси происходит **только** на обёртке `GpfSpace` (`Node3D` с базисом
  `SkeletonBuilder.AxisConversionBasis()` — столбцы `X(−1,0,0) Y(0,0,1) Z(0,1,0)`, `det=+1`). Кости и
  данные клипа сами по себе никогда не переводятся в другие оси.
- **`CultureInfo.InvariantCulture`** на каждом `float`/`int` парсинге (`ParseF`, `int.Parse` в
  `LoadData`/`LoadFootballExtension`) — иначе парсинг ловит региональные настройки Windows (запятая как
  разделитель дробной части вместо точки).
- **Кадр = 10 мс.** `timeOffsetMs` семплит между `frame` и `frame+1` в пределах одного 10-мс кадра
  (`SampleBias`).
- **`frameCount = maxFrame + 1`**, не число ключей. `SetKeyFrame` растит `_frameCount` при каждом кадре
  `>= _frameCount` (порт `animation.cpp:123`) — трек может кончиться раньше `frameCount`, тогда
  интерполяция держит последний ключ до конца клипа (см. `GetInterpolatedValues`).
- **Сентинел `timeOffsetMs < 0` → `bias 0.5`**, не 0. Порт `animation.cpp:391-392`: «смещение неизвестно»
  семантически отличается от «смещение 0 мс» — рендер-путь оригинала (`humanoidbase.cpp:779`) зовёт
  `Apply` именно с этим сентинелом. Проверено тестом `check_gpf_anim_sample.gd` (сентинел −1 даёт то же,
  что offset 5 мс).

## Инварианты фазы 2 (`AnimCollection`/`AnimSelector`)

- **Сортировки только через `AnimSelector.StableSort`.** `List<int>.Sort` в C# **не**гарантированно
  стабилен (в отличие от `std::stable_sort` оригинала) — нестабильная сортировка даёт недетерминированный
  выбор клипа при равных ключах сравнения. `StableSort` реализован через LINQ `OrderBy` (документированно
  стабилен); в порте запрещено звать `List.Sort`/`Array.Sort` на любом `dataSet` отбора клипов.
- **Bug-for-bug `animcollection.cpp:619`.** Оригинал в CULL WRONG ROTATIONAL SIDE складывает
  `int`-сторону (`e_Side`, 0/1) с углом в радианах (`queryIncomingToFenceSide + fenceToOutgoingAngle`) —
  похоже на баг (нужен был угол `queryIncomingToFenceAngle`), но портируется **дословно, без починки**
  (`CrudeSelectionInternal`, секция `BySide`, `src/gpf/AnimCollection.cs`).
- **Кумулятивный lean в `GenerateAutoAnims` — так и задумано.** `movementChangeMps *= ...` (:315) стоит
  ВНУТРИ кадрового цикла тела и накапливается от кадра к кадру — это не забытый reset, а поведение
  оригинала; портировано как есть с комментарием на месте.
- **Порядок зеркал разный для файловых клипов и автогенов.** Файловая ветка `Load` добавляет клип в
  порядке оригинал→зеркало (`mirror` 0 затем 1). Ветка автогенов — наоборот, зеркало→оригинал
  (`animcollection.cpp:408-419`): для каждого сгенерированного клипа сначала `Clone().Mirror()`, потом
  сам оригинал.
- **`ConvertToStartFacingForwardIfIdle` работает внутри `LoadFromFile`.** Клипы с idle-входом
  (`GetIncomingVelocity() < 1.8`) разворачиваются лицом вперёд ещё на загрузке — до попадания в
  коллекцию; позиции root, ориентации body, касания и direction-теги вращаются синхронно.
- **Квантование скорости в дескрипторах клипа — верхняя корзина 7.0, не `Velo.Sprint` (8.0).**
  `Animation.BucketVelocity` (`animation.cpp:825-828`/`:902-905`, используется в
  `GetIncoming/OutgoingVelocity`): `<1.8→0`, `[1.8,4.2)→3.5`, `[4.2,6.0)→5.0`, `≥6.0→7.0`. Это отдельная
  функция от `Velo.RangeVelocity`, у которой верхняя корзина честные 8.0 (`Sprint`) — она используется
  для позиций квадрантов `AnimCollection`. Литерал `7.0` — так в оригинале, не опечатка порта.
- **Направление-теги существуют в ДВУХ форматах — bug-for-bug.** После `NormalizeDirectionTags()` (в
  `LoadFromFile`) значение сериализуется БЕЗ пробелов после запятых (`"0.000000,1.000000,0.000000"` —
  инлайн-формат `real_to_str` оригинала, `animation.cpp:1162/1170/1178`). После `Mirror()`/
  `ConvertToStartFacingForwardIfIdle()` то же значение переписывается через `BluntMath
  .GetStringFromVector` — С пробелами (`"0.000000, 1.000000, 0.000000"`, `utils.cpp:213-219`,
  `animation.cpp:337-339,1308-1310`). `BluntMath.GetVectorFromString` парсит оба формата (`Split(',')` +
  `Trim()` на каждом токене) — читатель не должен полагаться на конкретный формат строки.
- **`AnimationApplier.Apply` теперь доворачивает и позицию корня.** Шов, оставленный в фазе 1
  (`position.Rotate2D(baseRot)` не был подключён), закрыт: `!noPos` доворачивает `pos` на `baseRotZ` и
  прибавляет `basePos` (`animation.cpp:413-415,715`). Прежний открытый вопрос закрыт и удалён из
  [[открытые-вопросы]].

## Инварианты фазы 3 (`PhysicsVector`/`HumanoidBase`)

- **Два разных `GetMaxVelocity` в одном `Calculate`.** В начале функции (`:2065`) скорость считается
  один раз и touch-жмётся `*0.92` (`:2066`) — этим локальным значением масштабируется спринтовая часть
  клипа (`StretchSprintTo`, `:2241`). В блоке сопротивления воздуха (`:2422-2427`) оригинал зовёт
  `GetMaxVelocity()` ЗАНОВО, БЕЗ touch-множителя — не переиспользует локальную переменную `:2065`. Порт
  бьёт оба места отдельными вызовами `GetMaxVelocity(_statVelocity)`, не кэширует значение `:2065` для
  `:2422` — bug-for-bug, не оптимизация.
- **`(int)`-усечение кадра (`:2229`).** `CalculateMovementAtFrame` зовётся с
  `(int)(anim.GetEffectiveFrameCount() * adaptedFrameBias)` — оригинальный `(unsigned int)`-каст того
  же выражения (`humanoid_utils.hpp:25`) усекает вниз; C#-`(int)` на неотрицательном float делает то же
  самое. Кастовать именно на границе вызова, не раньше (промежуточный `float` теряет точность иначе).
- **Mod-флаги — `static readonly`, не `const`.** Четыре выключенных мода оригинала
  (`ModCorneringBraking`/`ModMaximumAccelDecel`/`ModBrakeOnTouch`/`ModCheatBodyDirection`, `:2138-2146`)
  и их мёртвые ветки портированы дословно и продолжают компилироваться (`static readonly bool`, не
  `const` — иначе C# ловит недостижимый код как `CS0162`). Не удалять эти ветки при рефакторинге — это
  дословный порт выключенного функционала оригинала, не мусор.
- **`Velo.AnimSprint` (7.0) ≠ литерал 7.0 квантования дескрипторов.** `AnimSprint` — `gamedefines.hpp:23`,
  исходное пространство спринта для `StretchSprintTo` (`:2241`, растяжка клипа под `GetMaxVelocity`);
  литерал `7.0` в `Animation.BucketVelocity` (фаза 2, инварианты выше) — верхняя корзина квантования
  `incoming`/`outgoingVelocity` дескрипторов клипа. Числовое совпадение случайно, роли разные — не
  объединять при рефакторинге.
- **Последние два кадра клипа (`frameCount-2`) — жёсткое квантование idle-ности.** С `timeMs >=
  (frameCount-2)*10` (`:2459`) итоговая скорость кадра принудительно приводится к той же idle/не-idle
  категории, что заявляет `outgoingVelocity` клипа (`:2468-2471`): если клип «должен» кончиться в idle,
  а физика ещё разогнана — обнуляем; если клип не-idle, а физика ещё не разогналась — подтягиваем к
  `Velo.Dribble`. Мягкая недо-версия (`:2464-2467`) — мёртвая ветка (`hardQuantize` всегда `true`,
  `:2461-2462`), портирована, но недостижима.
- **Порядок тика `HumanoidBase.Tick`: `CalculateSpatialState → frameNum++ → выбор клипа → apply`.**
  Именно в этом порядке (`humanoidbase.cpp:585-594` → `:615-637` → `:700-711`): состояние читается ДО
  инкремента кадра (то есть для ещё не показанного кадра предыдущего клипа), кадр инкрементируется, и
  только потом на границе клипа (`frameNum == frameCount-1`) идёт выбор следующего движения; apply-буфер
  собирается в конце тика из уже (возможно) обновлённого `_current`. Перестановка любого шага ломает
  синхронизацию между `positions[frameNum]` и заявленным `frameNum`.
- **`rotationSmuggleOffset` и `desiredBodyDirectionRel` портированы по НАСЛЕДНИКУ `Humanoid`, не по базе
  `HumanoidBase`.** Игроки инстанцируются как `Humanoid` (`player.cpp:88`); голый `HumanoidBase` в
  оригинале — только судьи. Поэтому:
  - лерп `rotationSmuggleOffset` — версия наследника с 16-кадровым ease-in капом
    (`humanoid.cpp:722-742`), НЕ незакапленный лерп базы (`humanoidbase.cpp:687-695`);
  - `desiredBodyDirectionRel` — «hax»-формула наследника (`humanoid.cpp:1664-1665`: упреждение
    `_spatial.Movement * 0.1f`, нормализация ДО `GetRotated2D`, БЕЗ вычета `nextAnim.GetTranslation()`),
    НЕ формула базы (`humanoidbase.cpp:1529-1530`). Перенесено bug-for-bug, включая todo-«hax» оригинала
    — не чинить.
- **`GetMaxVelocity = Sprint * (0.9 + statVelocity * 0.1)`, диапазон `[7.2, 8.0]` ПРИ статах `[0,1]`.**
  Оговорка обязательна: `GetStat` оригинала не клампит статы к `[0,1]` — при статах вне этого диапазона
  (модифицированные/баговые данные) диапазон max-скорости может выйти за `[7.2, 8.0]`. Порт статы тоже
  не клампит (bug-for-bug); диапазон в комментарии — для типичного случая статов `[0,1]`.

## Инварианты фазы 4 (мяч, смагглы, ReQueue, касание)

### Физика мяча — порядок секций внутри шага священен

`Ball.CalculatePrediction` (`ball.cpp:137-537`) одним проходом наполняет кэш траектории на 3 с вперёд
(300 точек с шагом 10 мс) **и** возвращает состояние на +10 мс; `Process()` берёт ровно `Predict(10)`.
Кэш пересчитывается целиком при каждом изменении состояния мяча и каждый тик — так в оригинале.
Порядок внутри шага: **гравитация → драг → grass-bias → отскок → трение газона → штанги → вращение от
качения и его обратное влияние на движение → магнус → интеграция**. Менять порядок нельзя: секции
читают результаты друг друга.

Что легко сломать «улучшением»:

- **Драг применяется к полному 3D-вектору**, не только к горизонтали (`ball.cpp:180-183`).
- **Два разных порога земли.** Отскок срабатывает при `z < 0.11`, трение газона — при
  `z < 0.11 + grassHeight`. Это не опечатка оригинала.
- **Линейная часть отскока вычитается ПОСЛЕ множителя** (`vz = -vz*0.62`, затем `max(vz-0.06, 0)`), а
  `frictionFactor` считается **до** перезаписи `vz` — то есть один раз на отскок.
- **Обратное влияние вращения берётся от СТАРОГО `rotationPredictMs`**, до присваивания нового.
- **Оси в блоке качения нарочно перепутаны** (x-скорость крутит вокруг y).
- **Пара `GetAngles` → масштаб → `SetAngles` НЕ взаимно-обратна** (перестановки осей C++ не гасятся) —
  асимметрия входит в интегрирование вращения bug-for-bug; кватернион шага умножается **слева**.
- **Радиус мяча 0.11 — литерал**, а не константа: в оригинале он захардкожен по всему `ball.cpp`.
- `Predict` при отрицательном аргументе даёт **последнюю** точку кэша (unsigned-каст оригинала), а кап
  истории сравнивает *число записей* с 4000 мс при записи раз в 10 мс — фактическая глубина истории 40 с,
  не 4. Оба квирка перенесены и запиннены тестами.

Сетка ворот (`ball.cpp:331-408`) сознательно **не портирована** — ею занимается собственный `NetSim`
проекта (см. [[сетка-ворот]]); вместе с ней выброшен только питавший её мёртвый код. Звук, привязки к
`Match`, `Put`/temporal smoothing и debug-телепорт мяча тоже вне порта.

### Отбор клипа идёт по НАСЛЕДНИКУ — и движение тоже

Фаза 4 перевела на наследника весь отбор, включая движение (до этого movement шёл по базовому
`AnimSelector` — это и был главный открытый вопрос фазы 3, теперь закрыт). Отдельного класса
`Humanoid` в порте нет: `Humanoid.cs` — партиальная часть того же `HumanoidBase`, потому что игроки в
оригинале инстанцируются как `Humanoid` (`player.cpp:88`). Базовый `AnimSelector.SelectMovementInternal`
остался **только для судей**.

Единая точка входа `SelectAnim(command, localInterruptAnim, preferPassAndShot)` (`humanoid.cpp:1159-1820`)
идёт по шагам: ранние выходы (мяч дальше 10 м через 200 мс → отказ) → отказные фильтры ReQueue →
`CalculateFactualSpatialState` → **crude query** (грубый булев фильтр) → **сорт-цепочка** → ветка по типу
→ quadrant-reject.

Что важно знать про crude query (`humanoid.cpp:1244-1365`):

- **`FnLongPass` подменяется на `FnShortPass`** — длинный пас использует клипы короткого («hax» оригинала).
- **Флаги строгости зависят от типа**: movement и ballcontrol → строгая входящая скорость; пасы/удар →
  запрет dribble→sprint без линейности; всё прочее (trap, interfere, trip, sliding) → полная линейность.
  Ballcontrol намеренно **нелинеен** по направлению корпуса, чтобы его клипы чаще годились как trap.
- **`bySide`-фильтр** включается только при `UseDesiredLookAt`, пустом `outgoing_special_state` и не-ретейнере.
  Внутри фильтра оригинал складывает enum-сторону (int) с радианами — перенесено дословно, не «починено».
- **`ByFoot = false`, но `FootId` всё равно заполняется** противоположной ногой — поле мёртвое; фильтра по
  ноге на этом пути нет, как и секции forced foot.
- Пустой результат для `FnMovement` дополняется idle-клипом; для остальных типов пусто == отказ.

Сорт-цепочка (`:1556-1637`) — **строгий порядок**, и только через `AnimSelector.StableSort`
(`List.Sort` запрещён как нестабильный): `priority` → `idlelevel` → похожесть ноги → похожесть входящего
направления корпуса (**кроме** ballcontrol) → похожесть входящей скорости → trip-направление →
baseanim (для всех НЕ-movement) → catch-or-deflect (только `FnDeflect`).

- **Двух попыток lenient→strict в порте нет** — живёт только strict. Lenient-проход оригинала
  (`:1383-1435`) — закомментированная диагностика, переносить нечего. Нестрогость выражена флагом
  `strict` в action-ветках: movement — строго; ballcontrol — строго, но нестрого при `AllowLastDitch`;
  trap — допуск `0.3π` и 2 шага скорости; interfere — нестрого по умолчанию.
- **Порядок двух `KeepBest*` внутри веток разный** bug-for-bug: в ballcontrol сначала корпус, потом
  направление; в movement/trap/interfere — наоборот.
- **Trap умеет отказаться** («too wrong»): если угол лучшего клипа больше `0.375π` или разница движений
  больше 5.5 / 8.5 м/с — клип не выбирается вовсе, ждём следующего кадра.
- `desiredIdleLevel` считается цепочкой `if/else if`, которая **перезаписывает** уровень «мяч не в игре»
  уровнем «мяч далеко» — перенесено как есть.

**`NeedTouch`** (`:1822-1857`) отвечает на «надо ли вообще трогать мяч»: стоим и хотим стоять — не надо.
Внутри — **скобочный баг оригинала**: сравнение выполнено *внутри* вызова `FloatToEnumVelocity(...)`,
из-за чего первое условие всегда false. Не чинится.

### `GetBestCheatableAnimID` — first-fit, а не поиск максимума

«Читабельный» клип — тот, у которого есть касание, чей идеальный кадр попадает в **деформированную
смаггл-зону** вокруг реальной позиции мяча. Ключевое: перебор идёт по уже отсортированному набору и
**первое попадание выигрывает** (двойной `while … && !found`) — это first-fit по порядку сортировки, а
не выбор лучшего по метрике.

- **Порядок перебора касаний клипа не по возрастанию**: сначала «среднее» касание и вниз до нулевого,
  затем среднее+1 и вверх. Порт выделяет `totalTouches + 1` слотов: при нуле касаний оригинал пишет за
  границу вектора (UB), запас делает это безвредным, не меняя поведения.
- **Гейт по высоте `ballDistanceZ < 0.22`** отсекает высокие мячи до всей остальной математики.
  Ослабляется высотой мяча, усиливается ×2 выше 1.8 м и ×20 выше 2.6 м.
- **Свежее касание жмёт радиус**: только что коснувшийся мяча читит меньше (окно 600 мс).
- **`GetBodyBallDistanceAdvantage` — по сути булев**: возвращает ровно 1.0 («дотягиваемся») или 0.0.
  Внутри — бонусы близости и низкой скорости, сдвиг зоны **назад по ходу** (`behindVector`) и
  деформация круга в эллипс: латеральный чит труднее на скорости, площадь при этом сохраняется.
  Ретейнер дотягивается всегда.
- Наружу отдаются id клипа, **кадр касания**, `touchPos = ball.Predict(кадр·10)`, полный и урезанный
  action-смаггл, rotation-смаггл и траектория.

### Смагглы — «чит» позиции, который не должен быть виден

Смаггл — сдвиг корня тела сверх траектории клипа: анимация играется как есть, а игрок дополнительно
доводится к мячу или к желаемому направлению. Три живых вида (плюс один мёртвый):

| Вид | Кто считает | Когда применяется |
|---|---|---|
| **action** (к мячу) | `GetBestCheatableAnimID` | только у тач-клипов, пока `FrameNum <= TouchFrame` |
| **movement** (к линии мяча) | `CalculateMovementSmuggle` (`humanoid.cpp:2326+`) | только у клипов БЕЗ касания |
| **rotation** (угловой) | `Begin` — наследник, `End` — выход `PhysicsVector.Calculate` | каждый кадр, лерп |
| ~~action sustain~~ | никто: блок `humanoid.cpp:1799-1814` закомментирован в оригинале | всегда нули, но входят в суммы дословно |

- **Накопление — это профиль скорости, а не затухание.** За кадр начисляется
  `(smuggle / (N+1)) * (cos(...) * 0.1 + 0.9)`: косинусный горб, приплюснутый до ±10 % («add some
  linearity» оригинала), интеграл за клип даёт полный смаггл. `N` — `TouchFrame` для action и
  `EffectiveFrameCount` для movement. Условия по `TouchFrame` **взаимоисключающие**: один кадр
  намеренно пропущен, иначе скорость смаггла повлияла бы на само касание (`humanoid.cpp:700`).
- **Rotation-смаггл идёт по блоку НАСЛЕДНИКА** (`humanoid.cpp:722-742`) с 16-кадровым капом ease-in:
  `Begin` затухает от полного к нулю, `End` нарастает. Незакапленный лерп базы гоняют только судьи.
  До касания `End` равен нулю (`allowPreTouchRotationSmuggle = false`).
- **Частичный сброс — «cheat discard»**: `FullActionSmuggle` сохраняется целиком (нужен touch-логике),
  а показываемый `ActionSmuggle` урезается цепочкой вычетов и множителей. Внутри неё —
  **подозрение на баг оригинала, которое НЕ чинится**: `min(..., |full| - 0.2)` при `|full| < 0.2` даёт
  отрицательную дистанцию, то есть смаггл разворачивается ОТ мяча (`humanoid.cpp:2267`).
- **Смагглы вычитаются из физического движения** в `CalculateSpatialState` (`:1644`) — чит не должен
  считаться «настоящей» скоростью игрока. Побочный эффект признан ещё оригиналом: вместе с читом
  игнорируются и офсеты столкновений.

### Apply-буфер — порядок наследника, `smoothFactor` по типу перехода

Порядок сложения — **наследника** (`humanoid.cpp:769`): `startPos + Positions[FrameNum] + actionSmuggle
+ actionSmuggleSustain + movementSmuggle`. База (`humanoidbase.cpp:703`) складывала смагглы ДО
`positions`; при нулевых смагглах фазы 3 это численно одно и то же, при живых — разошлось бы. Игроки
инстанцируются как `Humanoid`, поэтому выбран наследник; это закрывает открытый вопрос фазы 3.

Поля `smooth`/`smoothFactor` (заведены задачей 5) определяют сглаживание перехода в
`AnimationApplier.Apply`: `beginBias` живёт всего 8 кадров, вес слерпа = `beginBias * smoothFactor * 0.5`.

| Переход | `smoothFactor` |
|---|---|
| граница клипа, движение → движение | **0.0** (перехода не видно — так в оригинале) |
| shot / short pass / high pass / deflect / sliding / ballcontrol / trap | **0.8** |
| любой другой, включая mid-anim ReQueue | 1.0 |
| старт ситуации | 0.0, `smooth = false` |

**`LongPass` в списке 0.8 отсутствует** — так в оригинале (`humanoid.cpp:279-280` знает только
short/high), перенесено bug-for-bug. Поля `anim` в буфере нет (клип читается геттером), `offsets`
(`:780`) не портированы. При `smoothFactor == 0` слерпа нет, но ограничитель угловой скорости костей
(7.5π рад/с, локти/колени ×1.2, стопы ×1.6) работает всё равно.

### ReQueue — прерывание клипа раньше границы

До фазы 4 клип всегда доигрывался до конца, и перевыбор шёл раз в клип (~100–240 мс). Теперь тик может
прерваться посреди клипа. Ключевая асимметрия: **`Switch` на границе обязан выбрать хоть что-то, а
ReQueue — лишь «улучшение» и вправе отказаться.**

Контур (`humanoid.cpp:136-336`): маска частоты → пригодность текущего клипа → командная очередь →
отказные фильтры внутри `SelectAnim` → quadrant-reject → карантин.

- **Маска частоты** разрежает попытки по матчевому времени и разводит фазу по `teamId`, чтобы команды не
  считали в один миллисекундный слот. Период зависит от роли и дистанции до мяча: designated ближе 3 м —
  20 мс, designated — 30, team-designated — 40, ближе 5 м — 50, ближе 10 м — 80, дальше — не пытаться.
- **Командная очередь** — упорядоченный список «чего хочет контроллер»; тик берёт **первую применимую**.
  Флаг `preferPassAndShot`, раз взведённый, **не сбрасывается между итерациями** — как в оригинале.
- **Отказные фильтры** (только при `InterruptReQueue`): не-movement→movement запрещён;
  movement→movement — при владении, при `focusDistance > 12 м`, при менее 6 оставшихся кадров или
  непустом карантине; ballcontrol→ballcontrol — только в первые 8 кадров; ballcontrol→trap запрещён.
  Плюс два «слишком похоже на то, что уже делаем»: сравнение со **снимком** `OriginatingCommand`
  (снимок по значению через `Clone()` — фильтр сравнивается именно со снимком, не с текущей командой) и
  проверка импульса `OutgoingMovement − IncomingMovement` против желаемого.
- **Quadrant-reject** работает уже ПОСЛЕ выбора клипа: тот же тип и тот же `quadrant_id` → выбор
  отменяется.
- **`reQueueDelayFrames = 22` кадра (~220 мс)** — карантин, накладываемый только на **последующие**
  клипы того же типа: реквей movement→ballcontrol не должен мешать немедленному реквею в другой
  ballcontrol.
- `_interruptAnim` — **поле, а не локальная переменная тика**: прерывание может выставляться извне
  (Trip) и живёт до сброса в конце тика.

Bump-прерывание (`humanoid.cpp:129-134`) не «недоделка порта» — оно **закомментировано в самом
оригинале** («todo: work in progress») и перенесено комментарием; вдобавок мертво по второй причине:
`positionOffset` копится только коллизиями игроков, которых в ядре порта пока нет. Сторож «FLYING
PLAYERS» (`:338-340`) наоборот **портирован и работает**. Отклонение от оригинала одно и оно
задокументировано: на «RED ALERT» (пустая коллекция) порт печатает ошибку и перезапускает клип с кадра 0
вместо `exit(1)`.

### Исполнение касания — один кадр, и мяч получает НЕ импульс

Касание исполняется в `ExecuteTouchTick()` (`humanoid.cpp:342-646`), которое тик зовёт **после** сброса
`_interruptAnim` и сторожа FLYING PLAYERS, но **до** начисления action-смаггла.

- **Главный гейт — `TouchFrame == FrameNum`**: касание живёт ровно в одном кадре клипа, и номер этого
  кадра назначен ещё при выборе клипа (`GetBestCheatableAnimID`).
- **`bumpyRideBias`** — мера промаха: чем дальше мяч от расчётной точки касания (порог дотягивания
  0.4 м, у ретейнера 1.0), тем большая доля его **текущего** движения сохраняется. Дальше порога и при
  расхождении высоты больше 1 м касание просто не исполняется.
- **`Ball.Touch` — это не импульс, а прямая установка momentum**: предыдущая скорость мяча
  отбрасывается целиком, её остаток приходит только через `bumpyRideBias` и `ballMovementFactor`. Заодно
  мяч поднимается до `z >= 0.11` и пересчитывается весь 3-секундный кэш предсказаний.
- **Угловая скорость задаётся слерпом с разным весом по ветке**: trap 0.5, ballcontrol 0.6, пас 0.9,
  удар 0.7, deflect 0.2; подкат не подкручивает вовсе.
- **Тип касания — из anim-переменной `touch_bodypart`**: `foot`/`lowerleg` → «намеренно пробит ногой»,
  всё прочее → «намеренно, не ногой». **Отдельной формулы «головой» нет** — голова и грудь идут теми же
  ветками, что трап и ведение. Interfere/deflect/sliding жёстко регистрируются как «случайное касание».

**Мяч может улететь вперёд и БЕЗ тач-клипа** — это отдельная ветка `ExecuteTouchTick`, помеченная в
оригинале как EXPERIMENTAL (`humanoid.cpp:362-388`): при `TouchFrame == -1` (то есть на обычном
движенческом клипе) сработавший триггер controlled-коллизии пинает мяч **trap-вектором**, `×1.35` при
knock-on. Практическое следствие для приёмки: игрок, бегущий по movement-клипам, всё равно толкает мяч
перед собой, хотя ballcontrol не выбран ни разу — на глаз это читается как «мяч едет сам», а не как
касание. Триггер считается внутри тика по дистанциям до мяча (`:342-360`), поэтому живёт и без
межтельных коллизий оркестратора.

**Продюсеров touch-вектора четыре, и у паса своего нет**: `GetBallControlVector` (ведение),
`GetTrapVector` (приём), `GetShotVector` (удар), а пас строится прямо в тике как
`направление · 36 · (power + 0.3)` и затем прогоняется через `GetBestPossibleTouch`. Порядок вызовов
внутри `GetTrapVector` объявлен священным (сначала difficulty — единственный вызов RNG, потом
ballcontrol), а в `GetShotVector` зафиксирован порядок семи вызовов RNG: в C++ порядок вычисления
аргументов конструктора `Vector3(...)` не специфицирован, поэтому порт фиксирует его явно.

**`GetBestPossibleTouch` — не «выбор лучшего касания», а деградация желаемого паса** до максимально
возможного для данного клипа и игрока: кламп мощности, увод избытка силы в высоту и модель ошибки. Если
у клипа есть своё `balldirection`, вектор **сползает к «родному» направлению клипа** — для клипа это
самое лёгкое направление.

**`GetDifficultyFactors`** — модель ошибки: насколько далеко мяч отскочит от ног, насколько высоко и
сколько его текущего движения сохранится. Штрафы за толкучку, за быстрый мяч и за дальний мяч; **ровно
один вызов RNG** — `(1 - technical_ballcontrol·0.5) · Uniform(0.5, 1.0)` множит все три фактора.

**Коллизии мяч↔тело — приближение.** `BallBodyCollider.Check` (`match.cpp:1926-2045`) считает
**AABB по сегментам костей утилитарного скелета** (AABB сегмента = min/max его концов ± паддинг 0.12),
тогда как **оригинал берёт AABB геометрии узлов тела**; `GetDerivedPosition` геометрии заменён серединой
сегмента, `AABB::GetRadius` — полудиагональю. Сам тест «сфера против AABB» портирован дословно, включая
поведение при отрицательном радиусе. Радиус мяча в этом тесте намеренно «фейковый» — тюнинг-хак
оригинала: «больше — блоки веселее, меньше — меньше отскоков». Зовёт коллизии **не тик гуманоида, а
оркестратор** (в лабе — `BallLabMain`), потому что в оригинале они живут в `Match::Process`.

## Интероп-гочи GDScript ↔ C#

Мост `load("res://src/gpf/....cs").new()` из GDScript-тестов поймал несколько граблей за задачи 3–6
(общий список интеропа проекта — [[конвенции]], здесь только специфичное для порта):

- **`var x := ...` не выводит тип** из `Variant`-результата C#-метода — парсинг падает. Объявляй тип
  явно (`var mid: Quaternion = anim.SampleRotation(...)`).
- **Мост не переносит C#-дефолт-аргументы** (`default_args` на GDScript-стороне пуст) — `AnimationApplier
  .Apply(skel, anim, frame, offset, noPos: bool = false, baseRotZ: float = 0f, basePos: Vector3 =
  default)` из GDScript зовётся только полным списком из 7 аргументов, иначе parse error (фаза 2
  добавила 7-й параметр `basePos`).
- **`Gpf.Animation` может быть затенено `Godot.Animation`** при `using Godot;` — но только в
  C#-файлах ВНЕ `namespace Gpf` и его вложенных подпространств. Поиск типа в C# сначала обходит
  объемлющие неймспейсы изнутри наружу и лишь потом — using-директивы, поэтому внутри `Gpf`/`Gpf.Lab`
  (как `src/lab/LabMain.cs`) голое `Animation` и так резолвится в `Gpf.Animation` — риска нет
  (проверено минимальной репродукцией). Риск реален для будущего C#-кода в других слоях (например,
  gameplay) с `using Godot;` без `using Gpf;` — там пиши полное `Gpf.Animation`.

## `AnimCollection` и выбор клипа

Фаза 2 добавляет слой над сырыми клипами: загрузку **всей** библиотеки, автогенерацию промежуточных
вариаций и грубый+точный отбор клипа под текущее состояние тела и желаемую команду движения.

**Квадранты.** `AnimCollection` строит 34 квадранта на старте (не из файлов): 1 idle + 3 скорости
(dribble/walk/sprint) × 11 углов (`0°, ±20°, ±45°, ±90°, ±135°, ±179°`). Каждому загруженному клипу
`PrepareAnim` присваивает `quadrant_id` — ближайший квадрант по `outgoing movement` (`GetQuadrantID`,
`animcollection.cpp:865-880`).

**`Load` (`animcollection.cpp:354-487`).** Два независимых источника клипов, оба идут в одну плоскую
`List<Animation>`:
1. Все `.anim` вне `templates/`/`luxury` — по 2 раза (оригинал + `Mirror()`), файлы отсортированы
   `Ordinal` для детерминизма (в C++ порядок диктовала ФС).
2. 10 шаблонов из `templates/` → `GenerateAutoAnims` (`animcollection.cpp:162-352`) перебирает все пары
   шаблонов × 9 направлений (`10×10×9 = 900` кандидатов), режет нелегальные комбинации (макс.
   ускорение/торможение, угол поворота, суммарный разворот тела и т. д.) и bias-интерполирует
   оставшиеся в клип на 25 кадров. Каждый легальный автоген кладётся в коллекцию дважды: зеркало, затем
   оригинал.

После `Load` ищется первый клип `type=="movement"` с `incoming`/`outgoing` velocity `< 1.8` — это
`idleMovementAnimId`, фолбэк-стенд-клип (`humanoidbase.cpp:1416`), возвращается им же `SelectMovementAnim`
при пустом отборе.

**`CrudeSelection` (`animcollection.cpp:494-863`)** — один линейный проход по коллекции, ~12 независимых
секций-фильтров (`CrudeSelectionQuery`): тип функции, вход/выход скорости (строго или с матрицей
нестрогих запретов + линейность), сторона поворота (bug-for-bug `:619`, см. инварианты выше), подбор
мяча, last-ditch, направление тела на входе, направление мяча вход/выход, произвольные строковые
свойства, тип спотыкания, форсированная нога. Каждая секция может только выключить `selectAnim`, не
включить обратно — семантика чистого AND-фильтра оригинала.

**`AnimSelector` — movement-путь (`humanoidbase.cpp:1374-1496`).** `SelectMovementAnim` собирает
`CrudeSelectionQuery` по текущему состоянию (входящая скорость/направление тела, строго), затем цепочкой
`StableSort`/`Keep*` сужает список до одного клипа: `KeepBestDirectionAnims` (ближайший квадрант по
`GetMovementSimilarity`) → опционально `KeepBestBodyDirectionAnims` (если задан `desiredLookAt`) →
`StableSort` по близости `idlelevel` → по совпадению желаемой ноги → по близости входящего направления
тела → по близости входящей скорости. Итоговый `dataSet[0]` — выбранный клип; `SelectMovementDataSet`
отдаёт весь отсортированный список (для тестов/отладки).

**Этот базовый movement-путь с фазы 4 гоняют только судьи.** Игроки идут по цепочке наследника —
`SelectAnim` из `Humanoid.cs` с crude query, сорт-цепочкой и ветками ballcontrol/trap/pass/shot/
interfere/deflect/sliding поверх того же `CrudeSelection`. Подробности — «Инварианты фазы 4» выше. До
фазы 4 движение шло по базовому пути, и это было задокументированным расхождением с эталон-exe.

## Что НЕ портировано (после фазы 4)

- **`Slowdown`/`SmoothPositions`** — мертвы в самом оригинале: `_PrepareAnim` считает
  `expectedFrameCount` (`CalculateAnimDifficulty`), но вызов, который использовал бы его для растяжки
  клипа, закомментирован в C++ (`animcollection.cpp:1186`). Порт честно повторяет это — считает и
  выбрасывает результат (`_ = expectedFrameCount;` в `PrepareAnim`), функцию растяжки не пишет.
  Не «недоделка порта» — оригинал сам этого не делает.
- **Сетка ворот (`ball.cpp:331-408`)** — решение роадмапа: у проекта своя объёмная Verlet-сетка
  ([[сетка-ворот]]), физике мяча из порта она не нужна. Вместе с ней выброшен питавший только её мёртвый
  код. `ballTouchesNet` тоже не переносится.
- **`MentalImage` — задержка восприятия. Главное системное приближение фазы 4.** В оригинале игрок
  принимает решения по «мячу, каким он его помнит», через `currentMentalImage->GetBallPrediction(t)`;
  порт всюду берёт **объективный** `Ball.Predict(t)`. Расхождение с оригиналом ровно в этой задержке — но
  оно широкое: его видят ранние выходы `SelectAnim`, crude query по направлению прилёта, `NeedTouch`,
  `CalculateMovementSmuggle`, перебор касаний, `touchPos` и маска частоты ReQueue. Побочно из-за нулевой
  задержки **мертва** ветка «в памяти игрока мяч не там, где он есть»: обе стороны сравнения — один и тот
  же `Predict`.
- **AI-слой: `AI_GetPass` и `AI_GetShotDirection`.** Рефайн цели/направления паса и направления удара, а
  также выбор игрока-адресата (`targetPlayer`/`SelectPlayer`) — вне скоупа фазы; направление и сила
  берутся из `TouchInfo` как есть. У удара это безвредно из-за квирка оригинала: локальный
  `ballDirection` после рефайна дальше не читается, `GetShotVector` берёт `TouchInfo.DesiredDirection`
  напрямую.
- **Матч-слой целиком**: `Match::CalculatePossession` (настоящий `HasPossession`), владение команды,
  `UpdatePossessionStats`, `MatchData::AddShot`, `SetLastTouchTeamID`, ретейнер-состояние «чужой
  игрок», `Team::GetSide`, реальные `PlayerData`-статы вместо дефолта 0.6. В ядре это **швы** с
  лаб-суррогатами — полный список в [[открытые-вопросы]], потому что от них зависят и отбор клипа, и
  смаггл, и коллизии.
- **`TimeNeededToGetToBall`/`AllowLastDitch`** — считает ИИ (`AIfunctions`), которого нет; сеттеры с
  дефолтом 0 («уже у мяча») и `false`. Это напрямую меняет отбор: `AllowLastDitch` переключает
  строгость в ballcontrol/trap-ветках.
- **Superglue ретейнера (`humanoid.cpp:648-665`)** — приклейка мяча к телу вратаря требует позиций узлов
  скелета (`nodeMap`), которых в ядре нет; вратарский контур вообще вне фазы 4 (в лабе `canRetain`
  deflect-ветки выключен лаб-параметром, сама ветка портирована дословно).
- **Коллизии игрок↔игрок.** `PositionOffset`/`decayingPositionOffset` копятся только толкучкой, которой
  в ядре порта нет — поля тождественно нули, но **контур распада и все потребители живы** и заработают,
  когда появятся столкновения. По этой же причине мертво bump-прерывание клипа — а оно вдобавок
  закомментировано и в самом оригинале.
- **Звук** (`TriggerBallTouchSound`, удар о штангу), сцена/geometry, `Ball::Put`/temporal smoothing,
  debug-клавиши и debug-принты — вне порта по построению.
- **Остальные пути `SelectAnim`: `FnHeader` и `FnCatch`.** `CrudeSelection` фильтровать по ним умеет
  (`CheckFunctionType` знает все 14 типов), но собственных веток в тике у них нет — придут с
  вратарским контуром и игрой на втором этаже.
- **Ещё живущее «мёртвым» по построению**: `ActionSmuggleSustain` (блок закомментирован в оригинале),
  `offsets` apply-буфера, «метод 2» дискарда смаггла (deprecated в оригинале), lenient-проход отбора
  (закомментированная диагностика). Это не долги порта — это код, которого нет и в C++.

## Лаб-сцены: запуск и управление

**Видимое тело во всех трёх лабах — `Gpf.Lab.LabBody`** (`assets/models/gpf_fullbody.glb`, заглушка из
оригинала, см. [[презентация-и-ассеты]]). Клавиши общие: `M` — показать/скрыть меш (остаётся палочник),
`T` — родные текстуры / ровная серая заливка (по текстурам зазоры и силуэт читаются хуже, за форму
принимается рисунок). Нет файла модели — лаба работает палочником, как раньше.

### `anim_lab` — листалка клипов (фаза 1)

```powershell
& "C:\Users\User\Desktop\Godot_v4.7.1-stable_mono_win64\Godot_v4.7.1-stable_mono_win64_console.exe" `
  --path "C:\Users\User\Desktop\projects\OpenFootball" res://scenes/lab/anim_lab.tscn
```

`LabMain._Ready` сканирует `res://assets/gpf/animations` рекурсивно, собирает все `.anim` (293 клипа) и
сразу включает первый по алфавиту. Управление:

| Клавиша | Действие |
|---|---|
| `→` / `←` | следующий / предыдущий клип |
| `Space` | пауза |
| `R` | сначала |
| `M` / `T` | меш/кости · текстуры/заливка (см. выше) |

HUD (`CanvasLayer`) печатает имя клипа, `type`, `frameCount`, текущий кадр и кадры касаний мяча.
Оранжевые полупрозрачные сферы — маркеры позиций касаний из `extension,football` в пространстве клипа.
Палочник: левая сторона красная, правая синяя (`StickmanRenderer.BoneColor`) — быстрый визуальный тест
на зеркало осей при ревью.

Дисциплина мультиплеера с первого дня порта — фикс-тик **100 Гц**. Лабы ставят его из кода
(`LabMain._Ready`/`WalkLabMain._Ready`: `Engine.PhysicsTicksPerSecond = 100`), а с фазы 4 (задача 9)
он стоит **глобально** в `project.godot` — значит и матч. Наследие GDScript-геймплея, затюненного при
60 Гц, приведено к тику через `TickScale` (драг мяча, `MAGNUS_DECAY`, `DRIBBLE_ROLL_DRAG`,
`DRIBBLE_CONTROL_LERP`, `KEEPER_DIVE_DECAY`; предохранитель подтверждения гола переведён в секунды) —
см. [[константы]]. Приёмка ощущения после перевода — за человеком, [[открытые-вопросы]].

### `walk_lab` — палочник бегает по командам (варпнутые траектории, фаза 3)

```powershell
& "C:\Users\User\Desktop\Godot_v4.7.1-stable_mono_win64\Godot_v4.7.1-stable_mono_win64_console.exe" `
  --path "C:\Users\User\Desktop\projects\OpenFootball" res://scenes/lab/walk_lab.tscn
```

`WalkLabMain._Ready` грузит всю `AnimCollection` (1452 клипа, ~0.8 с) и заводит `Gpf.HumanoidBase`
(`ResetSituation` — старт на `idleMovementAnimId`, кадр 0). Клавиши задают **команду** (направление +
желаемая скорость), а не прямое управление — на каждой границе клипа `HumanoidBase.Tick` сам подбирает
следующий клип (`SelectNextMovementAnim` → `AnimSelector`) и варпит его траекторию под текущую физику
(`PhysicsVector.Calculate`); скорость доезжает до команды непрерывно, не бакетами (idle→dribble→walk→
sprint по-прежнему максимум +1 уровень скорости клипа за смену, как в оригинале):

| Клавиша | Действие |
|---|---|
| `↑` / `↓` / `←` / `→` | направление команды; **две стрелки сразу — диагональ** (45° / 135°) |
| `0` / `1` / `2` / `3` | желаемая скорость: idle / dribble / walk / sprint |
| `A` | циклит пресет статов (все шесть статов разом): 0.3 → 0.6 → 0.9 → 0.3…, дефолт 0.6 |
| `M` / `T` | меш/кости · текстуры/заливка (см. выше) |

**Медленной ходьбы в датасете нет** — три движущиеся походки GameplayFootball это трусца/бег/спринт
(`dribble` 3.5, `walk` 5.0, `sprint` 8.0 м/с; даже самая медленная — бег с мячом ~12,6 км/ч), поэтому
`1/2/3` дают бег с тремя скоростями, а не «шаг → бег». HUD печатает непрерывную скорость `v=` в м/с (не
бакет — физика варпинга даёт промежуточные значения между дескрипторами клипов, в отличие от lite-версии
фазы 2).

Направление читается **опросом зажатых стрелок** каждый физкадр (`PollDirectionInput`), а не дискретным
событием — так набираются диагонали, и `←`/`→` соответствуют экрану (клавиша `←` = «их-влево» `(1,0,0)`,
которое базис `GpfSpace` зеркалит в экранный «влево»; прямое присваивание `Left→(-1,0,0)` уводило бы
персонажа вправо). Скорость и пресет статов — дискретно в `_UnhandledKeyInput`. HUD печатает имя
текущего клипа, `quadrant_id`, номер кадра, состояние (скорость в м/с, угол), саму команду и активный
пресет статов. Жёлтая стрелка над палочником — визуализация команды. Ввод — не `InputMap`: сцена не
участвует в `_setup_inputs()` матча, конфликтов с игровыми биндингами нет.

Интеграция состояния между клипами — **настоящая**: `WalkLabMain` не хранит и не вычисляет позицию/
угол/скорость сама, всё отдаёт `Gpf.HumanoidBase` через apply-буфер тика (`humanoidbase.cpp:700-711`);
lite-дескрипторное приближение фазы 2 удалено. См. «Инварианты фазы 3» выше.

### `ball_lab` — игрок и мяч на ядре порта (фаза 4)

```powershell
& "C:\Users\User\Desktop\Godot_v4.7.1-stable_mono_win64\Godot_v4.7.1-stable_mono_win64_console.exe" `
  --path "C:\Users\User\Desktop\projects\OpenFootball" res://scenes/lab/ball_lab.tscn
```

Сцена глазной приёмки фазы 4: `Gpf.HumanoidBase` + `Gpf.Ball` на настоящем ядре, `walk_lab` фазы 3 при
этом не тронут. `BallLabMain` — **оркестратор одного тика**, повторяющий порядок `Match::Process`:
сборка очереди команд → `Humanoid.Tick` (внутри — исполнение касания) → `CheckBallCollisions`
(`match.cpp:1926-2045`) → `Ball.Process()` → отрисовка. Сид ядра зафиксирован (`RngSeed = 20260731`) и
печатается в HUD — прогон детерминирован. Старт: игрок в нуле на idle, мяч в `(0,-2,0)`, ворот нет
(`WoodworkEnabled = false`), игрок помечен designated possession.

| Клавиша | Действие |
|---|---|
| `↑` / `↓` / `←` / `→` | направление команды; опрос **зажатых** клавиш каждый физкадр, сумма векторов даёт диагонали (как в `walk_lab`) |
| `0` / `1` / `2` / `3` | желаемая скорость: стойка / дриблинг / бег / спринт |
| `W` (держать) | короткий пас: заряд `gauge` +10 мс за тик (кламп 10…1000), исполнение по отпусканию |
| `S` (держать) | удар: то же, `FnShot` |
| `R` | сброс ситуации (игрок в ноль, мяч на место, буфер действия и счётчик касаний) |
| `M` / `T` | меш/кости · текстуры/заливка — здесь важнее всего: зазор нога-мяч на палочнике не оценить |

Буфер действия — порт `humancontroller.cpp:41-49,97-197,428-459`: исполнить, если кнопка отпущена,
ИЛИ заряд перевалил 500 мс, ИЛИ владения нет и буфер уже ждёт; отмена по таймеру 2000 мс. Очередь
команд собирается в порядке оригинала (`humancontroller.cpp:97-330`): пас/удар из буфера, затем
`FnBallControl`, **движение последним** — тик берёт первую применимую (`humanoid.cpp:229-247`).
`FnBallControl` ставится безусловно, «надо ли трогать мяч» решает `NeedTouch` внутри отбора. Ввод — не
`InputMap`, конфликтов с матчем нет. Для headless есть программный вход: `SetCommand`,
`RequestAction(functionType, gaugeMs)`, `StepOneFrame()` и геттеры состояния — на них стоит
`check_gpf_ball_walker.gd`.

Маркеры и HUD: жёлтая стрелка — команда; белый мяч радиуса 0.11 — позиция И ориентация из буферов
`Ball` (аналог `Ball::Put`); **жёлтая сфера `touchPos`** — куда выбранный тач-клип собирается поставить
касание, видна пока `TouchPending` и гаснет в кадр исполнения. HUD печатает клип + function type,
`frame/frameCount` и `touchFrame`, `|actionSmuggle|`/`|movementSmuggle|`, скорость и угол игрока,
скорость и `z` мяча, дистанцию игрок-мяч, счётчик касаний с типом последнего и дистанцией нога-мяч в
кадр контакта, состояние буфера действия, команду и сид.

⚠️ **Строка «мяч↔расчётная точка» в консоли и поле HUD `нога-мяч` НЕ измеряют дистанцию до ноги.**
Считается `|ball − touchPos|`, а сам `touchPos` получен как `ball.Predict(touchFrame·10)` — то есть это
проверка «мяч пришёл туда, где его предсказали» (гейт `humanoid.cpp:398`), и в стабильном ведении она
всегда ~0.00–0.03 м. Реальный зазор от мяча до костей ноги она не видит **по построению**: у
утилитарного скелета нет ступни, а точка касания в данных клипа лежит в ~0.2 м от голеностопа. Замеры
зазора — см. [[открытые-вопросы]], там же измеренные значения.

Что этим смотрится на глаз:

- **Смаггл** — встать в стойку, задать `↑` со скоростью `2`/`3`: на подбегании выбирается тач-клип,
  `|actionSmuggle|` в HUD нарастает, и видно, что нога попадает по мячу доводкой, а не телепортом
  позиций. Movement-смаггл живёт только когда мяч рядом (гейты `humanoid.cpp:2330-2332`).
- **Касание** — `touchPos` показывает цель, гаснет в кадр исполнения; рядом фактический промах/попадание.
- **Ведение** — держать `↑` на скорости `1`/`2`: серия касаний, дистанция игрок-мяч остаётся малой.
  Смена `1`→`3` меняет вынос мяча вперёд (`GetFrontOfFootOffsetRel` зависит от скорости).
- **ReQueue** — резко сменить направление посреди клипа: имя клипа в HUD меняется, а `frame`
  сбрасывается, не дойдя до `frameCount`. Единственный игрок лабы — designated possession player,
  поэтому маска частоты ReQueue (`humanoid.cpp:165-169`) пропускает часто.
- **Пас/удар по направлению** — заряд `W`/`S`, отпускание; разный заряд даёт разную мощность, спринт
  добавляет модификатор скорости.
- **Физика мяча** — отскок, затухание, трение газона и магнус (меш крутится от ядра). Ворот нет.
- **Межтельная коллизия** — пробежать сквозь мяч на спринте: в консоли «коллизия тело-мяч». Ветка
  «отбор корпусом» не срабатывает: соперников в лабе нет — это поведение оригинала (гейт
  `match.cpp:1953`), не дыра порта.

**Лаба — не матч, и часть её входов суррогатна.** Соперников нет → биас последнего касания соперника 0
и `unexpectedDistance = 0` (задержки восприятия/`MentalImage` в лабе нет); `HasPossession` подменён
лаб-предикатом «мяч ближе 1.6 м по земле и ниже 1 м»; `TimeNeededToGetToBall` и
`GetDesiredTimeToBall_ms` — сеттеры с дефолтом 0 («уже у мяча»), настоящие считает ИИ;
`AI_GetPass`/`AI_GetShotDirection` не портированы, поэтому желаемое направление равно введённому;
`AllowLastDitch = false`, владение команды = 1.0, `teamId = 0`, стандартов и вратарского контура нет.
Полный список швов — в [[открытые-вопросы]].

## Тесты

Двадцать пять headless-скриптов `tests/check_gpf_*.gd` (запуск поштучно — см. CLAUDE.md, «Один
check-скрипт»); все зелёные на фикс-тике 100 Гц. Первые пять — фундамент фазы 1 (парсинг/
сэмплирование/метаданные/скелет/применение), следующие восемь — фаза 2, четыре — фаза 3 (варпинг
траектории), последние восемь — фаза 4:

- `check_gpf_anim_parse.gd` — CSV-парсинг: порядок треков, разрежённые ключи, `frameCount`, сброс
  состояния при повторном `LoadFromFile` на одном экземпляре, смоук по всему корпусу (293 файла — везде
  ровно 14 треков), негативы (несуществующий путь, битый файл).
- `check_gpf_anim_sample.gd` — интерполяция: точный ключ, lerp позиции корня, slerp между ключами,
  субкадровый lerp+normalize, сентинел `timeOffset=-1`, края (`frame<=0`, экстраполяция за концом клипа,
  трек, кончившийся раньше `frameCount`).
- `check_gpf_anim_meta.gd` — метаданные: касания (кадр+позиция, сортировка по кадру внутри одной
  extension-строки с несколькими касаниями в обратном файловом порядке — сценарий нескольких
  отдельных extension-строк в одном файле тестом не покрыт, по коду там last-wins, см. «Формат
  `.anim`» выше), сброс касаний при повторной загрузке (пиннится тестом), обновление
  `GetAnimType()`/`GetVariable()` при повторной загрузке (пиннится только перезапись `type`; сброс
  протухшего тега вроде `baseanim` не проверен), выход за границы (`GetTouchFrame`/`GetTouchPosition`
  вне диапазона), битый XML-хвост → `false`.
- `check_gpf_skeleton.gd` — 14 костей, иерархия родителей, рест-позы 1:1 `player.object`, базис
  `GpfSpace` (det=+1, «их-вперёд»/«их-верх» → ожидаемые Godot-оси).
- `check_gpf_apply.gd` — `Apply()`: ротация джойнта == ключ клипа, позиция корня, глобальная поза
  (рест-смещение не искажено), `noPos` зануляет X/Y корня, `baseRotZ` доворачивает `body` и (фаза 2)
  доворачивает позицию корня через `Rotate2D`, `basePos` смещает корень аддитивно.
- `check_gpf_bluntmath.gd` — `BluntMath`/`Velo`/эйлеры `QuatUtil`: `Curve`/`ModulateIntoRange`/
  `FixAngle`/`GetAngle2D` (обе перегрузки, включая знак)/`GetNormalized` (по-осевой эпсилон, не по
  длине)/скоростные конверсии/`GetAngles`/`AngleAxis`-круговая проверка.
- `check_gpf_anim_write.gd` — write-API: `Clone` (глубина — мутация клона не трогает оригинал), `SetKeyFrame`
  (рост `frameCount`), `Shift` (±1, drop-правило на границе, синхронный сдвиг касаний), `SetVariable`/
  `SetName`, `SetCurrentFootId`.
- `check_gpf_anim_descriptors.gd` — 12 ленивых дескрипторов на эталонном `movement/walk/045.anim`:
  опорные числа выведены из сырых ключей файла по формулам `animation.cpp:789-1052` (квантование
  скорости, углы, направления тела/движения, нога).
- `check_gpf_anim_mirror.gd` — `Mirror` как инволюция (двойное зеркало == оригинал), смена ноги,
  зеркало касаний, `ConvertToStartFacingForwardIfIdle`, нормализация direction-тегов при загрузке (оба
  формата строки).
- `check_gpf_collection.gd` — каркас `AnimCollection`: 34 квадранта, файловая загрузка ×2 (`566 =
  283×2`), автогены (размер `>566`, чётность `(total-566)`, `priority=1`/`type=="movement"`/25 кадров,
  `autogen_count == total-566`, `GetAutoAnimVelocityMismatchCount()==0`) — на живой библиотеке
  фактический размер коллекции **1452** (566 файловых + 2×443 легальных автогена).
- `check_gpf_crude.gd` — `CrudeSelection`: свойства-фильтры перепроверяются на живой коллекции
  повторной валидацией каждого результата (тип, скорости, сторона, свойства, forced foot и т. д.).
- `check_gpf_selector.gd` — `AnimSelector`: детерминизм `StableSort`, `ForceInto*`-таблицы,
  `CalculateBiasForFastCornering`, осмысленность выбранных клипов (idle→walk, смена направления).
- `check_gpf_walker.gd` — поведенческий тест `walk_lab` целиком на настоящей интеграции
  `Gpf.HumanoidBase`/`AnimCollection`/`AnimSelector` (не моках): разгон idle→dribble→walk→sprint,
  непрерывность float-скорости (физика варпинга даёт промежуточные значения между бакетами — было
  наоборот у lite-версии фазы 2), поворот вправо по команде, торможение по стоп-команде.
- `check_gpf_physics_foundation.gd` — примитивы `Vector3` (`NormalizeMax`/`GetNormalizedTo`/
  `GetNormalizedMax`, `vector3.cpp:190-228`) и кэш позиций корня `AnimCollection` (`match.cpp:86-105`)
  на эталонном `movement/walk/045.anim` — опорные числа из сырых ключей файла.
- `check_gpf_physics_helpers.gd` — хелперы `CalculateMovementAtFrame`/`StretchSprintTo`/`GetMaxVelocity`
  (`humanoid_utils.cpp:68-100,129-144`, `playerbase.cpp:131-139`) на синтетических позициях, все
  ожидания перевычислены по формулам C++ прямо в тесте.
- `check_gpf_physics_vector.gd` — `PhysicsVector.Calculate` == `CalculatePhysicsVector`
  (`humanoidbase.cpp:2014-2544`) на живой `AnimCollection`: инварианты и перевычисления формул на
  выходах (растяжка спринта, поворот к желаемому углу, сопротивление воздуха, квантование последних
  кадров).
- `check_gpf_humanoid_state.gd` — `HumanoidBase.Tick`/`CalculateSpatialState`/
  `CalculateFactualSpatialState` (`humanoidbase.cpp:585-711,1622-1737`): жёсткие ассерты первого тика из
  сырого кэша idle-клипа, порядок apply-буфера, смена клипа на границе.

Восемь тестов фазы 4:

- `check_gpf_ball_foundation.gd` — кватернионные примитивы, `Line`, `GpfRng` и константы поля
  **bug-for-bug**: `SetAngles` НЕ обратна `GetAngles` на смешанном вращении (запиннено намеренно —
  «починить» до настоящей обратной нельзя), `GetInverse` делит на `|q|`, а не на `|q|²`,
  `LineClosestToPoint` строго 2D и без клампа, RNG детерминирован по сиду.
- `check_gpf_ball.gd` — `Gpf.Ball` == `Ball::CalculatePrediction`: один шаг интеграции 10 мс,
  перевычисленный по формулам C++ **в самом тесте**, совпадает с `Predict(10)` — этим запиннен весь
  порядок применения констант. Плюс покой на `z = 0.11`, затухание отскока, `Process()` берёт ровно
  `Predict(10)`, магнус уводит закрученный мяч, точное отражение от штанги без пролёта насквозь,
  детерминизм и unsigned-квирк `Predict(<0) == Predict(2990)`.
- `check_gpf_touch_select.gd` — `SelectAnim` наследника и `NeedTouch`: ballcontrol-отбор непуст и
  типо-чист, сортировка повторяема и уважает первый ключ `priority`, long-pass hax даёт тот же отбор,
  что short pass, `NeedTouch` false на стоячем мяче при idle-желании и true на быстром.
- `check_gpf_cheatable.gd` — `GetBestCheatableAnimID` + advantage + `GetFrontOfFootOffsetRel`: FFO равен
  перевычисленной формуле, `touchPos == ball.Predict(touchFrame·10)` с точностью 1e-5, высокий мяч не
  берётся низким тачем (гейт 0.22), мяч в 5 м отвергается радиусом, ретейнер обходит advantage.
- `check_gpf_smuggle_apply.gd` — накопление смаггла равно косинусному ease-разложению
  (`humanoid.cpp:682-691`), перевычисленному в тесте; смаггл реально входит в позицию apply-буфера;
  `smoothFactor` тач-клипа = 0.8; movement-смаггл строго нулевой при далёком мяче.
- `check_gpf_requeue.gd` — **поведенческие** инварианты ReQueue (пороги внутри ядра тест намеренно не
  трогает): резкая смена команды перевыбирает клип раньше границы и поднимает счётчик, неизменная
  команда при далёком мяче не даёт ни одного ReQueue, контур детерминирован по последовательности id.
- `check_gpf_touch_vectors.gd` — факторы сложности ~0 в покое, штраф расстояния равен перевычисленной
  формуле на фиксированном сиде, `GetShotVector` детерминирован и направлен куда просили,
  `BallBodyCollider` регистрирует случайное касание не разгоняя мяч, ставит кулдаун 150 мс и уходит в
  ветку контролируемой коллизии для designated-игрока.
- `check_gpf_ball_walker.gd` — сквозной прогон `ball_lab` в headless: игрок добегает до мяча и касается
  его (momentum меняется именно в кадр касания), ведение удерживается, прогон на фикс-сиде
  детерминирован по позициям игрока и мяча.

Границы этих тестов честно очерчены в их же шапках: `check_gpf_ball_walker.gd` **не проверяет смаггл**
(нулевой смаггл там допустим — настоящая проверка только в `check_gpf_smuggle_apply.gd`), критерий
ведения намеренно грубый, а `check_gpf_requeue.gd` терпит несработавший ReQueue с первой попытки. В
`check_gpf_cheatable.gd` кейс «высокий мяч» условный: отсутствие подходящего клипа тоже считается
прохождением.

Чистая математика порта (`BluntMath`, `Velo`, `QuatUtil`, дескрипторы `Animation`, `PhysicsVector`)
намеренно **не читает `FootballConstants`** — тот же инвариант, что у `PassSystem`/`KeeperLogic` и
остального проекта. См. [[архитектура]].

## Ссылки

[[архитектура]], [[конвенции]], [[открытые-вопросы]]

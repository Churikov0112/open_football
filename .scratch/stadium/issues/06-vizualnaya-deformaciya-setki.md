# 06 — Визуальная деформация сетки

**What to build:** гол в лабе выглядит как гол — сетка ловит мяч и колышется. Физика сетки из
тикета 05 проверяется глазами до трассы фазы 8.

Портируется как презентация лабы: сбор вершин с `|x| > pitchHalfW + 0.06` и подтягивание их к мячу
с falloff (`match.cpp:2140-2215`). Вход — флаг касания сетки из физики `Gpf.Ball`, а не собственный
расчёт столкновений.

**Blocked by:** 05

**Status:** ready-for-agent

- [ ] Забитый мяч видимо деформирует сетку и она возвращается в форму
- [ ] Деформация питается флагом касания сетки из физики; своей коллизии у презентации нет
- [ ] Код деформации живёт в слое лабы, не в `Gpf.*`
- [ ] Мяч снаружи по-прежнему проходит сквозь сетку и её не деформирует
- [ ] `check_gpf_stadium.gd` и `check_gpf_netting.gd` зелёные; `dotnet build` без предупреждений

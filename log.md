# log.md — хронология проекта

Append-only журнал по методу Карпатого (LLM Wiki): **что произошло и когда** — вехи, крупные
фичи, отвергнутые подходы, решения по направлению. Дополняет вики: `docs/wiki/` отвечает на
«как устроено сейчас», лог — на «как мы сюда пришли». Записи только **добавляются в конец**,
никогда не редактируются и не удаляются (опечатки — новой записью-поправкой).

Формат записи (парсится unix-утилитами, `grep "^## \[" log.md | tail -5` — последние 5 событий):

```
## [YYYY-MM-DD] тип | краткое описание
```

Типы: `feat` (новая механика), `fix`, `refactor`, `docs`, `decision` (выбор направления, отказ
от идеи), `lint` (сверка вики/констант), `session` (итог рабочей сессии, если не покрыт другими
типами).

Затравка ниже восстановлена 2026-07-27 из git-истории при заведении вики — даты по коммитам,
события укрупнены до вех. До этой даты хронология велась только в git.

---

## [2026-07-06] feat | Первый прототип: дриблинг, мяч, боковая камера
Дриблинг переведён с spring-force на velocity-matching (позже вытеснен lead-follow, см. ниже).

## [2026-07-07] feat | ИИ-соперник (simple_ai)
Погоня за мячом/целью, ведение к воротам, удар.

## [2026-07-08] feat | Гибридная презентация: реальные Mixamo-модели вместо капсул
`PlayerVisual` + `apply_appearance` (тинт кита), обе команды на риггованных моделях (team_1 синий,
team_2 красный). Пайплайн сборки `footballer.glb` из FBX через Blender. Спека 3D-ассетов. См.
[[презентация-и-ассеты]].

## [2026-07-08] feat | Слайд-подкат и система падения (v1)
`move_and_collide`-скольжение, коллизия игроков, сбивание с ног. Два раздельных радиуса попадания
(мяч / игрок). Спеки slide-tackle + physical-tackle. См. [[подкат-и-падение]].

## [2026-07-09] feat | Тайминг «мяч↔действие»: контакт по сигналу анимации
`PlayerVisual` оборачивает StateMachine в BlendTree+TimeScale, шлёт `action_contact`/`action_finished`;
импульс мяча запускается по контакту клипа, не по кадру нажатия. Фундамент commit-action системы.

## [2026-07-09] feat | Living locomotion: PlayerMotor (velocity+inertia)
Движение переведено с прямого сдвига позиции на `velocity` + `move_and_slide()` через компонент
`PlayerMotor` (разгон/торможение, доворот, крен, спринт, раздельные слои коллизий). Рецепт из
OpenSoccer. Спека + план living-locomotion. См. [[локомоция]].

## [2026-07-09] feat | Удар с зарядом + отдельный клип kick
Charge-as-timer для удара, отдельная анимация kick.fbx (позже kick временно делит клип `pass`).

## [2026-07-10] decision | Падение: физ-ragdoll построен и выброшен в пользу анимации
`RagdollSkeleton` (`PhysicalBone3D`) собран и заработал, но не подружился с масштабированным Mixamo-
импортом → заменён на анимационную цепочку падения (knockdown → 2 переката → вставание). Файлы
ragdoll и их тесты остались как мёртвый код. См. [[подкат-и-падение]].

## [2026-07-11] feat | Ball model Phase 0: импульсный трап + lead-follow дриблинг (из OpenSoccer)
Машина состояний `BallState` (OPEN/TRAPPED/FLIGHT/CAUGHT), непрерывное удержание мяча впереди
(дистанция растёт со скоростью), break-away + догон, коллизия мяча с игроками только в FLIGHT,
ручной отскок от газона. Вытеснил velocity-matching/spring. См. [[мяч-и-дриблинг]].

## [2026-07-11] feat | Система паса: пять типов + чистый PassSystem
Короткий/на ход/навес/стенка/верховой на ход, общий заряд, авто-наводка/упреждение/разброс,
receive-assist, give-and-go, честный перехват. Математика — чистые статические функции
`PassSystem`. Спека + 19-задачный план. См. [[пас]].

## [2026-07-11] feat | Система удара: настильный/черпачок/кручёный + непрерывный прицел
`ShotSystem`: короткий тап низом, chip (баллистика), curl (Magnus), непрерывный горизонтальный
прицел (центр → дальняя девятка по стику). Плюс очередь «в одно касание». См. [[удар]].

## [2026-07-12] feat | Ворота с объёмной сеткой на Verlet-симуляции
Коробчатая сетка, физическое колыхание при попадании, стопперы-коллайдеры, 5-сек празднование,
правило гола. Чистая математика — `NetSim`. Спека + план + 4 раунда тюнинга. См. [[сетка-ворот]].

## [2026-07-13] feat | Вратарь v1 (ИИ-сейвер)
Позиционирование, drag-aware перехват траектории, центральная ловля / нырок / перевод. Математика —
`KeeperLogic`. Спека + план. См. [[вратарь]].

## [2026-07-15] feat | Пенальти (Фаза A)
`PenaltyController` + `PenaltyLogic`: кольцо-прицел, заряд силы, прямой/черпачок, слепой нырок
вратаря, root-motion разбег. Клавиша P. См. [[пенальти]].

## [2026-07-17] feat | Штрафной (Фаза A)
`FreeKickController` + `FreeKickLogic` (общий математический хаб стандартов): прицел+curl, стенка
с прыжком, расчистка конуса, пас/навес. Клавиша F. См. [[штрафной]].

## [2026-07-19] feat | Фабрика игроков + ростер (Фаза 1)
`PlayerFactory.spawn` — единственный шов спавна; `Team`/`PlayerConfig`. Именованные синглтоны
`player_home`/`teammate`/`away` удалены. См. [[фабрика-игроков]].

## [2026-07-20] refactor | ИИ как компонент-мозги (Фаза 2)
`simple_ai`/`teammate_ai`/`keeper_ai` теперь `extends Brain` (дочерний узел тела, не `set_script`).
`MatchManager._ai_of()` — единый мост. См. [[фабрика-игроков]], [[архитектура]].

## [2026-07-21] feat | Угловой и удар от ворот (Фаза A)
`CornerController`/`CornerLogic` (клавиша C, навес head/standard/свеча, короткая опция) и
`GoalKickController`/`GoalKickLogic` (клавиша G, бьёт вратарь, две расчистки). См. [[угловой]],
[[ввод-от-ворот]].

## [2026-07-21] refactor | ActionExecutor (Фаза 3a)
Резолвинг удара/паса вынесен из `match_manager` в компаньон-узел `ActionExecutor`
(`fire_shot`/`fire_pass`, deferred-impulse). См. [[пас]].

## [2026-07-22] feat | Вбрасывание из аута (Фаза A) + роадмап оркестрации матча
`ThrowInController`/`ThrowInLogic` (клавиша T, мяч в руках CAUGHT, правило 2 м). Спека роадмапа
оркестрации матча (какой актор какой рестарт получает). См. [[вбрасывание]].

## [2026-07-23] feat | Per-actor intent-шов: пилот на пенальти + фундамент судьи
`KickerIntent`/`HumanKickerIntent`/`SetPiecePresentation` — источник ввода отделён от контроллера.
Начат `MatchReferee` (edge-detection рестартов). См. [[стандарты-и-intent-seam]].

## [2026-07-24] refactor | Intent-шов на остальные 4 контроллера + нырок/шаги вратаря на пенальти
Штрафной/угловой/ввод-от-ворот/вброс переведены на шов (поведение человека без изменений).
`KeeperIntent` + K-режим (ИИ бьёт, человек — вратарь), приставные шаги по линии. См.
[[стандарты-и-intent-seam]], [[пенальти]], [[вратарь]].

## [2026-07-25] feat | Начальный удар (kickoff) — с диспетчеризацией судьёй
`KickoffController`/`KickoffLogic` с первого дня на intent-шве; `MatchReferee._dispatch_kickoff`
на старте матча (жеребьёвка) и после гола. Первый стандарт с реальным триггером, не только дебаг-
клавишей. Добавлен второй полевой team_2 (открытая игра 2v2 + вратари). См. [[начальный-удар]].

## [2026-07-26] feat | Второй вратарь + обобщение синглтона _keeper
Оба голкипера спавнятся; freeze/подбор/сброс/контакт — по группе `role_gk`. Синглтон `_keeper`
удалён, контроллеры резолвят вратаря в `start()` через `_keeper_at()`. См. [[вратарь]],
[[архитектура]].

## [2026-07-27] feat | Управляемый вратарь: HANDS / OUTFIELD / RETURNING + правило бэк-паса
На ловле человек берёт управление вратарём (руки: бросок A / вынос X / направленный B / дроп Y,
правило 6 секунд), дроп → игра как полевой → возврат домой. Правило «мяч своих» (пас/скидка назад
берётся в ноги, не в руки; пас вратарю не отдаёт управление, пока мяч не у ног). План 1 (шов
`KeeperHandsIntent` + `KeeperPlayLogic`) + План 2 (исполнение). См. [[вратарь]].

## [2026-07-27] feat | Правило гола «весь мяч за линией» (Law 10) + пенальти в наши ворота (Alt+K)
Гол подтверждается опросом каждый физкадр — только когда центр мяча за линией на радиус; отмена
при сейве/отбое на линии. `keeper_dive_debug` (Alt+K): пенальти в team_1-ворота, соперник бьёт,
человек играет за нашего вратаря. См. [[сетка-ворот]], [[пенальти]].

## [2026-07-27] docs | Заведена живая вики (docs/wiki/) + этот log.md
Полный перенос CLAUDE.md в вики (страница на подсистему, перекрёстные `[[ссылки]]`, `index.md`,
`константы.md` сверена с кодом). CLAUDE.md ужат до трип-вайров. Хуки на Python (`python3` тут —
сломанный алиас): session-start TOC, per-edit подсказка страницы, stop-check «код изменился —
вики нет». Скилл `wiki-lint`. Перенято из проекта signs-recognizer.

## [2026-07-28] feat | ИИ-удар от ворот + реальный запуск судьёй (Этап 2)
`GoalKickPlan` (скорер: гейт дуги / коридор `interception_time` / продвижение) + `AIGoalKickIntent`
(directional-playback). Первый стандарт через подключённый `restart_awarded` (`_on_restart_awarded`
→ `_dispatch_goal_kick`); судья не делает интерим для GOAL_KICK. Law 16 (соперник вне штрафной) —
непрерывно до удара, а не одноразово на SETUP. См. [[ввод-от-ворот]].

## [2026-07-28] feat | Живая защита человека при ИИ-ударе от ворот
При Role.NONE (бьёт ИИ-соперник) человек больше не заморожен: играет своей командой (бег/переключение),
но без подката и захода в штрафную, пока мяч не введён. `_setup` морозит только бьющую команду
(team-scoped `set_field_ai_active(false, group)`); `_physics_process` делает ранний return лишь при
`camera_is_owned()`. Заход в штрафную — не-локирующий `_clear_opponent_box`; подкат — гейт
`_goal_kick_active`. Бьющая команда пока стоит (открывания отложены); принцип ляжет на штрафной/угловой.
См. [[ввод-от-ворот]], [[открытые-вопросы]].
